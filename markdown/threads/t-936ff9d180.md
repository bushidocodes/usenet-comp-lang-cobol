[← Index](../README.md) · [Topics](../topics.md) · [Years](../years.md) · [Subjects](../subjects.md) · [Authors](../authors.md)

# Unique identifiers

_7 messages · 3 participants · 2010-02_

---

### Unique identifiers

- **From:** "Pete Dashwood" <dashwood@removethis.enternet.co.nz>
- **Date:** 2010-02-23T16:19:40+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<7ugvqdFfdtU1@mid.individual.net>`

```
I am building some software that will be multiprocessed and multithreaded.

I am beng constrained to write it in COBOL for reasons of history and am 
using Fujitsu NetCOBOL (unmanaged code).

It involves accessing various table sets on a RDB from various machines on a 
network, using objects generated for the purpose. (Data Access Layer (DAL) 
objects generated from the PRIMA Toolset.) Various users, machines, and 
application programs could be simultaneously accessing the same table set, 
each using their own instance of the DAL object that manages that table set. 
As these DAL instances will be in the memory space of each of the 
applications running them, this is not problematic.

However, although the DAL objects handle their own multithreading (for GET 
and PUT operations), they do need to maintain state (especially when cursors 
are opened and being run from COBOL) and this requires around 16K of 
information to be attached to each instance of a DAL object. It is, in 
effect, a stateful context of that thread from that instance of the DAL 
object.

It is a bit inelegant to keep all this stuff in memory so it gets stepped to 
disk. It is currently keyed under the the ID of the DAL object, but once 
things get externalised (to disk) there is a risk of collision and different 
threads retrieving the wrong context. (As noted, multiple platforms could be 
running the same DAL object, just using different instances of it...)

(I thought about using the DAL factory to stamp a serial number on each 
instance, but different processors would have their own factory and use the 
same serial numbers, so collisions would be inevitable, given they are 
sharing the same DB.)

I figured I could serialize the object reference and use that as part of the 
key to the context on disk.

I can't.

There is no way I can find to manipulate an object reference in Fujitsu 
COBOL. You can't REDEFINE it or ref. mod. it or do anything underhand to get 
a definition of it as 8 bytes. They really protect these references 
extremely well.

I considered generating a random number but there is always a risk of 
collision with these. Somehow, I have to make sure that each context on disk 
is uniquely keyed so it can be accessed from any processor on any machine 
without collisions, and the "home base" which originated it has to remember 
that key in its memory space, so it can't be TOO big...

Obviously, we use unique GUIDs  (Generated Unique Identifiers) all the time 
in the Windows environment. CLSIDs are just one form of this and these GUIDs 
are 16 bytes (128 bit) so they aren't too big. (After serializing them into 
the Hex form of what they contain we get a 32 byte string which is 
guaranteed to be unique (or the probability of collision is longer than the 
age of the Universe in hours...)

I figured I should be able to make a quick COBOL call to the Windows API, 
pick up a GUID and go merrily on my way.

However, the Windows API contains no such signature that I can find.

I could do this in one statement from C#; but calling a .NET assembly from 
this particular COBOL is a last resort.

I know SQLServer has a stored procedure that returns one, but I can't be 
sure they will always be using SQL Server and the code really should be DB 
independent, just as the DAL objects are.

Has anyone obtained GUIDs using COBOL?

I know SQLServer has a stored procedure that returns one, but I can't be 
sure they will always be using SQL Server and the code really should be DB 
independent, just as the DAL objects are.

Suggestions appreciated.

Pete.
```

#### ↳ Solved! WAS: Re: Unique identifiers

- **From:** "Pete Dashwood" <dashwood@removethis.enternet.co.nz>
- **Date:** 2010-02-23T18:27:16+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<7uh79mFf09U1@mid.individual.net>`
- **References:** `<7ugvqdFfdtU1@mid.individual.net>`

```
Pete Dashwood wrote:
> I am building some software that will be multiprocessed and
> multithreaded.
…[65 more quoted lines elided]…
>
I found out that the COM support .DLL (Ole32.dll) has exactly such a 
function.

It was just a matter of obtaining a copy of Ole32.lib so I could link the 
routine into COBOL.

Wouldn't you think that Fujitsu would provide such a lib? They provide one 
for the Kernel and User32...

I COULD have downloaded a tool that generates a lib from a dll but it was 
part of a C++ download package that I neither need nor want.

I ran a search on my main development notebook (BIGBLACK... 1.3 million 
files...) and went and made coffee... :-)

Sure enough it found a number of copies of the required .lib. Seems those 
nice people at Microsoft provide .lib versions of  all the system .dlls as 
part of their platform Software Development Kit. It is so you can reuse all 
the functions they have already written into the Operating system (that is, 
the ones that AREN'T COM components...[the COM components are already easily 
accessible without need for the export/import information carried in 
.libs.] ) I had a copy of it within the SDK I installed some years back, and 
another copy within the Visual C support installed with Visual Studio 2008.

I selected what looked like the most recent, copied it to the Fujitsu COBOL 
area (running on a virtual machine called LITTLEBLACK, hosted by BIGBLACK 
:-)), and then added it to my NetCOBOL project as a library.

Coded it:

     call "CoCreateGuid"
     WITH STDCALL
       USING
            BY REFERENCE GUID   *> this is a 16 byte string
       RETURNING returnCode
     end-call

Compiled and linked without problem.

Stepped through in debugger and received 16 bytes of complete garbage in 
GUID and zero in returnCode :-)

Moved the debug line back to re-execute the call and received another 
(different...) 16 bytes of garbage and zero return.

Changed the debug view and suddenly the 16 bytes of garbage becomes 32 bytes 
of HEX symbols that look very much like the registration keys we find on 
boxed software (but without the hyphens...)

The final step will be to pass the 16 bytes to a component I have which 
returns the hex values, then add the 32 bytes to the context key for a given 
DAL object, before writing it to disk.

Now I can sleep without worrying about context collisions across the system, 
and secure in the knowledge that they can run as many concurrent copies of 
their apps as they like and it won't screw the DAL.

Good result :-)

Pete.
```

##### ↳ ↳ Re: Solved! WAS: Re: Unique identifiers

- **From:** Rene_Surop <infodynamics_ph@yahoo.com>
- **Date:** 2010-02-22T21:35:38-08:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<a68f6e77-7155-40dc-82af-e900fd1e5521@o16g2000prh.googlegroups.com>`
- **References:** `<7ugvqdFfdtU1@mid.individual.net> <7uh79mFf09U1@mid.individual.net>`

```
On Feb 23, 1:27 pm, "Pete Dashwood"
<dashw...@removethis.enternet.co.nz> wrote:
> Pete Dashwood wrote:
> > I am building some software that will be multiprocessed and
…[129 more quoted lines elided]…
> "I used to write COBOL...now I can do anything."

MF IDE create (automatically) GUIDs everytime you will add a new class
to a COM application. It is fixed. Such as;

         move z"{085CF6BE-F8C3-4C28-92D6-873E632F4237}" to theClassId
         move z"{0018E7E7-52D9-45C7-9068-375516F410D4}" to
theInterfceId

Though I never tried working on it using Ole32.DLL, it sure did do the
work for me. Let the IDE create one for me.... and adopt it on my
'manually' created object.
```

###### ↳ ↳ ↳ Re: Solved! WAS: Re: Unique identifiers

- **From:** "Pete Dashwood" <dashwood@removethis.enternet.co.nz>
- **Date:** 2010-02-23T23:13:00+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<7uho1eF2k7U1@mid.individual.net>`
- **References:** `<7ugvqdFfdtU1@mid.individual.net> <7uh79mFf09U1@mid.individual.net> <a68f6e77-7155-40dc-82af-e900fd1e5521@o16g2000prh.googlegroups.com>`

```
Rene_Surop wrote:
> On Feb 23, 1:27 pm, "Pete Dashwood"
> <dashw...@removethis.enternet.co.nz> wrote:
…[147 more quoted lines elided]…
> 'manually' created object.

No, that's not the same thing at all, Rene. (although it looks the same... 
:-))

That is a CLSID and the fujitsu COM build also applies one to each COM 
component it builds, as a signature for the registry to uniquely identify 
the Class. This allows COM components to run as remote procedures across 
networks using COM+ and DCOM.

Each DAL object has a CLSID exactly like this. (Because each object is a COM 
object and a Class.) However, what I need is to have the CONTEXT of 
information which is ATTACHED to the object (and MAY be present, depending 
on the environment it is running in - for example, if it is running on a web 
page it doesn't need to maintain state, or it can do so using session 
variabes, but if itis running on a desktop no session variables are 
available so it needs another "state maintenance" facility...).

There are times when it is useful to have a unique identifier on something 
and KNOW that it is unique :-)

Pete.
```

##### ↳ ↳ Re: Solved! WAS: Re: Unique identifiers

- **From:** "James J. Gavan" <jgavandeletethis@shaw.ca>
- **Date:** 2010-02-23T00:02:17-07:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<N3Lgn.3697$BD2.3281@newsfe14.iad>`
- **In-Reply-To:** `<7uh79mFf09U1@mid.individual.net>`
- **References:** `<7ugvqdFfdtU1@mid.individual.net> <7uh79mFf09U1@mid.individual.net>`

```
Pete Dashwood wrote:
> The final step will be to pass the 16 bytes to a component I have which 
> returns the hex values, then add the 32 bytes to the context key for a given 
…[9 more quoted lines elided]…
> 
Dammit and heres me having just finished writing a long message that 
perhaps you could cherry-pick from. I only stopped to see a repeat of 
the Canadian couple win the ice skating Gold. Pleased but not ecstatic. 
Not a patch on the Sarajevo, Torval and Dean. As a really wonderful 
surprise birthday present, way back Eileen bought tickets to see them in 
Calgary. Very entertaining. The final act a blacked out rink. Spotlights 
came on and Torval and Dean wrapped in black cloaks. Their cloaks fell 
to the ice; we all went nuts in acclamation. They were dressed in their 
purple uniforms and to the beautiful mournful music of Ravel's 'Bolero' 
they became magic on ice.

Anyway I'll post it now. Are you absolutely sure you have covered for 
all potential collisions - my reference to locking techniques in what 
I've written. Not checked for typos or spelling, I'll send it after this.

Jimmy
```

#### ↳ Re: Unique identifiers

- **From:** "James J. Gavan" <jgavandeletethis@shaw.ca>
- **Date:** 2010-02-23T00:05:51-07:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<67Lgn.3698$BD2.3250@newsfe14.iad>`
- **In-Reply-To:** `<7ugvqdFfdtU1@mid.individual.net>`
- **References:** `<7ugvqdFfdtU1@mid.individual.net>`

```
Pete Dashwood wrote:

**** As per other message - NOT checked for typos or spelling.

OK I'll do this from the top. Your message follows my comments. (*** 
Ignore that, doesn't need a repeat) See if any of the folowing helps.

When you offered as a reason for use of Factory - using Factory for
instance IDs, where you incrmented a number for all instances - and
attached the result to an SQL Table identifier. You didn't provide a
complete example, so wasn't totally clear on what you wanted to do -
thinking back I worked on the assumption you were using one instnce for
'Gets" and the other for "Writes or sets".

***** an Aside issue.

If you look at IBM, Fujitsu and M/F all three are somewhat 'silent' on
the use of Factory, telling you can use it, how you can access it etc.,
but no examples. That's not stricly true with M/F :-

invoke ClassA "new"  which contains a Factory method "new" which hits
class Base to get the new object. So via linkage you get back to the
caller program/class with an instance object reference. But their
specific version from the classes they wrote also tell lnk-self to
invoke method "initialize" which is in the Instance/Object. Young Simon
Hart was horrified when one of the really good tech supporters at
Newbury suggested that approach.

"I don't do it that WAY" he protested. Nor do I, so I wasn't horrified.
GUI-wise I want to pass the Desktop or an Application Window as the
parent, along with a Resource.dll (not dissimilar to your F/J approach
where you drag icons around the draft dialog - the finished dialogs end
up in MyResource.RC which the compiling procedure turns it into
MyResource.DLL). This is the bit that seemed daft to me :-

Sensibly if using the Factory "new" apporach it should be :-

invoke ClassA "new" using os-Parent os-Resource
within ClassA Factory "new" method :-
invoke lnk-self (the reference to the new instance) "initialize"
			using os-Parent os-Resource

Seeing no justificaton for Factory that appeared to me as a two-step
game. My Factories ae always commented out *> but are there to indicate
the class structure. So no "new" method in Factory and invoke as follows :-

invoke ClassA "new" returning os-NewClass
invoke os-NewClass "main" ( "begin" or whatever) using os-Parent,
os-Resource

Method "begin/main/whatever"
---------------------------
set os-Parent to lnk-Parent
set os-Resource to ln-Resource

I've never had any problem ignoring their own "initialize" method.

Now when it comes to using the Factory as an incrementer. Guess who uses
the same technique as you. And it's the only example they give for
Factory; otherwise they are silent, like other vendors. The compiler
vendor Veryant in Italy - and I betcha they pinched the idea from you
here in CLC :-)

end of aside *************

Back to problem at hand. There are some subtle difference between F/J
and M/F and the most obvious the approach to collection classes. This is
what got me truly pissed off with J4. With due respect to his various
efforts, Thane as the then F/J J4 representative pushed the Fujitsu
approach. I've already indicated what I think about that. Waited and
waited for further explanation and wrote to Thane - no reply; meanwhile
he got the shaft by F/J. Bib Karlin, never met him took over the
collection assignment. I wrote to him - no reply. Chuck Stevens used to
write here, write to him again, he's a busy man. Some am I, and he's
earning a bllody sight more than I am.

Here's the point: looking zt the F/J on-line manuals I found a reference
to tutorials; same appli3es to M/F. But these damned tutorials only give
you salient code, not the complete project. As users of the vendor's
software on your CDs you have copies of the complete source they ae
picking bits from. I think you'll understand what I'm writing, I hope !
Using what I refer to as the Record Class, and I think this is a feature
you used with F/J, retrieve a record from a COBOL file or a DB and it is
returned to an instance of RecordBox class for the record/row. Then your
COBOL code is, as I recall  :-

move CustomerName of ThisInstanceOfRecordBoxClass to ws-customername

I got the impression from the tutorial that's how F/J handled elements
in a collecton. Could be wrong but you've got a whole bunch of
'ElementObjects' handing around. And it was this I was specifically
querying from Messrs. Hubble and Karlin. I could see immediately why
there was no reference to SortedCollections - it might well have been a
monster. Now I know GUI-ing they have to produce collections to feed
Treeviews, Listboxes, Listviews - which invariably you the developer
want in Ascending sorted sequence. How they do it God only knows.

I don't know whether or not I've recently highlighted the approach, but
by using the Class CharacterArray I can turn text into a display object
and viced versa, say when coming back from a Dialog with an entry field.
Fairly recentl;y I've adaptedc to getting aaway from sneind /receiving
strings. Using that CharacterArray class I transmit them as 'displayable
objects'.

Now on to your SQL Example and using unique identifers for the original
DB problem you posed. I took your 'problem', well 'solution', and
adapted. I tried several things :-

WORKING-STORAGE (OBJECT STORAGE originallly in M/F, hence I haven't got
away from prefixing my objects as os-CustomerRecord etc.)

01 os-TableCollection		object-reference value null.

Create the above and just add an instance of the DB-Table as it occurs.
Unlimited number, but sure a problem trying to remeber which is which if
you get to 99. Nevertheless as each instance is added to an
OrderedCollection they are in ascending sequence of being allocated.
It works, but I wasn't too thrilled about that so I tied this,
particularly as you were referring to one Table with two instances.

01 Os-TableObject occurs 2 	object reference value null.

Either use a perform varying n from 1 by 1 until n > ???

invoke CustomerTable "new" returning os-TableObject (n)

OR :-

invoke CustomerTable "new" returing os-TableObject (1)
invoke CustomerTable "new" returning os-TablObject (2)

Now at some time in the application you are going to want to switch
between the "Get" and "Set" instances, which I understood to be what you
wanted to do. Exactly the same approach I'm using with my DateAndsTime;
assuming you wanted day and month names in either English or Portuguese.

set os-CuurentObject to os-TableObject (1)

and later on :-

set os-CuurentObject to os-TableObject (2)

You can then use general methods applicable to both or only those
aplicable to the specific instance. I'll re-read your message before I
finish, but I queried with M/F when they were still supporting the
'oldie' versions, there is a DB SQL Statement that hels dentify which
instance of the DB Tabke. Realy couldn't figure that out, but recalled I
had asked the same sort of question from M/F much earlier, perhaps two
years, but in no way related to DBs. Clearing up papers I luckily came
acros the generalised routine.

I proved to my satisfaction (using the 'set B to A' technique) that I
definitely was in the correct instance, depending upon the methods I
wanted to use. Here's the code I used to prove it :-

*>--------------------------------------------------------------
Method-id. "GetInstanceHexID".
*>---------------------------------------------------------------
WORKING-STORAGE SECTION.
01 ls-Comp5                        object reference.
01 ls-Value                        object reference.

     invoke COBOLComp5 "withValue" using self
            returning ls-COMP5
     invoke ls-COMP5 "asHex" returning ls-Value

EXIT METHOD.

*> The following are from setting Breakpoints on the Animator
*> having done the above code.

*> The Hex values appearing below will differ from machine
*> to machine, but should be consistent.
*> "GetInstanceHexID" is invoked from the methods shown below.
*> "Self" is the Animator object Hex reference for the two
*> instances of this class

*> Animator Results :   Ist Instance (Get) 2nd Instance (Put)
*>
*> Method : "begin" :-
*>
*>    self              91 00 00 00         92 00 00 00
*>    ls-Comp5          93 00 00 00 145     96 00 00 00 146
*>    ls-chars          94 00 00 00 '91'    95 00 00 00 '92'
*>
*> Method : "makeCollection" :-
*>
*>    self              91 00 00 00             NULL OBJECT
*>    ls-Comp5          98 00 00 00 145
*>    ls-chars          9C 00 00 00 '91'
*>
*> Method : RewriteRecord" : -
*>
*>    self              NULL OBJECT         92 00 00 00
*>    ls-Comp5                              A2 00 00 00 146
*>    ls-chars                              A1 00 00 00 '92'

End Method  "GetInstanceHexID".
*>--------------------------------------------------------------

Before double-checking your text to see if I've covered all bases, I'll
mention the following. Just might have some relevance to you. While
searching through the Web for tips on DateAndTime I came across a
free-standing page M/F originated, (presumably), but no cross-reference
to any of their sites. Starting in Column 1 it goes on and on without
any breaks. It has an elapsed time formula, which the author indicates
can be used with RANDOM function to 'get slices of time', useful in
multi-threading applications. It's been tested using N/E, Server Express
and some 6 or more Unix/Linux type OS's. If it might be pertinent, and
you can't find it, I can send a copy if interested.

Just looked at your text - where you talk about being constrained by F/J
on objects - can you not use the :-

set os-CurrentObject to os-ObjectA (or to TableObject(1) )
set os-CurrentObject to os-ObjectB (or to TableObject(2) )
set os-CurrentObject to os-ObjectC etc..... as required ?

If you can't then F/J are breaking the J4 rules.

I'm not suggesting any of the above is applicable to your desired
solution, but I've spelled out a lot of stuff so you can cherry-pick on
ideas, to see if they fit.

I can see the problem with different users and retaining unique ID's as
to who is doing what. And if two are in effect hitting the same Table
you are into a multi user environment with the equivalent of some record
locking. At this stage that's a big blank. I would have to ponder more,
or throw some quesitons back at me - which wont necessqrily give you the
answers you want :-).

To give credit where it is due, Artur Reitman the former F/J J4 member
picked up on this with regards to collectons. Myopically viewing
collections with a single user - I gave no thought to multi users
accessing the same collection. But again some record-locking technique
would be required on a 'multi-used' collection.

Jimmy
```

##### ↳ ↳ Re: Unique identifiers

- **From:** "Pete Dashwood" <dashwood@removethis.enternet.co.nz>
- **Date:** 2010-02-24T01:13:55+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<7uhv46F9snU1@mid.individual.net>`
- **References:** `<7ugvqdFfdtU1@mid.individual.net> <67Lgn.3698$BD2.3250@newsfe14.iad>`

```
James J. Gavan wrote:

Thanks for the response, Jimmy.

As you will be aware, the problem is now solved but I had a look through 
your post anyway and made some comments, along with some snippage; I don't 
want another rap over the knuckles from DD about unnecessary quoting... :-)

> Pete Dashwood wrote:
>
…[7 more quoted lines elided]…
> attached the result to an SQL Table identifier.

That was a long time ago and things have moved on. The SQL Table identifier 
is now a Table SET "stem". (A table set is the normalized implementation of 
a COBOL record that contains OCCURS fields. There is a base table and 
attached tables for each repeating group in compliance with 2NF.) So, yes, I 
DO use the Factory to provide an instance identifier which is then used for 
threading. There will be a GET and a PUT instance and the interface blocks 
used by each contain this instance stamp so the wrong interface is not used 
with the wrong instance. The cool thing about this is that the instance 
stamp is also the DB connection name, so each instance gets its own DB 
connection and they hold and release these connections from the pool during 
concurrent running. All of this is fine if you are running on a single 
processor. But if you have 2 processors churning out instances from the 
factory they COULD get the same instance stamp. Again it doesn't matter 
because they are running in separate address spaces so the threading will 
still work properly within each space.

You didn't provide a
> complete example, so wasn't totally clear on what you wanted to do -
> thinking back I worked on the assumption you were using one instnce
> for 'Gets" and the other for "Writes or sets".

Yes, more or less... There aren't always 2 instances; it depends on a number 
of factors.
>
> ***** an Aside issue.
>
Sorry, Jimmy, I don't want to get sidetracked, as time is limited and I have 
no issues with Factory methods in OO anyway. (I use them in C# and OO COBOL 
where it suits my purposes.)


<snipped stuff about vartious vendors and Factory>

>
> Back to problem at hand. There are some subtle difference between F/J
> and M/F and the most obvious the approach to collection classes.

I got to this through VB (which has excellent collection classes available). 
C# has even more and it even implements stack and queue collections as Base 
Classes that have their own POP and PUSH methods. I have used these on 
several occasions and found them so simple and easy it was a delight. String 
Collections are also a Base Class and there are Base Classes for a number of 
other Classes, including a "Collection" Class that can have any type you 
care to define. Node Classes are a specialised form of collection which is 
particularly useful for Tree Views, and Fujitsu support these collections 
for NetCOBOL.

I can honestly say I haven't yet found a Collection need that wasn't 
fulfilled in one or other of the maor langages, so I don't see it as a 
problem.

<snipped J4 on Collections - Hey, let it go, Jimmy. It was years ago and it 
doesn't matter anyway... :-)>

> Here's the point: looking zt the F/J on-line manuals I found a
> reference to tutorials; same appli3es to M/F. But these damned
> tutorials only give you salient code, not the complete project.

Fujitsu do provide sample code and projects and they do document it in their 
"getting started" manuals (which I have not looked at for a VERY long time, 
having gotten started nearly 20 years ago... :-)). Occasionally when 
something doesn't work the way I think it should I will refer to the 
appropriate sample project and see what options they are exercising in the 
project, but it is very rare and I don't do COBOL any more (except for this 
current project which I am kind of enjoying but know I could complete much 
quicker in C#... :-))


> As
> users of the vendor's software on your CDs you have copies of the
> complete source they ae picking bits from. I think you'll understand
> what I'm writing, I hope !

Nope, sorry, you lost me here. I don't pick bits of code off CDs.


>Using what I refer to as the Record Class,
> and I think this is a feature you used with F/J,

Not me. I never heard of a "Record Class".


retrieve a record
> from a COBOL file or a DB and it is returned to an instance of
> RecordBox class for the record/row. Then your COBOL code is, as I
> recall  :-
> move CustomerName of ThisInstanceOfRecordBoxClass to ws-customername

No. I've never done that and I wouldn't... Are you thinking about bound 
controls, maybe? Where the GUI control is bound to a datasource and changes 
to the source are automatically reflected on the screen (and vice versa)? 
PowerCOBOL has this but it is pretty obsolete now and I have never used that 
feature of it personally.


>
> I got the impression from the tutorial that's how F/J handled elements
…[4 more quoted lines elided]…
> a monster.

No, I don't think so. Collections generally implement SORT as a method of 
the Base class if you want to use it, just as indexing is implemented 
through the base class for most collections.


>Now I know GUI-ing they have to produce collections to feed
> Treeviews, Listboxes, Listviews - which invariably you the developer
> want in Ascending sorted sequence.

Well, for PowerCOBOL it is really simple (I wrote an article on this around 
10 years ago and it is still being referenced in Latin America.) Basically, 
you set up a node collection and it already has a Sort method in it. 
Controls are designed to invoke it if you click on the Heading. It is pretty 
fundamental and basic really. You can easily enhance the nodes to include 
icons and bit maps.

In C# the whole thing is much sexier and I recently added some Tree and List 
Views to the Toolset. It is really cool because you can easily collapse and 
expand nodes, and even have secondary views created dynamically from any 
node. It is a very good way to present hierarchic information.




How they do it God only knows.

(Not ONLY God...:-))

>
> I don't know whether or not I've recently highlighted the approach,
…[4 more quoted lines elided]…
> them as 'displayable objects'.

Yes, that's probably better. Have a look at "nodes"... they are very useful.
>
> Now on to your SQL Example and using unique identifers for the
> original

Did I post an SQL example?  Doesn't sound like me... Not sure what you're 
referring to here, sorry.

> DB problem you posed. I took your 'problem', well 'solution', and
> adapted. I tried several things :-
…[7 more quoted lines elided]…
> occurs.

What "DB-Table"? I'm completely lost here Jimmy. Please be more specific as 
to what you are referring to.

<snipped discussion as it just baffled me. Sorry.>

 I think there may be some confusion here over what it is I need to avoid 
colliding with.

It isn't about Collections or table instances or anything like that.

There is an Object that provides all possible actions against a table set. 
(DAL object). Get sequential (next and previous), get Random, Add, Change, 
and Delete. One (or more) instance(s) of it can be used for GET activities; 
one (or more) instance(s) of it can be used for PUT activities; or One (or 
more) instance(s) of it can be used for  both GET and PUT activities, all 
from the same application or any given address space.

Each instance has its own interface block containing data and control 
information. These are stamped so we know which instance they belong to. 
(the object will reject an interface block which is not its own...)

An application might access 3 table sets (using a minimum of three object 
instances) and it could also be running joins across these table sets 
(although this is generally catered for by views- so I won't muddy the water 
by discussing that further...)

The various instances all process correctly and are multithreaded 
automatically. Typically, sequential access will be running on one thread 
while intermittent updates are being made on another. It is fast and 
efficient.

In such a scenario, the interface blocks for each instance are held in the 
address space so there is no problem.

However, once these blocks (context) are stored externally (say in a central 
disk location) it becomes stickier...

Consider the same application running in another address space, with 
interface blocks (etc) all being stored at a central location. The 2 address 
spaces (processes) are running the same software, so the same identifiers 
will be assigned to the interface blocks etc. (contexts). They need to be 
uniquely identified so that each of the processes gets the contexts that 
belong to it. (each process holds its own unique identifier in its address 
space so it will always get what it spawned, and not what any other process 
spawned.)

I can't really explain it any clearer than that without a whiteboard or 
visio... :-)

Leaving this exercise aside, there are cases where it is useful to have 
something uniquely keyed, so being able to assign such an identifier may 
come in handy for some people.

I therefore took the trouble to post the solution :-)



   >
> I proved to my satisfaction (using the 'set B to A' technique) that I
…[14 more quoted lines elided]…
> EXIT METHOD.

That's interesting. Fujitsu does not allow messing with object references in 
ANY way...
>
> *> The following are from setting Breakpoints on the Animator
…[30 more quoted lines elided]…
>

I don't have these facilities  (probably just as well... :-)) so the GUID 
does it for me.

> Before double-checking your text to see if I've covered all bases,
> I'll mention the following. Just might have some relevance to you.
…[8 more quoted lines elided]…
> pertinent, and you can't find it, I can send a copy if interested.

Thanks Jimmy, I appreciate your time. I too came across some unbelievable 
instances of code designed to give random ids (some in VB were quite 
graceful, almost like a ballet :-))

None of them are as collision proof as the GUID.
>
> Just looked at your text - where you talk about being constrained by
…[6 more quoted lines elided]…
> If you can't then F/J are breaking the J4 rules.

Yes, objects and object stored references can be passed around like that but 
it doesn't really help solve the problem.

On reflection I realized that an 8 byte object reference would be more 
likely to collide than a 16 byte GUID anyway.

The facility is there and it is available as part of the OS (well, an 
extension to it if you like...). Other languages use it all the time; the 
trick was doing it from COBOL.
>
> I'm not suggesting any of the above is applicable to your desired
> solution, but I've spelled out a lot of stuff so you can cherry-pick
> on ideas, to see if they fit.

I appreciate the post. I have spent more time than I can really afford to 
respond, accordingly.
>
> I can see the problem with different users and retaining unique ID's
…[5 more quoted lines elided]…
> the answers you want :-).

Yes, I will be looking at locking on Thursday... :-). Mostly, the DAL uses 
Transact-SQL to take care of it but the spec. (which I have largely deduced 
from existing system facilities :-)) says they want to manually lock certain 
rows on the RDB so I need to provide it.
>
> To give credit where it is due, Artur Reitman the former F/J J4 member
…[4 more quoted lines elided]…
>
Again, transactional processing can provide it.

Also, it wouldn't be too hard to build  "lock" and "unlock" methods into the 
base class of a collection. It really depends on the nature of the 
collections and the nature of the processing.

Pete.
```

---

[← Index](../README.md) · [Topics](../topics.md) · [Years](../years.md) · [Subjects](../subjects.md) · [Authors](../authors.md)
