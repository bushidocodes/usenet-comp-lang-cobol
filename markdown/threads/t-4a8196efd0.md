[← Index](../README.md) · [Topics](../topics.md) · [Years](../years.md) · [Subjects](../subjects.md) · [Authors](../authors.md)

# Confusion calling subprograms

_6 messages · 3 participants · 2004-07_

---

### Confusion calling subprograms

- **From:** Andy <andy@dontbother.com>
- **Date:** 2004-07-13T16:34:58+00:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<cd12vi$ngt$1@hercules.btinternet.com>`

```
Hi All,

Bit of a newby question I'm afraid:

I'm using MF Cobol on Solaris and I'm trying to get one program to call 
a function in another program but I keep getting linker errors. Ideally 
the function I was calling would reside in a shareable library (.so) so 
that multiple applications could access it.

Does anybody have a noddy example of how to write and build such a system?

Cheers,
Andy
```

#### ↳ Re: Confusion calling subprograms

- **From:** riplin@Azonic.co.nz (Richard)
- **Date:** 2004-07-13T15:13:55-07:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<217e491a.0407131413.7db41453@posting.google.com>`
- **References:** `<cd12vi$ngt$1@hercules.btinternet.com>`

```
Andy <andy@dontbother.com> wrote 

> I'm using MF Cobol on Solaris and I'm trying to get one program to call 
> a function in another program but I keep getting linker errors. Ideally 
> the function I was calling would reside in a shareable library (.so) so 
> that multiple applications could access it.

What is it that you are calling a 'function' ?

If it is a nested program, then you cannot call that directly as all
nested programs are local to the compile unit it is contained in.  You
could call the containing program with parameters to indicate that it
should call the nested program and pass back the results.

Or you could add an ENTRY statement in that compile unit which calls
the 'function' and returns.  Note that an ENTRY is not a paragraph
label so do be careful that you don't get a drop-through into this.

The fact that you are getting 'linker errors' indicates that you are
not using dynamic calls.  There is a compiler option that indicates
whether CALL "literal" should be a static call or a dynamic one.
 
> Does anybody have a noddy example of how to write and build such a system?

Yes, I have several.
```

#### ↳ Re: Confusion calling subprograms

- **From:** riplin@Azonic.co.nz (Richard)
- **Date:** 2004-07-13T15:17:42-07:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<217e491a.0407131417.314fb3ce@posting.google.com>`
- **References:** `<cd12vi$ngt$1@hercules.btinternet.com>`

```
Andy <andy@dontbother.com> wrote 

> I'm using MF Cobol on Solaris and I'm trying to get one program to call 
> a function in another program but I keep getting linker errors. Ideally 
> the function I was calling would reside in a shareable library (.so) so 
> that multiple applications could access it.

What is it that you are calling a 'function' ?

If it is a nested program, then you cannot call that directly as all
nested programs are local to the compile unit it is contained in.  You
could call the containing program with parameters to indicate that it
should call the nested program and pass back the results.

Or you could add an ENTRY statement in that compile unit which calls
the 'function' and returns.  Note that an ENTRY is not a paragraph
label so do be careful that you don't get a drop-through into this.

The fact that you are getting 'linker errors' indicates that you are
not using dynamic calls.  There is a compiler option that indicates
whether CALL "literal" should be a static call or a dynamic one.
 
> Does anybody have a noddy example of how to write and build such a system?

Yes, I have several.
```

#### ↳ Re: Confusion calling subprograms

- **From:** riplin@Azonic.co.nz (Richard)
- **Date:** 2004-07-13T15:18:15-07:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<217e491a.0407131418.6c7c4a1d@posting.google.com>`
- **References:** `<cd12vi$ngt$1@hercules.btinternet.com>`

```
Andy <andy@dontbother.com> wrote 

> I'm using MF Cobol on Solaris and I'm trying to get one program to call 
> a function in another program but I keep getting linker errors. Ideally 
> the function I was calling would reside in a shareable library (.so) so 
> that multiple applications could access it.

What is it that you are calling a 'function' ?

If it is a nested program, then you cannot call that directly as all
nested programs are local to the compile unit it is contained in.  You
could call the containing program with parameters to indicate that it
should call the nested program and pass back the results.

Or you could add an ENTRY statement in that compile unit which calls
the 'function' and returns.  Note that an ENTRY is not a paragraph
label so do be careful that you don't get a drop-through into this.

The fact that you are getting 'linker errors' indicates that you are
not using dynamic calls.  There is a compiler option that indicates
whether CALL "literal" should be a static call or a dynamic one.
 
> Does anybody have a noddy example of how to write and build such a system?

Yes, I have several.
```

#### ↳ Re: Confusion calling subprograms

- **From:** robert.deletethis@wagner.net (Robert Wagner)
- **Date:** 2004-07-13T23:59:51+00:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<40f47754.186695651@news.optonline.net>`
- **References:** `<cd12vi$ngt$1@hercules.btinternet.com>`

```
>I'm using MF Cobol on Solaris and I'm trying to get one program to call 
>a function in another program but I keep getting linker errors. Ideally 
>the function I was calling would reside in a shareable library (.so) so 
>that multiple applications could access it.

Compile the .so with -z. Compile the main with -x. Make sure the .so is in
LD_LIBRARY PATH. It will work like a charm. If it doesn't, do an ldd on the
executable.
```

#### ↳ Re: Confusion calling subprograms

- **From:** robert.deletethis@wagner.net (Robert Wagner)
- **Date:** 2004-07-14T08:38:31+00:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<40f4ed8d.216964974@news.optonline.net>`
- **References:** `<cd12vi$ngt$1@hercules.btinternet.com>`

```
>I'm using MF Cobol on Solaris and I'm trying to get one program to call 
>a function in another program but I keep getting linker errors. Ideally 
>the function I was calling would reside in a shareable library (.so) so 
>that multiple applications could access it.

In the simplest case, program a calls b. Both are in the current directory. "b"
is both the program name and entry point.

1.  Make sure the current directory is in your path. If not, "export
PATH=.:$PATH" (dot colon)
2. Make sure the current directory is in LD_LIBRARY_PATH. If not, do the same to
it.
3. cob -xg a.cbl 
        Output will be a file named "a". Option g lets you run the debugger.
   cob -zg b.cbl
        Output will a file named "b.so".
4. Execute a by typing "a"

If that doesn't work, type "ldd a". Run the debugger by typing "anim a". You can
call b from the command line by insuring current directory is in COBPATH and
typing "cobrun b".
```

---

[← Index](../README.md) · [Topics](../topics.md) · [Years](../years.md) · [Subjects](../subjects.md) · [Authors](../authors.md)
