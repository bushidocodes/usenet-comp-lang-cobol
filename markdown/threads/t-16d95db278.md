[← Index](../README.md) · [Topics](../topics.md) · [Years](../years.md) · [Subjects](../subjects.md) · [Authors](../authors.md)

# COBOL and SQL Server

_1 message · 1 participant · 2012-08_

**Topics:** [`Databases and SQL`](../topics.md#databases)

---

### COBOL and SQL Server

- **From:** "Pete Dashwood" <dashwood@removethis.enternet.co.nz>
- **Date:** 2012-08-18T14:12:33+12:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<a98c0jFir2U1@mid.individual.net>`

```
Recently I have had to revise the use of DAO with SQL Server, due to 
limitations in the DAO implementation from Microsoft which were put there in 
the days when people used Access databases.

Data Access Objects (DAO) is an excellent platform independent model which 
works across most Relational databases. (I have used it successfully with 
MySQL, PostgreSQL, Access 2003, Access 2007, Oracle, SQL Lite and SQL 
Server.)

It lets you issue and execute Data Description Language (DDL) as well as 
Data Manipulation Language (DML), while most COBOL implementations of  SQL 
only support DML. (You can't CREATE, ALTER, and DROP SQL tables from COBOL 
with embedded SQL, for example...)

The quickest way to get to the DAO objects was to use the MS Access Jet 
Engine and this works fine, even with non-Access DBs (which it should do, as 
it is an object model and should be platform independent.)

However, there are some limitations on the MS implementation of DAO, which I 
believe may heark back to the days when they built Access.

The main one is that it doesn't support tables with more than 255 columns.

Now, you can argue that 255 columns for a single table is more than adequate 
and only poor RDB design would require more, but that is an argument for 
some other place and time...

I spent the last couple of weeks learning, designing, and developing  the 
use of SQL Server Management Objects (SMO) as an alternative to DAO, for SQL 
Server.

http://msdn.microsoft.com/en-us/library/ms162169.aspx

I have abstracted the complexity out of it and produced a COM component 
(SMOGateway) that can be used from COBOL. Currently, it implements DML but 
adding DDL support is a simple matter of adding some more methods.

Using the component, you can obtain full programmatic control of  any SQL 
Server system, and you can do it from COBOL (or any language that supports 
COM). Navigate the hierarchy and get all the RDBs on a server, all the 
tables in a given RDB, all the columns in a given table (and the most 
important properties of each column, like type, size, etc., all under the 
control of your COBOL program.)

If there is interest, I am prepared to make the basic component available to 
people here, for free. If you have specific requirements not currently 
implemented (create, alter, and drop tables, schedule RDB Admin Tasks, 
control replication/backups, and so on) you should talk to me.

There is also a GUI enabled "harness" that I wrote to test the component. 
This is a useful little tool that can sit on the task bar and give you 
information about your SQL Server system with a mouse click.

The component has already been implemented into the PRIMA Migration Toolset 
and is proving to be an excellent replacement for DAO when using SQL Server 
systems. It has no limitations we have found yet...

Pete.
```

---

[← Index](../README.md) · [Topics](../topics.md) · [Years](../years.md) · [Subjects](../subjects.md) · [Authors](../authors.md)
