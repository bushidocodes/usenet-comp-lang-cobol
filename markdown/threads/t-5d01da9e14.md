[← Index](../README.md) · [Topics](../topics.md) · [Years](../years.md) · [Subjects](../subjects.md) · [Authors](../authors.md)

# invoking a method

_35 messages · 15 participants · 2006-12_

**Topics:** [`Object-oriented COBOL`](../topics.md#oo)

---

### invoking a method

- **From:** Frank Swarbrick <infocat@earthlink.net>
- **Date:** 2006-12-02T20:05:21+00:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net>`

```
Looking at examples supplied with MF Net Express reminded me of 
something that seems odd to me about all OO languages I've looked in to. 
  They pretty much all invoke methods in a somewhat similar format in 
that the object comes first, followed by the name of the method, 
followed by any parameters.

COBOL 2002
   invoke object "method" using by value parm1, parm2
MF COBOL
   invoke object::"method"(parm1, parm2)
Java
   object.method(parm1, parm2)
Objective-C
   [object method:parm1 parm2name:parm2]
Ruby
   object.method parm1, parm2

One thing that I've come to believe is that it would often be more 
"natural" (for me) if the method name was specified first.  The method 
is very often a verb.  So why not something like:
   insert employee into list

where 'insert' is the method name, 'employee' is the object to be 
inserted (the parameter) and 'list' is the object that is to receive the 
message.  'into' is just a noise word to make it more readable.

What follows below is code from one of the MF ISO2002 examples, along 
with modified versions in other languages.  The last "language" is just 
something I've been toying with.  Right now it's a 2000+ line text 
document of my thoughts on an 'ideal' (hah!) programming language. 
Probably that's all it will ever be.  But still, it keeps me off the 
streets.

COBOL 2002:
  working-storage section.
  01 displayList usage object reference DLinkList.
  procedure division.
      set displayList to DLinkList::"new"
      invoke displayList "add" using by value emp1
      invoke displayList "add" using by value car1
      invoke displayList "add" using by value indPlace1
      invoke displayList "add" using by value car2
      move listSize of displayList to noItems
      if noItems > 0
          invoke displayList "getfirst" returning displayItem
          invoke displayItem "display"
          perform until exit
              set displayItem to displayList::"getnext"
              if displayItem not = null
                  invoke displayItem "display"
              else
                  exit perform
              end-if
          end-perform
      end-if
      exit program.

Micro Focus COBOL:
  working-storage section.
  01 displayList DLinkList.
  procedure division.
      set displayList to DLinkList::"new"
      invoke displayList::"add"(emp1)
      invoke displayList::"add"(car1)
      invoke displayList::"add"(indPlace1)
      invoke displayList::"add"(car2)
      move displayList::"listSize" to noItems
      if noItems > 0
          set displayItem to displayList::"getfirst"
          invoke displayItem::"display"
          perform until exit
              set displayItem to displayList::"getnext"
              if displayItem not = null
                  invoke displayItem::"display"
              else
                  exit perform
              end-if
          end-perform
      end-if
      exit program.

Java:
{
     DLinkList displayList;

     displayList = new DLinkList();
     displayList.add(emp1);
     displayList.add(car1);
     displayList.add(indPlace1);
     displayList.add(car2);
     noItems = displayList.listSize();
     if (noItems > 0) {
         displayItem = displayList.getfirst();
         displayItem.display();
         while () {
             displayItem = displayList.getnext();
             if (displayItem != NULL)
                 displayItem.display();
             else
                 break;
         }
     }
}

Objective-C:
{
     DLinkList *displayList;

     displayList = [[DLinkList alloc] init];
     [displayList add:emp1];
     [displayList add:car1];
     [displayList add:indPlace1];
     [displayList add:car2];
     noItems = [displayList listSize];
     if (noItems > 0) {
         displayItem = [displayList getfirst];
         [displayItem display];
         while () {
             displayItem = [displayList getnext];
             if (displayItem != NULL)
                 [displayItem display];
             else
                 break;
         }
     }
}

Ruby:
begin
     displayList = DLinkList.new
     displayList.add emp1
     displayList.add car1
     displayList.add indPlace1
     displayList.add car2
     noItems = displayList.listSize
     if noItems > 0
         displayItem = displayList.getFirst
         displayItem.display
         while true
             displayItem = displayList.getnext
             if displayItem
                 displayItem.display
             else
                 break
             end
         end
     end
end

"Frank's programming language":
begin
     displayList := new DLinkList$
     [add emp1 to displayList]
     [add car1 to displayList]
     [add indPlace1 to displayList]
     [add car2 to displayList]
     noItems := displayList.listSize  #(listSize is a property)#
     test noItems
     when gt 0
         displayItem := [getfirst displayList]
         [display displayItem];
         loop until exit
             displayItem = [getnext displayList]
             test displayItem
             when true
                 [display displayItem]
             when false
                 exit
             end
         end
     end
end

Like Objective-C, I've wrapped brackets around the object invocation. 
To me it makes it a bit more readable, plus it would probably making 
source code parsing simpler.  If the method is simply a "property" (or 
"attribute", depending on your language preference) it would be:
   object.property
I suppose you could also have
   [property of object]
but I prefer the former.  Plus, while I can see
   object1.property := object2.property
I can't really picture:
   [property of object] := [property of object]
Well, I can, but again I prefer the former.
A regular (non-OO) "function call" would probably just be:
   func(parm1, parm2)

Comments and constructive criticism welcomed.  Ignoring my nonsense is 
perfectly fine.  Comments like "don't you have better things to do with 
your time then create yet another programming language" will be ignored. 
   I have no illusions of this "language" going anywhere beyond this 
text document.  :-)

Frank
```

#### ↳ Re: invoking a method

- **From:** "P. Raulerson" <paul.rl@raulersons.com>
- **Date:** 2006-12-02T17:46:34-06:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<Afoch.628$i37.580@newsfe13.lga>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net>`

```
You should look into Smalltalk- right up your alley. Of course, it drives 
those of us who are indoctrinated with procedural languages right crazy...

I did not know that the MF had a new "University Edition" out, the last one 
I knew of was version 3.0. Wayyy behind the times. This one may be a little 
old as well, which is why you are picking up errors. The University edition 
is a way that MF can make a bit of cash off older - basically 
non-supported - versions of the product.

-Paul


"Frank Swarbrick" <infocat@earthlink.net> wrote in message 
news:50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net...
> Looking at examples supplied with MF Net Express reminded me of something 
> that seems odd to me about all OO languages I've looked in to. They pretty 
…[192 more quoted lines elided]…
> Frank
```

##### ↳ ↳ Re: invoking a method

- **From:** Frank Swarbrick <infocat@sprynet.com>
- **Date:** 2006-12-03T18:59:07+00:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<%7Fch.6392$tM1.4619@newsread1.news.pas.earthlink.net>`
- **In-Reply-To:** `<Afoch.628$i37.580@newsfe13.lga>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <Afoch.628$i37.580@newsfe13.lga>`

```
P. Raulerson wrote:
> You should look into Smalltalk- right up your alley. Of course, it drives 
> those of us who are indoctrinated with procedural languages right crazy...
…[5 more quoted lines elided]…
> non-supported - versions of the product.

Hmm, I've looked at Smalltalk and never found it particularly readable. 
  Or understandable.  :-)  I am definitely a procedural language 
programmer.  I find OO stuff to be fascinating, but not always 'natural'.

NE 5.0 is the "latest and greatest".  Just released this year.  You may 
be thinking of Fujistu, which I believe gives away 3.0 of their product. 
  As far as I know the NE "University Edition" is pretty much full 
functioning, other than only allowing up to 2200 lines of source code 
per executable.  See the following link for more information:
http://www.microfocus.com/Resources/Communities/Academic/shop/description.asp?productid=52

Frank
```

###### ↳ ↳ ↳ Re: invoking a method

- **From:** "P. Raulerson" <paul.rl@raulersons.com>
- **Date:** 2006-12-03T13:27:17-06:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<wyFch.11138$YI1.3059@newsfe15.lga>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <Afoch.628$i37.580@newsfe13.lga> <%7Fch.6392$tM1.4619@newsread1.news.pas.earthlink.net>`

```
I got to talk once with a man named Dan Ingalls, just for a few minutes. He 
impressed me that Smalltalk could actually be a language worth using. Years 
later I ran into VisualAge Generator, and convinced myself beyond any 
possible doubt that Smalltalk and the RealWorld should not be allowed to 
meet, interact, or seriously depend upon each other. The Sprint billing and 
customer service system tends to support that point of view. ;)

Okay, I really do think Smalltalk has it's uses and is a pretty cool 
language. I do find it difficult to interface with the existing "real world" 
of COBOL, RPG, Assembler, C, and Java. Very good concepts in it, and where 
it DOES work and work exceptionally well, is when it is used to produce 
visual  interfaces. It really shines in that area, and with good reason of 
course.

Since this discussion has taken off on an unexpected turn, I offer this 
link. It is a (legal or not) copy of a paper written by Andy Kay, and it 
really cool. ;)

http://www.iam.unibe.ch/~ducasse/FreeBooks/SmalltalkHistoryHOPL.pdf

To really throw a zinger into the discussion, Ada is probably the best 
example of an implemented OOL that can actually be used to get things done. 
That is of course, my opinion, but check it out. The Algol background on the 
language gives rise to really intuitive syntax, and the "real world" things 
in it, like exception handling (and some COBOL heritage), allow you to 
really use the thing.

-Paul


"Frank Swarbrick" <infocat@sprynet.com> wrote in message 
news:%7Fch.6392$tM1.4619@newsread1.news.pas.earthlink.net...
> P. Raulerson wrote:
>> You should look into Smalltalk- right up your alley. Of course, it drives 
…[20 more quoted lines elided]…
> Frank
```

###### ↳ ↳ ↳ Re: invoking a method

- **From:** "Pete Dashwood" <dashwood@removethis.enternet.co.nz>
- **Date:** 2006-12-04T12:37:22+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<4th5diF1411i4U1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <Afoch.628$i37.580@newsfe13.lga> <%7Fch.6392$tM1.4619@newsread1.news.pas.earthlink.net>`

```

"Frank Swarbrick" <infocat@sprynet.com> wrote in message 
news:%7Fch.6392$tM1.4619@newsread1.news.pas.earthlink.net...
> P. Raulerson wrote:
>> You should look into Smalltalk- right up your alley. Of course, it drives 
…[12 more quoted lines elided]…
>

You are in good company, Frank. Most of the procedural programmers I've 
tried to retrain have exactly this problem. On the other hand, people who 
know nothing about programming take to OO like ducks to water :-)

It comes down to being able to set aside what you already "know", and look 
at the concepts as if you were encountering programming fo rthe first time.

(See my discussion elsewhere in CLC archives regarding ITSA and ITSLIKE...)

It took me several months of grappling with OO COBOL when it first came 
out... I gave up and decided to learn Java instead.  Because it was a 
completely new (to me) language I had to look at it as if I knew nothing. 
Within  a week I was comfortable with OO concepts, within a month I was 
writing Java and enjoying it, and then going back for another look at OO 
COBOL, everything just fell into place...

> NE 5.0 is the "latest and greatest".  Just released this year.  You may be 
> thinking of Fujistu, which I believe gives away 3.0 of their product. As 
…[5 more quoted lines elided]…
> Frank

This raises another pet peeve of mine... Why would you support a company 
that simply limits and restricts you, instead of encouraging you?

Limits on source lines? Royalties on developed applications?  Why? It is 
this attitude (and MF are by no means alone in it) that is killing COBOL.

I would MUCH rather have a complete and full evaluation for say, 30 days, 
that lets me get the feel of a product, than some emasculated version that 
simply wastes my time and effort.

There's not much incentive to develop anything if it doesn't belong to you 
after you have done so...Run time fees are simply a joke. Vote with your 
feet and don't support companies that use this model.

Case in point...

I have recently completed setting up my wireless LAN and need to have remote 
access to control machines that are used sometimes on the network (all 
laptops that could be at any location around the house). There is a company 
called LogMeIn that offer such a service. I tried it  for a week and was 
very impressed. I could grab any machine anywhere in the house and control 
all the others from it. Good support, stable platform, and some nice touches 
like being able to share the local clipboard with any other machine on the 
network.

Decided I might buy it, then found it is $12 a month for the service, on 
each machine, ongoing....

Downloaded Remote Administrator instead, got all the same effects except for 
the clipboard (and I wrote a script to workaround that in about 10 minutes), 
and it is a one time registration of $29.99. I registered in a heartbeat.

The point is that a revenue generation model that goes for an ongoing fee 
(unless the fee is tiny enough to be insignificant and brings added benefits 
like ongoing support), will lose every time against one that has a one time 
charge and provides mainly the same service.

Pete.
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 4)_

- **From:** "Frank Swarbrick" <Frank.Swarbrick@efirstbank.com>
- **Date:** 2006-12-04T18:37:04-07:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<4tk0q0F14arsfU1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <Afoch.628$i37.580@newsfe13.lga> <%7Fch.6392$tM1.4619@newsread1.news.pas.earthlink.net> <4th5diF1411i4U1@mid.individual.net>`

```
Pete Dashwood<dashwood@removethis.enternet.co.nz> 12/03/06 4:37 PM >>>
>
>"Frank Swarbrick" <infocat@sprynet.com> wrote in message 
>news:%7Fch.6392$tM1.4619@newsread1.news.pas.earthlink.net...
>> P. Raulerson wrote:
>>> You should look into Smalltalk- right up your alley. Of course, it
drives 
>>> those of us who are indoctrinated with procedural languages right 
>>> crazy...
>>>
>>> I did not know that the MF had a new "University Edition" out, the last

>>> one I knew of was version 3.0. Wayyy behind the times. This one may be a

>>> little old as well, which is why you are picking up errors. The 
>>> University edition is a way that MF can make a bit of cash off older - 
>>> basically non-supported - versions of the product.
>>
>> Hmm, I've looked at Smalltalk and never found it particularly readable.
Or 
>> understandable.  :-)  I am definitely a procedural language programmer. 
I 
>> find OO stuff to be fascinating, but not always 'natural'.
>>
…[3 more quoted lines elided]…
>know nothing about programming take to OO like ducks to water :-)

I've been doing OO on and off since the early 90s, but I've never done what
I would consider a 'major project' in anything but COBOL (and C, long ago). 
Until I do that I won't consider myself to be OO fluent.  Now just to figure
out a 'major project' to do!  :-)

>It comes down to being able to set aside what you already "know", and look

>at the concepts as if you were encountering programming fo rthe first
time.
>
>(See my discussion elsewhere in CLC archives regarding ITSA and
ITSLIKE...)
>
>It took me several months of grappling with OO COBOL when it first came 
…[6 more quoted lines elided]…
>> NE 5.0 is the "latest and greatest".  Just released this year.  You may
be 
>> thinking of Fujistu, which I believe gives away 3.0 of their product. As

>> far as I know the NE "University Edition" is pretty much full
functioning, 
>> other than only allowing up to 2200 lines of source code per executable.

>> See the following link for more information:
>>
http://www.microfocus.com/Resources/Communities/Academic/shop/description.as
p?productid=52 
>>
>> Frank
>
>This raises another pet peeve of mine... Why would you support a company 
>that simply limits and restricts you, instead of encouraging you?

Why?  Because I can't find any alternative.  At least when it comes to OO
support.  There's OpenCobol, but it does not support OO.

>Limits on source lines? Royalties on developed applications?  Why? It is 
>this attitude (and MF are by no means alone in it) that is killing COBOL.

I don't disagree.

>I would MUCH rather have a complete and full evaluation for say, 30 days, 
>that lets me get the feel of a product, than some emasculated version that

>simply wastes my time and effort.

Well there I will have to disagree a bit.  30 days is not enough time; at
least when your doing it on your own time and spending only an hour or two a
day on it, at most.

>There's not much incentive to develop anything if it doesn't belong to you

>after you have done so...Run time fees are simply a joke. Vote with your 
>feet and don't support companies that use this model.

I don't (at this point, at least!) have any intention of selling a product I
might write in MF COBOL, so while I agree its ridiculous, I can't say it
really affects me.

But yes, it would certainly be nice if MF would do something like what
Microsoft did with Visual Studio Express.  I don't expect them to give away
the bank, but it would be nice to have a fully functional compiler for free.
 Perhaps only the command-line version, or something.  Or a stripped down
version of the IDE that only offers the basics, while the 'bells and
whistles' would cost more.  Have to leave them something to sell, after all!
 :-)

Still, if I can get a good grip on the OO COBOL concepts I'd love to add
them to OpenCobol, which is indeed free.  Perhaps a pipe-dream, but we shall
see...

Frank


--- 
Frank Swarbrick
Senior Developer/Analyst - Mainframe Applications
FirstBank Data Corporation - Lakewood, CO  USA
```

#### ↳ Re: invoking a method

- **From:** Donald Tees <donald_tees@donald-tees.ca>
- **Date:** 2006-12-02T19:44:44-05:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<ekt6lr$6ph$1@aioe.org>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net>`

```
Frank Swarbrick wrote:
> Looking at examples supplied with MF Net Express reminded me of 
> something that seems odd to me about all OO languages I've looked in to. 
…[14 more quoted lines elided]…
> 

<snip>

> Comments and constructive criticism welcomed.  Ignoring my nonsense is 
> perfectly fine.  Comments like "don't you have better things to do with 
…[4 more quoted lines elided]…
> Frank

The syntax I prefer is to invoke a procedure by use of the move 
statement.  It seems more "Cobolish" to me.

So instead of:
 >      invoke displayList "add" using by value emp1
I'd more likely write:
        MOVE EMP1 TO ADD-KEY OF DISPLAYLIST.

The cobol data field, described by the picture clause, combined with the 
move method, is probably the most primitive example of OOP. Might as 
well continue the tradition.

Donald
```

#### ↳ Re: invoking a method

- **From:** Paul Robinson <paul@paul-robinson.us>
- **Date:** 2006-12-02T23:31:16-05:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net>`
- **In-Reply-To:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net>`

```
Frank Swarbrick wrote (edited):

> about all OO languages ... pretty much all invoke methods 
> in a somewhat similar format that the object comes first, 
> followed by the name of the method, followed by any parameters.

> COBOL 2002:   invoke object "method" using by value parm1, parm2
> MF COBOL:     invoke object::"method"(parm1, parm2)
…[11 more quoted lines elided]…
> message.  'into' is just a noise word to make it more readable.

Okay, having actually done maintenance on compilers including written 
more than one, I'll state why I don't think your idea will work, at 
least, not in the context of Cobol.

Now, I will admit I have not worked on anyone else's Cobol compiler so I 
don't know how they work, I will offer some thoughts on the subject 
based on how I suspect they work.  I may be wrong on this point but 
considering the way Cobol reference manuals show Cobol syntax, I have a 
reasonable belief that this would be the way it would be done.

Presumably the parser for a Cobol Compiler is going to presume some 
fixed syntax, i.e. a specific DIVISION having certain contexts.  In 
fact, the typical reference manual for a Cobol Compiler will have line 
and box based syntax diagrams showing exactly what particular symbols 
are permissible at a particular point, e.g. in the DATA DIVISION, if 
there is a two-digit number, it is expected to be followed by either a 
data name, blank or FILLER, followed by PIC or PICTURE or some other terms.

Now, in order to allow something like this, you would have to have some 
way to allow a Cobol parser to identify that this is a method (or 
function) call.

If we enter the PROCEDURE DIVISION, typically what you have are a series 
of sentences in which the first word, which is a "verb", dictates what 
the sentence means, i.e. what action is to take place.  That particular 
verb has certain permitted parameters and options.

Actually, you could do something like this if you had a provision that 
referencing an identifier is conditionally a method call.  Of course, 
this also causes a problem if the method name or object name are the 
same as or similar to a Cobol keyword.  But the high probability is that 
it would be an extreme effort to be able to implement such a thing, 
since I suspect that Cobol compilers tend to be keyword driven, thus 
allowing them to implement the "implicit call" function (where reference 
to a procedure or function implicitly calls that function without use 
of, say, a CALL (or PERFORM in Cobol) would require potentially a 
significant rewrite or even a reengineering of the parsing routines of 
the compiler.

Languages that implement "implicit call" can do this because they have 
provision for referencing an identifier as either an assignment (if the 
symbol following the identifier is the assignment symbol), or a 
procedure call if the identifier is a procedure / function, or is 
followed by a parameter list.

C, Pascal and Visual Basic all support "implicit call."  Some languages, 
it would be very difficult to implement.

In Fortran, for example, you have something like

	DO 5 I = 1, 10
	or
	DO 5 I = 1.10
	
The first is a loop construct, the second is an assignment.  Fortran 
does not depend on spaces, does not have reserved words, and has few 
restrictions on how you form an identifier, so until it sees the "." or 
the "," it can't tell that the statement is an assignment or is a loop. 
  In fact, while technically it is legal syntax, most people who write 
Fortran would consider the second example to be incorrect.

So in Fortran, you can't even use spaces or delimiters to determine 
whether some reference is a variable or an imperative statement until 
much further along in the line.  Thus implementing an "implicit call" 
would be very difficult if it was even possible.

Now, Cobol has a number of advantages including imperative command-based 
statements.  This makes writing a compiler easier, at the expense of 
making the language to be compiled more rigid.  Which is exactly the 
state of Cobol programs.

Now, it is conceivable to allow such a construct if some way can be 
found to allow a method reference to be used such that some keyword in 
the stream allows the program to be aware that this is what is being done.

Actually, this might not be a bad idea, and it sounds interesting, 
although I don't know if it can be implemented in a reasonable fashion 
giving the method I suspect most Cobol compilers are implemented.
```

##### ↳ ↳ Re: invoking a method

- **From:** "Pete Dashwood" <dashwood@removethis.enternet.co.nz>
- **Date:** 2006-12-04T01:23:36+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<4tftu9F13rndoU1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net>`

```

"Paul Robinson" <paul@paul-robinson.us> wrote in message 
news:6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net...
> Frank Swarbrick wrote (edited):
>
…[9 more quoted lines elided]…
>>

This is a logical top down progression which may seem strange at first, but 
becomes clear when you realise that everything is relating to an Object.

OBJECTS  have METHODS, PROPERTIES, and EVENTS.

OBJECT METHODS may have parameters. (Personally, I avoid this as much as 
possible and use GET and SET Methods to set  object properties, rather than 
passing parameters at invocation time. The reasons for this are too complex 
to go into here in the time I am prepared to spend on it... One benefit is 
that the interface to method invocation doesn't change; you simply invoke 
the method, having already "passed" the parameters.)

>> I've come to believe it would be more "natural" if the method name was 
>> specified first.

I would find that completely UNnatural... It is indeed a diverse world we 
inhabit :-)

 The method is very often a
> > verb.  So why not something like:
>>   insert employee into list
…[3 more quoted lines elided]…
>> message.  'into' is just a noise word to make it more readable.

No, parameters are NOT objects; they are properties. In your example, "list" 
is the object...

For me that would be

invoke list
     "SET-employee" using ws-employee
end-invoke

(The SET method of the list object is implicit if it is a COM object (and 
those are the ones I mainly deal with...) If it isn't, then you would need 
to write (or generate) your own GET and SET methods) The Fujitsu COM 
interface Class provides these methods and saves you the trouble of having 
to wrap your .dll with the layers and interfaces you would otherwise need. 
The use of SET as shown here, will not work outside the Fujitsu Object COBOL 
environment (You could simulate it very easily though).

It's not a good example, because SET actually sets the property, which is 
the same as "inserting" it... Any INSERT Method, presumably has something to 
be inserted, so there will usually be further information required.

Something that might use an INSERT method could be a database object 
(Collections use ADD)

invoke DBObject
     "SET-DBDataLocation" using ws-structure-ptr
end-invoke
invoke DBObject
     "insert"             *> Note no parameters to INSERT method...
     returning  ws-DBStatus
end-invoke

Here, the location of the structure to be inserted is set before invoking 
the INSERT method. The effect is exactly the same as if it had been passed 
as a parameter, and you might wonder why I chose not to address the Object 
once, and simply pass it parameters.

Here's the rationale, in my own words, as best I can explain it quickly and 
succinctly (there is much more here than meets the eye), so, 
over-simplified...

GENERAL:

1. Object methods allow objects to behave in various ways. Methods are about 
"behaviour".

2. Object properties  are the public data attributes of an object. 
Properties are about influencing and controlling behaviour, and are 
influenced and controlled by behaviours.

3. Because they are PUBLIC, properties can be easily used to affect 
behaviour, BEFORE the behaviour is initiated (SET). They can also be viewed 
AFTER the behaviour has completed, to see what happened (GET).

SPECIFIC (to the example above):

1. The insert may need a different number of parameters if you decide to 
extend it's functionality.

(Suppose we decide to add BLOBs (Binary Large Objects... say, images) to the 
database and need to add a signature to each one immediately before writing 
it, after virus checking it. Now there is a specific behaviour required for 
BLOBs, but it only marginally deviates from the standard INSERT processing. 
A new Method ("BLOBBY") is added to the DBObject Class as a private method 
that will be activated before the INSERT method if a certain property is 
set. BLOBBY will calculate and check the signature and ensure it is inserted 
in the data structure. The INSERT method will not be touched; it will insert 
the structure just as always.

invoke DBObject
     "SET-DBDataLocation" using ws-structure-ptr
end-invoke

if its-a-blob
    invoke DBObject
        "SET-DBBLOBFlag" using by value TRUE
    end-invoke
end-if

invoke DBObject
     "insert"             *> Note no parameters to INSERT method...
     returning  ws-DBStatus
end-invoke

Only new programs that are required to deal with BLOBs need the new code. 
Maybe BLOBs can only be added to one specific database, in one specific 
system... there are any number of possibilities. The point is that any code 
in any system which is using the INSERT method, and does NOT require BLOB 
functionality, continues to function as it always has. It requires neither 
change nor regression testing.)

2. If you use a parameter list, and one or more of the items in the list 
changes, due to extended functionality or overrides in the future, you must 
REGRESSION TEST EVERY single program that uses the INSERT method.

3. Linking specific parameters to specific methods does not encouirage 
method generalisation. Seeing fundamental behaviour as modifiable through 
properties, is a good way to avoid maintaining (changing and modifying what 
methods do) code. If functionality needs extending; extend the behaviours of 
the Class. Don't change existing behaviour that is working and proven. If 
the basic behaviour needs changing, you didn't design it properly, or things 
have changed so dramatically that it is no longer relevant. Either way, it's 
time to design and write it again.

Using the approach above, existing programs do not have their behavioural 
interfaces changed, the INSERT method remains encapsulated, and you only 
need to test the programs that use the new functionality.



>
> Okay, having actually done maintenance on compilers including written more 
> than one, I'll state why I don't think your idea will work, at least, not 
> in the context of Cobol.

Having had similar experience, I'll be interested in your comments :-)
>
> Now, I will admit I have not worked on anyone else's Cobol compiler so I 
…[3 more quoted lines elided]…
> belief that this would be the way it would be done.

Yes, that would be a fair assumption.

However, personally, I wouldn't make it :-)

There are so many good compilers that I believe it is risky to assume how 
they work.

Some people love doing the unexpected... :-)

The best compiler I ever worked on used transition diagramming for the 
parsing and was blazingly fast. Rather than looking at and unstringing 
words, every character of source was processed through predicted states. If 
the "interesting" states didn't eventuate the source was flushed to the next 
white space.

Nodes were built for the procedure division and referenced back to data 
names previously stored with attached attributes in shorthand.

Subsequent passes checked that COBOL rules were enforced.

It was blinding and brilliant... and it was written in 1968, before computer 
science screwed the world up :-)

It had 14 passes in total and I disassembled it so we could patch it to take 
tape input (it was designed to read cards only), and to get it to write its 
output to a fixed disk where we could offload it to tape (rather than 
punching Object decks which tended to be a bit "fragile". It had been 
written by a single person, on contract to a major manufacturer. Said person 
had fallen out with them and there was no support for it from said 
manufacturer... :-)

I spent nearly 6 months anaysing and modifying this code, and I learned 
much. There are products being written today by people with Masters degrees, 
which are not worthy to even act as bootstraps for this compiler... :-)

The discussion below is good and fair, but there are possibilities it 
doesn't consider. Some of the difficulties only arise if you assume certain 
approaches; a different approach means they aren't problematic.
>
> Presumably the parser for a Cobol Compiler is going to presume some fixed 
…[66 more quoted lines elided]…
>
We'll never know...:-)

Pete.
```

###### ↳ ↳ ↳ Re: invoking a method

- **From:** "P. Raulerson" <paul.rl@raulersons.com>
- **Date:** 2006-12-03T13:38:29-06:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<ZIFch.11140$YI1.4490@newsfe15.lga>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net>`

```
Just a comment:
"Pete Dashwood" <dashwood@removethis.enternet.co.nz> wrote in message 
news:4tftu9F13rndoU1@mid.individual.net...

"getters and setters" typically refer to METHODS used to set a property, as 
you so aptly described. However, there is nothing that says the properties 
*must* be PUBLIC. Indeed, Getters and Setters are used to encapsulate the 
format of private properties inside an object, and in specific, their 
implementation.

Second, unlike is some of the Microsoft world, it is pretty common to pass 
objects to ADD or INSERT methods and have the method determine what kind of 
object is dealing with. "+" is usually used as an example of this - being 
able to add two integers or concatenate two strings, or convert an integer 
to a string and add it to another string. That kind of stuff. As you pointed 
out, they are also regular parts of collections, and hide the underlying 
implementation.

-Paul


<snip lots of good stuff here to get to the part I am commenting on ...>
> (The SET method of the list object is implicit if it is a COM object (and 
> those are the ones I mainly deal with...) If it isn't, then you would need 
…[12 more quoted lines elided]…
>
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 4)_

- **From:** "Pete Dashwood" <dashwood@removethis.enternet.co.nz>
- **Date:** 2006-12-04T12:00:23+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<4th387F145bllU1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <ZIFch.11140$YI1.4490@newsfe15.lga>`

```
Thanks for the comments, Paul.

I should have made it clear that I was describing what I consider to be 
personal "best practice" and not necessarily what can actually be done.

I know GET and SET methods can be used to manipulate private properties, but 
I don't do that. If I want things manipulated I make them public... :-)

Pete.

TOP POST - nothing new below


"P. Raulerson" <paul.rl@raulersons.com> wrote in message 
news:ZIFch.11140$YI1.4490@newsfe15.lga...
> Just a comment:
> "Pete Dashwood" <dashwood@removethis.enternet.co.nz> wrote in message 
…[37 more quoted lines elided]…
>
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 5)_

- **From:** "P. Raulerson" <paul.rl@raulersons.com>
- **Date:** 2006-12-03T21:27:50-06:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<ZAMch.2560$Rj.108@newsfe19.lga>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <ZIFch.11140$YI1.4490@newsfe15.lga> <4th387F145bllU1@mid.individual.net>`

```
Now that one really does have me curious Pete. Care to go on with the 
subject a bit more length?

I tend to make almost every property private just so that I can hide the 
implementation of it. I usually generate a class that I use to pass the data 
as well. Works great in my world. In procedural parlance, I can publish an 
API and change the implementation behind it as many times as I wish without 
disturbing other people using it. Indeed, it may be that I am misusing 
properties in some way, but that has always seemed the most logical and 
correct way to implement them - since way back in the early 1980's at least.

-Paul

"Pete Dashwood" <dashwood@removethis.enternet.co.nz> wrote in message 
news:4th387F145bllU1@mid.individual.net...
> Thanks for the comments, Paul.
>
…[56 more quoted lines elided]…
>
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 6)_

- **From:** "Pete Dashwood" <dashwood@removethis.enternet.co.nz>
- **Date:** 2006-12-04T19:18:28+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<4thstlFu9ev2U1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <ZIFch.11140$YI1.4490@newsfe15.lga> <4th387F145bllU1@mid.individual.net> <ZAMch.2560$Rj.108@newsfe19.lga>`

```

"P. Raulerson" <paul.rl@raulersons.com> wrote in message 
news:ZAMch.2560$Rj.108@newsfe19.lga...
> Now that one really does have me curious Pete. Care to go on with the 
> subject a bit more length?
…[5 more quoted lines elided]…
> wish without disturbing other people using it.

Absolutely, a very good approach.

>Indeed, it may be that I am misusing properties in some way, but that has 
>always seemed the most logical and correct way to implement them - since 
>way back in the early 1980's at least.
>

No, I don't see that as misuse.

Going back to what I said earlier...

I see properties as allowing external change of behaviours, and also 
providing a  low volume data interface to/from my objects.

(Most of my objects have a property called interfaceBlock which is a COM 
String of 8192 bytes, and can be structured any way I like). This block is 
usually in the Class factory so it can be easily shared by all methods - 
different methods may interpret its contents differently.)

I like your idea of using a data interface class and that is probably a more 
elegant way of doing this. I'll think about that some more :-)

Coming back to use of private and public properties... It may have been 
confusing when I said that properties are public; obviously if you are 
getting and setting them, it doesn't matter whether they are public or 
private. It just so happens that in my own perceptions I consider anything 
that is available externally from the object to be public and so I have 
historically declared them that way... it's just a habit really.

We all tend to settle on what works best for us.

I'm not suggesting that my approach is the best or only one; it works for me 
and I don't maintain code (once is is debugged and doing what it is meant 
to), as I used to do when I developed with procedural code only.

After five years exclusively using OO (and a few years playing with it 
before that)
there is no way I would go back to non-OO development.

Pete
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 7)_

- **From:** "P. Raulerson" <paul.rl@raulersons.com>
- **Date:** 2006-12-04T18:25:02-06:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<B%2dh.71733$2t1.21101@newsfe18.lga>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <ZIFch.11140$YI1.4490@newsfe15.lga> <4th387F145bllU1@mid.individual.net> <ZAMch.2560$Rj.108@newsfe19.lga> <4thstlFu9ev2U1@mid.individual.net>`

```
Ah, I see where you are coming from now. Makes a lot of sense I think. I get 
into so many different kinds of projects, that I tend to use different 
methods for different tasks. OO doesn't help me much with say, UNIX Kernel 
programming. On the other hand, Database processing sometimes makes sense as 
an OO Programming Project, and sometimes only as a Object Oriented Design 
(with the implementation in a procedural language.)  Visual Processing, like 
Windows stuff - almost always OO seems to be the best choice.

I see very little value in using an OO design or Programming Language (or 
even many features) in a tree killer program. Where brute force is called 
for, very careful design at a low level of detail is called for. In a user 
interface, it is just the opposite - high level behaviors have to be 
defined.

In short, COBOL, RPG, Assembler, and such have their place, right along with 
VB, Java, Smalltalk, and that crowd. I don't want to loose access to *any* 
of them. ;)

-Paul

"Pete Dashwood" <dashwood@removethis.enternet.co.nz> wrote in message 
news:4thstlFu9ev2U1@mid.individual.net...
>
> "P. Raulerson" <paul.rl@raulersons.com> wrote in message 
…[50 more quoted lines elided]…
>
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 5)_

- **From:** "Charles Hottel" <chottel@earthlink.net>
- **Date:** 2006-12-05T00:19:10+00:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<2W2dh.6874$ql2.4513@newsread3.news.pas.earthlink.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <ZIFch.11140$YI1.4490@newsfe15.lga> <4th387F145bllU1@mid.individual.net>`

```

"Pete Dashwood" <dashwood@removethis.enternet.co.nz> wrote in message 
news:4th387F145bllU1@mid.individual.net...
> Thanks for the comments, Paul.
>
…[10 more quoted lines elided]…
>
Pete, if you have the time check out this link on "Why Getter and Setter 
Methods Are Evil" as I would like to hear your opinion on this:

http://www.javaworld.com/javaworld/jw-09-2003/jw-0905-toolbox.html

I am assuming that you would disagree but I would like to hear your 
reasoning. Thanks.
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 6)_

- **From:** "Pete Dashwood" <dashwood@enternet.co.nz>
- **Date:** 2006-12-06T00:26:41+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<4tl3bnF1448j5U1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <ZIFch.11140$YI1.4490@newsfe15.lga> <4th387F145bllU1@mid.individual.net> <2W2dh.6874$ql2.4513@newsread3.news.pas.earthlink.net>`

```

"Charles Hottel" <chottel@earthlink.net> wrote in message 
news:2W2dh.6874$ql2.4513@newsread3.news.pas.earthlink.net...
>
> "Pete Dashwood" <dashwood@removethis.enternet.co.nz> wrote in message 
…[21 more quoted lines elided]…
>
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 6)_

- **From:** "Pete Dashwood" <dashwood@enternet.co.nz>
- **Date:** 2006-12-06T00:43:16+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<4tl4arF14k40qU1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <ZIFch.11140$YI1.4490@newsfe15.lga> <4th387F145bllU1@mid.individual.net> <2W2dh.6874$ql2.4513@newsread3.news.pas.earthlink.net>`

```
Hi Charles,

I haven't time to go into all the pros and cons of this but I should clear 
up some confusion that could arise from my post.

I DO make things Public as a general rule, where they will affect the 
behaviour of a method. However, I expect them to be accessed via GET/SET 
(accessor) methods. After reflecting on this discussion I have decided to 
change this policy. (Not the accessor methiods; making them private...). I 
mentioned elsewhere it is really just a historical habit. I think Paul's 
point about hiding things (although this goes against my nature :-)) is a 
valid one.  Richard sees accessor methods as clogging up the object, and 
many people would agree.

The fact is that it isn't black and white.

There are some circumstances where you really should use accessor methods 
and there are others where it probably is OK to let object users manipulate 
properties directly. Fundamentally, it depends on the application.

I followed the link you posted and read the article with as open a mind as I 
could manage.:-)

I think some of the points are valid but there are other factors which were 
not considered.

For a much more balanced discussion on this see... 
http://discuss.fogcreek.com/joelonsoftware4/default.asp?cmd=show&ixPost=121194&ixReplies=21

I believe that all of the reasons why I would use accessors are covered in 
this discussion (and a lot more besides), along with reasons why you 
wouldn't use them.

Most of what I am developing are components, and COM components at that. (I 
am gradually going to transit to dotNET and am still studying dotNET Classes 
and learning C#). Therefore, I am writing Library applications and these are 
often best served with accessor methods (see discussion above)

Hope this helps,

Pete.


"Charles Hottel" <chottel@earthlink.net> wrote in message 
news:2W2dh.6874$ql2.4513@newsread3.news.pas.earthlink.net...
>
> "Pete Dashwood" <dashwood@removethis.enternet.co.nz> wrote in message 
…[21 more quoted lines elided]…
>
```

###### ↳ ↳ ↳ Re: invoking a method

- **From:** "Richard" <riplin@Azonic.co.nz>
- **Date:** 2006-12-03T14:18:19-08:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<1165184299.836974.231480@l12g2000cwl.googlegroups.com>`
- **In-Reply-To:** `<4tftu9F13rndoU1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net>`

```

Pete Dashwood wrote:

> SPECIFIC (to the example above):
>
> 1. The insert may need a different number of parameters if you decide to
> extend it's functionality.

That's what overloading and mangling is for.

     insert(something, something, blobby)

is not the same method as:

     insert(something, something)

but probably invokes the latter as well as dealing with the blobby.

Another way of doing this is with defaults on parameters:

    insert(something, somthing, blobby=None)

if the method is invoked with only two parameters then the variable
blobby has the value None (or some other as required) so inside the
method one can:

    if ( bloobly ):
         do something with blobby

> (Suppose we decide to add BLOBs (Binary Large Objects... say, images) to the
> database and need to add a signature to each one immediately before writing
…[28 more quoted lines elided]…
> change nor regression testing.)

Yeah, right. What happens if one part of the the program requires the
new funtionality and it is not required in the rest.  Once the
SET-DBBLOBFlag is set then it may stay set and an insert from somewhere
else duplicates the blob.

> 2. If you use a parameter list, and one or more of the items in the list
> changes, due to extended functionality or overrides in the future, you must
> REGRESSION TEST EVERY single program that uses the INSERT method.

No more so then your way.


> 3. Linking specific parameters to specific methods does not encouirage
> method generalisation. Seeing fundamental behaviour as modifiable through
> properties, is a good way to avoid maintaining (changing and modifying what
> methods do) code.

So does overloading.

So does having a method "insert-with-blob", or even class-with-blob,
insert method.


> The best compiler I ever worked on used transition diagramming for the
> parsing and was blazingly fast. 

XE13 ?
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 4)_

- **From:** "Pete Dashwood" <dashwood@removethis.enternet.co.nz>
- **Date:** 2006-12-04T12:11:23+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<4th3ssF143a7eU1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <1165184299.836974.231480@l12g2000cwl.googlegroups.com>`

```

"Richard" <riplin@Azonic.co.nz> wrote in message 
news:1165184299.836974.231480@l12g2000cwl.googlegroups.com...
>
> Pete Dashwood wrote:
…[14 more quoted lines elided]…
> but probably invokes the latter as well as dealing with the blobby.

Precisely my point. Maybe you missed it :-)?
>
> Another way of doing this is with defaults on parameters:

Sure. There are many ways of doing things; I out lined what I personally 
prefer.

>
>    insert(something, somthing, blobby=None)
…[52 more quoted lines elided]…
> else duplicates the blob.

Then it is your responsibility as a programmer to ensure that you unset it 
after use, IF you plan on using this same instance of the DBObject in places 
where you don't want it set...

It comes back to modifying behaviours (or not...)

>
>> 2. If you use a parameter list, and one or more of the items in the list
…[4 more quoted lines elided]…
> No more so then your way.

"My way" does not require this and I don't do it.
>
>
…[6 more quoted lines elided]…
> So does overloading.

Yes, I agree.
>
> So does having a method "insert-with-blob", or even class-with-blob,
> insert method.

Sure. It was a contrived example and not a good one. I was trying to 
summarize some concepts and attempt to explain why I do what I do. I never 
suggested it was the "only way".

>
>
…[4 more quoted lines elided]…
>
No, although I agree XE13 was fantastic in its time.

It was a B500 compiler for Burroughs (remember them :-)...? part of the 
BUNCH (Burroughs, Univac, NCR, CDC, and Honeywell) as IBM dismissively 
called anyone who was not IBM. Note that they didn't even consider ICL 
:-)... guess it didn't fit their acronym...

Pete.
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 5)_

- **From:** "Richard" <riplin@Azonic.co.nz>
- **Date:** 2006-12-03T15:56:01-08:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<1165190161.501114.48460@n67g2000cwd.googlegroups.com>`
- **In-Reply-To:** `<4th3ssF143a7eU1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <1165184299.836974.231480@l12g2000cwl.googlegroups.com> <4th3ssF143a7eU1@mid.individual.net>`

```

Pete Dashwood wrote:

> Then it is your responsibility as a programmer to ensure that you unset it
> after use, IF you plan on using this same instance of the DBObject in places
> where you don't want it set...
>
> It comes back to modifying behaviours (or not...)

It is only necessary to unset after use if there are places where it is
not set before use because it is not required, or you missed one.

It's much easier with an overloaded or defaulted parameter or even a
derived class because it can then be done in just one place and apply
everywhere.  No need to worry about whether an unset is needed.

> >> 2. If you use a parameter list, and one or more of the items in the list
> >> changes, due to extended functionality or overrides in the future, you
…[5 more quoted lines elided]…
> "My way" does not require this and I don't do it.

Using an overload, or a derived class, or a defaulted parameter also
does not need a regression test for every single program, or at least
no more so than your way.

> Sure. It was a contrived example and not a good one. I was trying to
> summarize some concepts and attempt to explain why I do what I do. I never
> suggested it was the "only way".

How do you deal with multithreading ?  It would seem to me that you are
creating a critical section the runs from the set through the invoke to
the unset.  That is not a good thing.  Parameter passing keeps the
critical section to just the invoke, which can be made thread safe
quite easily.

> It was a B500 compiler for Burroughs (remember them :-)...?

Yes, AEPB had one that I helped replace with a 1903S or something.
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 6)_

- **From:** "Pete Dashwood" <dashwood@removethis.enternet.co.nz>
- **Date:** 2006-12-04T19:29:39+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<4thtilF13ojovU1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <1165184299.836974.231480@l12g2000cwl.googlegroups.com> <4th3ssF143a7eU1@mid.individual.net> <1165190161.501114.48460@n67g2000cwd.googlegroups.com>`

```

"Richard" <riplin@Azonic.co.nz> wrote in message 
news:1165190161.501114.48460@n67g2000cwd.googlegroups.com...
>
> Pete Dashwood wrote:
…[10 more quoted lines elided]…
> not set before use because it is not required, or you missed one.

Isn't that what I said? If you want the behaviour of an object dynamically 
modifiable at run time then it is your responsibility to ensure it is 
modified or not. There certainly are other ways this can be achieved and I 
take no issue with the ones you have suggested. I just don't personally 
prefer them because of other considerations that are far too numerous to go 
into here.
>
> It's much easier with an overloaded or defaulted parameter or even a
> derived class because it can then be done in just one place and apply
> everywhere.  No need to worry about whether an unset is needed.
>
Well, that's your opinion.

And there are circumstances where I would completely agree with you. But not 
always, and that is why I am trying to deal with this at a conceptual, 
rather than a specific level

I see other spin offs from those solutions which make them unattractive to 
me, but I don't intend to argue it because I don't think what you are saying 
is wrong :-)


>> >> 2. If you use a parameter list, and one or more of the items in the 
>> >> list
…[10 more quoted lines elided]…
> no more so than your way.

I don't recall arguing that... I don't personally like overloading because 
it requires more complexity in the overloaded object, but that's just a 
personal preference and I don't think anything you have suggested is "wrong"
>
>> Sure. It was a contrived example and not a good one. I was trying to
…[13 more quoted lines elided]…
>

Pete.
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 7)_

- **From:** "Richard" <riplin@Azonic.co.nz>
- **Date:** 2006-12-04T10:06:18-08:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<1165255578.298333.270600@n67g2000cwd.googlegroups.com>`
- **In-Reply-To:** `<4thtilF13ojovU1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <1165184299.836974.231480@l12g2000cwl.googlegroups.com> <4th3ssF143a7eU1@mid.individual.net> <1165190161.501114.48460@n67g2000cwd.googlegroups.com> <4thtilF13ojovU1@mid.individual.net>`

```

Pete Dashwood wrote:

> Isn't that what I said?

No.

> If you want the behaviour of an object dynamically
> modifiable at run time then it is your responsibility to ensure it is
> modified or not.

That's why I prefer mechanisms where the program itself can take
responsibility for doing so. I'm too lazy to want to do it myself, and
I don't trust others to do so.

> There certainly are other ways this can be achieved and I
> take no issue with the ones you have suggested. I just don't personally
> prefer them because of other considerations that are far too numerous to go
> into here.

Well, just one will do. I can't think of _anything_ that makes setters
and unsetters better.

> I don't recall arguing that... I don't personally like overloading because
> it requires more complexity in the overloaded object, but that's just a
> personal preference and I don't think anything you have suggested is "wrong"

But in your example you still have to add code to the method to test
whether the blob was set or not and deal with it.  In what way is an
overloaded method (not object), or a 'with_blob' method, or default
parameter, adding more complexity than that ?

I noticed you ignored the issue of threading.
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 8)_

- **From:** "Pete Dashwood" <dashwood@removethis.enternet.co.nz>
- **Date:** 2006-12-05T11:40:01+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<4tjme1F143h1nU1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <1165184299.836974.231480@l12g2000cwl.googlegroups.com> <4th3ssF143a7eU1@mid.individual.net> <1165190161.501114.48460@n67g2000cwd.googlegroups.com> <4thtilF13ojovU1@mid.individual.net> <1165255578.298333.270600@n67g2000cwd.googlegroups.com>`

```

"Richard" <riplin@Azonic.co.nz> wrote in message 
news:1165255578.298333.270600@n67g2000cwd.googlegroups.com...
>
> Pete Dashwood wrote:
…[12 more quoted lines elided]…
>
Fine.

>> There certainly are other ways this can be achieved and I
>> take no issue with the ones you have suggested. I just don't personally
…[5 more quoted lines elided]…
> and unsetters better.

Derived classes are fine and I do use them sometimes, overloading is NOT 
fine because it requires parameters to be passed and I don't want to do 
that, the considerations behind this are much more than I intend to go into 
here.

I am not arguing that setters and unsetters are BETTER (in fact, I'm not 
arguing anything... sorry :-)), just that they are better for me, because I 
don't want parameters passed to methods.

I prefer to have the paramters pre-set as properties before invoking the 
method. I have no intention of discussing this further, other than to say it 
was many months of experiment which led me to this approach.
>
>> I don't recall arguing that... I don't personally like overloading 
…[8 more quoted lines elided]…
> parameter, adding more complexity than that ?

If overloading is used, parameters must be passed to the method. They will 
be different parameters to the same method (or another copy of it within the 
Class).  If the same method is used it will have an overhead to decide which 
set of parameters it was passed and set properties appropiately, if another 
method with the same name is used, this is just a needless duplication of 
Class code, as far as I'm concerned. There is a requirement for the method 
to set these parameters into properties anyway... I like to do this as a 
separate exercise, which is not the responsibility of the method, BUT that 
isn't carved in stone, there are many other factors that  may cause me not 
to do this, and I don't ALWAYS do it. It is a preference.

The principle is that each method is responsible for what it does, with the 
parameters that modify its behaviour pre-set for it. Yes, the code is the 
same either way, but there are subtle benefits in having a layer of 
separation. (In fact, I like Paul's idea of a separate interface Class and 
am thinking more on this...)
>
> I noticed you ignored the issue of threading.
>
 Mainly because it isn't relevant for the work I am doing.

When I actually NEED to have multi-threading I use Java and implement the 
threading Classes.  Most of what I'm doing is component based; I can use a 
Java threaded framework and call components within that, on the extremely 
rare occasions where I would need to. There is seldom need to have innate 
multi-threading in an environment where each "thread" can have its own 
instance of the object anyway...

COM components are always apartment threaded .

There are so many possible factors and considerations and specific instances 
and exceptions, that I have not attempted to make blanket statements here. 
What I have outlined are some principles which I have found to be useful, 
and some preferences which are personal.

Pete.
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 9)_

- **From:** "Richard" <riplin@Azonic.co.nz>
- **Date:** 2006-12-04T17:07:57-08:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<1165280877.871260.111520@73g2000cwn.googlegroups.com>`
- **In-Reply-To:** `<4tjme1F143h1nU1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <1165184299.836974.231480@l12g2000cwl.googlegroups.com> <4th3ssF143a7eU1@mid.individual.net> <1165190161.501114.48460@n67g2000cwd.googlegroups.com> <4thtilF13ojovU1@mid.individual.net> <1165255578.298333.270600@n67g2000cwd.googlegroups.com> <4tjme1F143h1nU1@mid.individual.net>`

```

Pete Dashwood wrote:

> > But in your example you still have to add code to the method to test
> > whether the blob was set or not and deal with it.  In what way is an
…[5 more quoted lines elided]…
> Class).

> If the same method is used it will have an overhead to decide which
> set of parameters it was passed and set properties appropiately,

Well, no, that is what name mangling in C++ is for.  The decision is
made at compile time.

> if another
> method with the same name is used, this is just a needless duplication of
> Class code, as far as I'm concerned.

Well, no.  It just invokes the other method, no duplication at all:

     method A_with_blob(p1, p2, blob):     not overloaded
 or method A(p1,p2, blob):                        overloaded
 or method A(p1, p2, blob=NULL):           if default parameter can be
use
            ....
            if ( blob != NULL )
                  ....
     method A(p1,p2):                                not required if
default parameters can be used.
            A_with_blob(p1,p2, NULL):


> There is a requirement for the method
> to set these parameters into properties anyway...

Well, no. Parameters don't need to be set into properties.

> I like to do this as a
> separate exercise, which is not the responsibility of the method,

Which imposes overheads and requires getter/setter methods to clog up
the class.
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 10)_

- **From:** "Pete Dashwood" <dashwood@enternet.co.nz>
- **Date:** 2006-12-06T00:07:43+13:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<4tl286F149qf0U1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <1165184299.836974.231480@l12g2000cwl.googlegroups.com> <4th3ssF143a7eU1@mid.individual.net> <1165190161.501114.48460@n67g2000cwd.googlegroups.com> <4thtilF13ojovU1@mid.individual.net> <1165255578.298333.270600@n67g2000cwd.googlegroups.com> <4tjme1F143h1nU1@mid.individual.net> <1165280877.871260.111520@73g2000cwn.googlegroups.com>`

```

"Richard" <riplin@Azonic.co.nz> wrote in message 
news:1165280877.871260.111520@73g2000cwn.googlegroups.com...
>
> Pete Dashwood wrote:
…[17 more quoted lines elided]…
>

I don't write C++, have no intention of doing so and have no idea why you 
would want or need to mangle names.

In the languages I DO use (including OO COBOL) what I stated stands.

Furthermore I ONLY use late binding and I don't want decisions made at 
compile time (irrespective of whether names are 'mangled' or not...)



>> if another
>> method with the same name is used, this is just a needless duplication of
>> Class code, as far as I'm concerned.
>
> Well, no.  It just invokes the other method, no duplication at all:

Not in the environments I use.

>
>     method A_with_blob(p1, p2, blob):     not overloaded
…[3 more quoted lines elided]…
>

All of the above use parameters. The whole point of my post is to avoid 
doing that. The reasons WHY I would want to avoid doing that are beyond the 
scope of this discussion.    (I did give some clues but I'm really not 
prepared to explain or debate it because I really don't care what people do. 
My whole point is that I have found this approach useful; if people 
disagree, that's fine, don't use it. I'm not on commission here... :-). If 
someone read it and found it made them think and in the course of that they 
got something they could use, my time was not wasted. Some people here are 
just getting in to OO. I think that's commendable, although I'd rather they 
had done it 10 years ago :-). Showing one carefully qualified, possible 
approach, and trying to explain the underlying concepts to it, seemed to me 
a good idea. (In retrospect, perhaps it wasn't and I'll think twice about 
future posts here.)

            if ( blob != NULL )
>                  ....
>     method A(p1,p2):                                not required if
…[7 more quoted lines elided]…
> Well, no. Parameters don't need to be set into properties.

So exactly how does the method store and reference them? (I am using 
'properties' loosely here to include local variables or any data storage, in 
line with the concept I previously outlined ('Methods' are behaviours, 
'Properties' are data.) A pointer to a parameter or a parameter which is, in 
fact a pointer, is still 'data' that is used by the method.)


>
>> I like to do this as a
…[4 more quoted lines elided]…
>
With COM these 'overheads' are in the Class wrapping and cost me nothing. My 
Classes (the ones which are COM ones, and that is most of them) do NOT 
contain GET and SET methods to 'clog them up'. These methods are provided 
automatically by the interface. (I did say that... but your own 
considerations as to how things should work may have blinded you to it)

Richard, I think we will have to disagree here. I'm not using C++ and my 
reasons for not wanting parameters to methods are more than I can even begin 
to spell out here. That's just the way I do it. As it works very well for me 
I see no reason to change it.

I respect very much your opinions and ability and I don't see disagreement 
as being personal, but I' m not looking for argument on OO (tired of it 
years ago in this forum) so I can't see any point in pursuing this further. 
You've said nothng positive about what I posted, despite the fact that I am 
not trying to persuade anyone, not suggesting my solution as 'the only way', 
and freely admitted that the circumstances surrounding a given 
implementation are complex and may require deviation from a specific. (I 
cannot in all honesty say that I have NEVER used overloading, or derived 
classes, but I pretty much discarded these approaches some time ago, for 
reasons of my own which are part of a much bigger framework.  I believe in 
separation layers. It has worked for me many times over the years. Like I 
have said all along, it is a personal preference.  But it is not black and 
white.)

I can live with your disapproval of what I do :-)

I still write SECTIONs in COBOL... :-) (That doesn't mean I don't appreciate 
your arguments for not doing so...)

Pete.
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 11)_

- **From:** "Richard" <riplin@Azonic.co.nz>
- **Date:** 2006-12-05T10:15:25-08:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<1165342525.119513.124490@j72g2000cwa.googlegroups.com>`
- **In-Reply-To:** `<4tl286F149qf0U1@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <1165184299.836974.231480@l12g2000cwl.googlegroups.com> <4th3ssF143a7eU1@mid.individual.net> <1165190161.501114.48460@n67g2000cwd.googlegroups.com> <4thtilF13ojovU1@mid.individual.net> <1165255578.298333.270600@n67g2000cwd.googlegroups.com> <4tjme1F143h1nU1@mid.individual.net> <1165280877.871260.111520@73g2000cwn.googlegroups.com> <4tl286F149qf0U1@mid.individual.net>`

```

Pete Dashwood wrote:

> >> If the same method is used it will have an overhead to decide which
> >> set of parameters it was passed and set properties appropiately,

> ..  and have no idea why you would want or need to mangle names.

To show that your claims about 'overhead' is a strawman in many cases.

> >> if another
> >> method with the same name is used, this is just a needless duplication of
> >> Class code, as far as I'm concerned.

> >     method A_with_blob(p1, p2, blob):     not overloaded
> > or method A(p1,p2, blob):                        overloaded
…[4 more quoted lines elided]…
> doing that.

And the point of my post was to show that using parameters does not
result in your claim of duplication of code.

> If
> someone read it and found it made them think and in the course of that they
> got something they could use, my time was not wasted.

Well, exactly. And if I illustrate that it does not necessarily aviod
overhead, or duplication, or other claims you have made, and does
introduce technical problems, such as critical sections, then I haven't
wasted my time either.

> > Well, no. Parameters don't need to be set into properties.
>
…[4 more quoted lines elided]…
> fact a pointer, is still 'data' that is used by the method.)

It may well be that a parameter is being passed in order to be stored
inside the object in a property, or it may be that the parameter can
simply be referenced by the method without being stored somewhere else
first.  You appeared to imply that it always required to be stored
before it could be used.

> With COM these 'overheads' are in the Class wrapping and cost me nothing. My
> Classes (the ones which are COM ones, and that is most of them) do NOT
> contain GET and SET methods to 'clog them up'. These methods are provided
> automatically by the interface. (I did say that... but your own
> considerations as to how things should work may have blinded you to it)

Well, yes, they do contain 'getters' and 'setters' even if they are
automatically created by the compiler, or perhaps they are created as
required on the fly at run-time.  Either way they are as much, or more,
overhead as overloading or name-mangled methods.

> my reasons for not wanting parameters to methods are more than I can even begin
> to spell out here.

I suspect that you are very wise to avoid even starting with those
reasons.

> I can live with your disapproval of what I do :-)

Just as well.
```

###### ↳ ↳ ↳ Binding alternatives was Re: invoking a method

_(reply depth: 11)_

- **From:** Clark F Morris <cfmpublic@ns.sympatico.ca>
- **Date:** 2006-12-16T20:11:59+00:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<76f8o2t7efb1jsc06rsc8blclde7v1nt8f@4ax.com>`
- **References:** `<6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <4tftu9F13rndoU1@mid.individual.net> <1165184299.836974.231480@l12g2000cwl.googlegroups.com> <4th3ssF143a7eU1@mid.individual.net> <1165190161.501114.48460@n67g2000cwd.googlegroups.com> <4thtilF13ojovU1@mid.individual.net> <1165255578.298333.270600@n67g2000cwd.googlegroups.com> <4tjme1F143h1nU1@mid.individual.net> <1165280877.871260.111520@73g2000cwn.googlegroups.com> <4tl286F149qf0U1@mid.individual.net>`

```
On Wed, 6 Dec 2006 00:07:43 +1300, "Pete Dashwood"
<dashwood@enternet.co.nz> wrote:

>
>"Richard" <riplin@Azonic.co.nz> wrote in message 
…[29 more quoted lines elided]…
>
The issue of bind time is interesting.  Binding at compile time has
the advantage of elimination of intermodule overhead which can be
significant.  Interestingly, IBM z series COBOL now binds all system
subroutines at run time (those called implicitly by verbs such as READ
or SEARCH ALL as opposed to those by CALL statement) unless the called
module is included in the compile code as a nested program.  The z/OS
environment has a higher overhead for execution time binding than many
others. The advantage of compile time binding is that code is frozen
at that point which is also the disadvantage.   Binding of separately
compiled modules also eliminates both first time overhead and
subsequent overhead to a lesser extent but does not have the advantage
of the compiler being able to optimize code and discard unreferenced
code.  The binding mechanism presumably can be smart enough to match
caller/invoker and called/invoked expectations on entities being
exchanged.  Binding at execution time means that not only is a
separate invocation made of the loading mechanism but that all
parameter passing validity checking (if any) is done at execution
time.  Even if no validity checking for matching parameter
descriptions is done there is a measurable overhead for both first
call and subsequent calls on an IBM z series system.  This overhead is
not great enough for most batch work to cause an installation to
change the batch compile option from DYNAM (bind all called modules at
execution time) to RES (bind all called modules at bind or link-edit
time).  I would assume that the name mangling done by C/C++ allows a
simple and direct way for the caller to assure that the parameter
passing is correct and that the descriptions match.  

In cases where performance matters, compile time binding is best.  It
also allows for the most validity checking.  Depending on the in-line
code necessary for run-time calls, it actually may save executable
load module size.  This is especially true where multiple modules can
be included through a standard copy mechanism with all unused code
being eliminated from compilation.  Depending on how a shop wants to
handle change, compile time binding offers the assurance that
subroutine/invoked module execution will change only after a
re-compile.  Run time binding allows a caller/invoker to pick up
changed behaviour, current fixes etc. without a re-compile.  

My personal view is that far more thought has to be given to what the
effects of changes are than shops do.  They can be paranoid about
source code and load module changes yet let script changes (REXX,
Perl, C-shell, JCL, utility control records) go through with minimal
testing.  Change of data control tables also may be loosely checked.
The idea that control tables specifying things like tax rules, sales
discounts by type of sale, etc. can be made external to programs and
thus open to the appropriate group within an organization for control
is good.  What is not good is that these changes many times are not
adequately tested and can have interesting unintended consequences.
Thus allowing changes in behaviour in dynamically invoked modules is
no more dangerous than allowing changes in behaviour caused
non-program changes.  Both have to be managed.  Both may have to be
better bounded by doing such things as adding an effective date for
the changed behaviour.  
>
>
…[86 more quoted lines elided]…
>
```

##### ↳ ↳ Re: invoking a method

- **From:** Frank Swarbrick <infocat@sprynet.com>
- **Date:** 2006-12-03T19:17:55+00:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<DpFch.6235$ql2.3266@newsread3.news.pas.earthlink.net>`
- **In-Reply-To:** `<6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net>`

```
Paul Robinson wrote:
> Frank Swarbrick wrote (edited):
> 
…[21 more quoted lines elided]…
> least, not in the context of Cobol.

Don't get me wrong.  I was not at all suggesting this usage for COBOL. 
I was suggesting it for if I were developing my own language; a language 
that is not COBOL.

> Now, I will admit I have not worked on anyone else's Cobol compiler so I 
> don't know how they work, I will offer some thoughts on the subject 
…[71 more quoted lines elided]…
> giving the method I suspect most Cobol compilers are implemented.

I'm sure you are correct about why this would not work for Cobol. 
Unlike many other languages, COBOL seems to be based on a "beginning of 
statement" indicator rather than an "end of statement" indicator.  This 
is why there is a need for the CALL and INVOKE statement keywords (not 
to mention COMPUTE), but why COBOL does not need to have parentheses 
around parameters.  It knows when the statement is done because it hits 
a keyword indicating the beginning of a new statement.  This is why one 
can have:

     move object::"method"(parm1, parm2) to receiver

but cannot have just

     object::"method"(parm1, parm2)

(I'm sure you know this.)

I will say it really annoyed me until I realized why it was done this way.

Anyway, if you're interested you can look at the language parser for 
OpenCobol.  Amazingly simple to read.  (Doesn't include any OO stuff, 
though.)

http://open-cobol.cvs.sourceforge.net/open-cobol/open-cobol/cobc/parser.y?view=markup

Frank
```

###### ↳ ↳ ↳ Re: invoking a method

- **From:** Howard Brazee <howard@brazee.net>
- **Date:** 2006-12-04T08:46:50-07:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<mlg8n2l7o3809q23du4kv9ff1ohl6pml0l@4ax.com>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <DpFch.6235$ql2.3266@newsread3.news.pas.earthlink.net>`

```
On Sun, 03 Dec 2006 19:17:55 GMT, Frank Swarbrick
<infocat@sprynet.com> wrote:

>Don't get me wrong.  I was not at all suggesting this usage for COBOL. 
>I was suggesting it for if I were developing my own language; a language 
>that is not COBOL.

Before we could reasonably discuss your new language's needs, we need
to know what was the intent of this new language.   What is need is
this language supposed to fulfill.
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 4)_

- **From:** "Frank Swarbrick" <Frank.Swarbrick@efirstbank.com>
- **Date:** 2006-12-04T18:49:58-07:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<4tk1i5F14arsfU2@mid.individual.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <DpFch.6235$ql2.3266@newsread3.news.pas.earthlink.net> <mlg8n2l7o3809q23du4kv9ff1ohl6pml0l@4ax.com>`

```
Howard Brazee<howard@brazee.net> 12/04/06 8:46 AM >>>
>On Sun, 03 Dec 2006 19:17:55 GMT, Frank Swarbrick
><infocat@sprynet.com> wrote:
…[7 more quoted lines elided]…
>this language supposed to fulfill.

Everything that other languages don't have that I wish they did.  Or "better
ways" of doing those things that they do have.  :-)

Seriously, I'm just playing around.  If I ever get serious about it I will
let you know.  In the end, I just have a lot of "pet peeves", and I get our
my frustrations by making notes on how I wished things worked.  Which is not
at all to say that anything I come up would be feasible, otherwise it seems
to me it would have already been done by someone else!

Frank


--- 
Frank Swarbrick
Senior Developer/Analyst - Mainframe Applications
FirstBank Data Corporation - Lakewood, CO  USA
```

###### ↳ ↳ ↳ Re: invoking a method

_(reply depth: 5)_

- **From:** Howard Brazee <howard@brazee.net>
- **Date:** 2006-12-05T08:37:01-07:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<434bn25up1vuohs764d55ockuv29b9rlgb@4ax.com>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <6bidnRNZEcyKzu_YnZ2dnUVZ_tSdnZ2d@cavtel.net> <DpFch.6235$ql2.3266@newsread3.news.pas.earthlink.net> <mlg8n2l7o3809q23du4kv9ff1ohl6pml0l@4ax.com> <4tk1i5F14arsfU2@mid.individual.net>`

```
On Mon, 4 Dec 2006 18:49:58 -0700, "Frank Swarbrick"
<Frank.Swarbrick@efirstbank.com> wrote:

>>Before we could reasonably discuss your new language's needs, we need
>>to know what was the intent of this new language.   What is need is
…[3 more quoted lines elided]…
>ways" of doing those things that they do have.  :-)

That has always been popular - to find a solution for everything.
Isn't that what Einstein spent most of his adult life working on?

But you might as well design a tool for your garage that you could use
for all your woodworking, plumbing, auto, and electrical needs.

>Seriously, I'm just playing around.  If I ever get serious about it I will
>let you know.  In the end, I just have a lot of "pet peeves", and I get our
>my frustrations by making notes on how I wished things worked.  Which is not
>at all to say that anything I come up would be feasible, otherwise it seems
>to me it would have already been done by someone else!

I enjoyed a language which someone wrote which I bought for my Atari
800.    Action! was an implementation of someone's theoretical optimal
language.    I tended to agree with its design, but the computing
world has gotten bigger.

Maybe a better analogy than the toolbox above is the transportation
industry.   We have designed "universal" truck trailers that fit on
trains and on ships - but even these aren't optimal for all tasks -
they just take less labor to go from one medium to another.    And as
with software, that design didn't anticipate some security issues.
```

#### ↳ Re: invoking a method

- **From:** "James J. Gavan" <jgavandeletethis@shaw.ca>
- **Date:** 2006-12-05T06:07:39+00:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<L08dh.418407$1T2.164834@pd7urf2no>`
- **In-Reply-To:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net>`

```
Frank Swarbrick wrote:
> Looking at examples supplied with MF Net Express reminded me of 
> something that seems odd to me about all OO languages I've looked in to. 
>  They pretty much all invoke methods in a somewhat similar format in 
> that the object comes first, followed by the name of the method, 
> followed by any parameters.

etc... <snip>

Frank I'm not going to repeat the whole of your message but your point 
was you found the generally accepted OO INVOKE syntax 'unacceptable' or 
non-intuitive.
-------------------------------
1. COBOL APPROACH :

Let's go back to square one with COBOL :

- COBOL '74 from my old RM manual :-

CALL (Program : identifier/literal) USING, data-name-1, data-name-2 etc...
		
- COBOL 85

along with this IBM and at least MF introduced (non-standard) :-

The above with the addition of entry points to go directly to a 
section/paragraph :-

CALL (Program: identifier/literal) ENTRY-NAME, USING, data-name-1, 
data-name-2 etc..  RETURNING
		
- sequence above looks familiar when you think about OO ?

CALL(INVOKE) identifier/literal(Object Class), entry-name(Method-Name),
USING param-1, param-2 (can be a mixture of a series of Level 01's which 
are either text or objects) returning ONLY ONE Level 1, (but can be a 
series of 05's which again can be a mixture of text or object references).

If you want it changed to your way, you're gonna have to persuade J4 to 
change the Procedural CALL syntax - good luck !  :-)

Initially I sure had problems coping with OO, (specifically the 
'show-bit', i.e. GUIs and their relationship to the rest of OO), but I 
never had a problem with the COBOL INVOKE syntax - just took it as read. 
Think on it :-

a) INVOKE (program/class - or the Object created from "new") which 
points at the "program" you want

b) Method-Name (equivalent to a Procedural Paragraph using the 
entry-point feature) - frequently used methods in M/F are cached in momory.

c) USING Parameters - optional

d) RETURNING - optional

Pete's already covered above - but another take on why OO syntax is 
'framed' as it is.
-------------------------------------------------------------------------

2. WHERE IT'S AT WITH OO COBOL

No peers to converse with, (and only some five names that I can think of 
in the MF Forum that *really* used OO), I had to sweat it on my own. 
Just like Pete focused on Components I dreamed up a design approach 
which seemed to work for me, and indeed does. I was seriously getting to 
the point that I thought I should write down my thoughts in a book. Step 
One though, to be more universal, I would have needed to be comfortable 
with the Web interfacing. Blow me down. M/F, (correct from their point 
of view), switched horses to dotNet. Sod it! Upgrade to N/E 4 and 5 and 
a new learning curve). At this stage I packed it in. I look at all the 
imponderables associated with Webbing. I'll go googling and trip over 
references which take me down a fresh avenue. Examples :-

- "I use Java 'cos that's where the money is, but using Smalltalk's 
advanced features I can be more productive in Smalltalk than Java...."
No doubt he was thinking of the commercial versions Dolphin or VisualAge 
(???) from Cincomm ???).

- SQUEAK (the non-commercial version of Smalltalk) has a tool SEASIDE 
which is web focused - good looking screens. Then I pick up in a 
separate message, "SEASIDE is SLOWooooooo !".

- "PERL or RUBY lack these features .....".

The list of negatives for ANY language is bewildering. You're right we 
DO need a new language for programming called BABEL or ESPERANTO - it 
truly is crazy how people keep on coming up with new approaches, rather 
than developing a solid base model - and using OO - to tack on new 
features which are missing.

Just read in the paper about Vista - seems Bill's gang had to sit down 
and rewrite much of it from scratch !. What about OO REUSE ???? C# - 
read an article where one of the architects was purloined from Borland 
for $1 million. It's claimed C# embraces the best of Java and C++ - 
remains to be seen. Anyway, how do you know ? A superficial knowledge 
wont help, you need to be not just a 'looker' but a reasonably long-time 
developer in both those languages before you can endorse Microsoft's 
intended objective.

---------------------------------------------------------------------------

3. PASSING ON MY KNOWLEDGE (EXPERIENCE).

Wont be using it any more but you're welcome to use code below 
associated with that MF DisplayList example you quoted.

Gavan's Rule : Write a routine once. Subsequently you write something 
similar a little way on. Hmmmm. Then later on you find yourself writing 
a third version under slightly different circumstances. CRAZY ! Think it 
through; how can you make one routine serve all three. The answer is - 
REUSE, generic support classes - in Pete's case he has gone for 
components. The support class approach doesn't trigger immediately, but 
when you get a comfort zone with OO coding, it sits there in the back of 
your mind as a technique. Doesn't immediately fall into place and much 
of your thoughts can be diagrammed on paper before you achieve what you 
eventually code. (Your design patterns in banking although different 
from mine oil/gas, stil contain common demnominators).

So firstly an explanation of the code below. I *had* an ISAM file, which 
is now an SQL Table and I need a Business Control Class "Edit 
Descriptions". I need a menu to allow the user to select different 
record types from that Table, display them in a Listbox then either 
allow the user to enter a new Prime Key (new record) or select an 
element from the Listbox for changing/deleting. Further, should the user 
enter the PrimeKey for an EXISTING record in the Listbox, that is 
checked against the Listbox Collection and if present, that's what gets 
displayed.

Now the code you quoted is 'Mickey Mouse' - that's not to denigrate M/F 
but they have to keep the examples simple to get the idea across. But as 
you should be able to pick out from the code below, Reusable classes can 
be used with any display list/collection/dictionary. (If you check the 
methods for collections it is possible to display the whole list with 
the method "display" - not particularly helpful - but gives you a DOS 
text screen wrapping around, where you can view the unformatted contents 
of the collection. (Probably may not have found it yet but all the 
methods are in the following file - using your appropriate path :-

"C:\Program Files\MERANT\Net Express\Base\BIN\MFNXCL31.CHM"

Note mine is Version 3.1).


        Master Menu
        Selection
        ------------
              .
        -----------------    ---------
        . Edit               DBIMain
        . Descriptions       (DataBase      ----------------
        .                    Interface) ---> SQL
        -----------------    ----------      Descrips Table
               .                  .          ----------------
               .             ----------
               .------------> DBI
                             Descriptions
                             ------------

So we select Edit Descriptions from the Master Menu which invokes a 
parameter class for dialogs required for this operation and uses a 
Dialog Template class - I'm not showing the GUI bit - just the 
collection aspect.

In brief, EditDescriptions invokes DBI Descriptions which is a 
subordinate class to DBIMain containing all my *own* common methods 
pertaining to collections/dictionaries. Both DBIMain and DBI 
Descriptions can access the Class Descrips Table. Any errors resulting 
from accessing the Table - it in turn invokes SQLERRORS - a list of 
literals for all known error codes.

Without being unnecessarily expansive, see if you can follow the code 
through - it should compile under UE N/E 5.0 and you can use the 
ANIMATOR feature to follow the code as necessary. (Options on animating 
- you can make the Animator display M/F support class source as well - 
but you will soon tire of that when you see it hopping continuously back 
and forth through different support class methods :-) I'm still amazed 
at the alacrity, considering all the code that OO involves).

I debated in my mind restricting the source for Edit Descriptions to 
just that code applicable to Collections - although not needed I've 
given you the full McCoy - pick out the collection stuff and how it 
interacts

CONTROL or BUSINESS CLASS - EDIT DESCRIPTIONS :
        *>-------------------EdtDes.cbl -------------------------------- ---

        Class-id.          DescripsEdit
                           inherits from Base.

        Class-Control.     CtHelp                 is class "cthelp"
                           DescripsEdit           is class "edtdes"
                           DescripsMenu           is class "mnudes"
                           DialogParameters       is class "dlgdes"
                           ItemNames              is class "itemname"
                           TabbedDialog           is class "dlgtabbd"

                           Messagebox             is class "msgbox"
                           .

        *> NOTE : For changes to Collections DESCRIP-record fields are
        *> moved to the shorter format xx-DesListRecord, which is
        *> passed to DesDBI

        *>-------------------------------------------------------------
        FACTORY.
        *>-------------------------------------------------------------
        END FACTORY.
        *>-------------------------------------------------------------
        OBJECT.
        *>---------------------------------------------------------------
        OBJECT-STORAGE SECTION.
        78 ws-MaxLength                     value 100.

        copy "\copylib\dlgLengths.cpy".
        *>--------------------- dlgLengths.cpy--------------------------
        78 TL                           value 40.
        78 ML                           value 30.
        78 RL                           value 70. *> RL = TL + ML
        *>--------------------------------------------------------------

        copy "\copylib\collection.cpy"   replacing ==(tag)== by ==ws==.
        copy "p2cevent.cpy".  *> used for Messages - mb-return-ok
        copy "\copylib\recordflags.cpy".
        *>--------------- recflags.cpy ---------------------------------

        01 RecordChangedFlag         pic x(4) comp-5 value 0.
           88 RecordChanged          value 1.
           88 RecordNotChanged       value 0.

        01 RecordFoundFlag           pic x(4) comp-5 value 0.
           88 RecordFound            value 1.
           88 RecordNotFound         value 0.

        *>--------------------------------------------------------------
        copy "rectype.cpy".
        *>-------------- rectype.cpy ---------------------------------

         78 COATING-rectype                    value 1.
         78 COMMENT-rectype                    value 2.
         78 COUNTRY-rectype                    value 3.
         78 DESIGN-rectype                     value 4.
         78 EQUIPMENT-rectype                  value 5.
         78 INSPECTION-COMPANY-rectype         value 6.
         78 INSULATION-rectype                 value 7.
         78 INSTRUMENT-rectype                 value 8.
         78 ITEM-NAME-rectype                  value 9.
         78 ITEM-SUBNAME-rectype               value 10.
         78 LENGTH-rectype                     value 11.
         78 MANUFACTURER-rectype               value 12.
         78 MATERIAL-TYPE-rectype              value 13.
         78 MATERIAL-SUB-rectype               value 14.
         78 PROVINCE-rectype                   value 15.
         78 SYSTEM-rectype                     value 16.
         78 TECHNICIAN-NAME-rectype            value 17.
         78 VESSEL-NAME-rectype                value 18.
         78 POINT-rectype                      value 19.
         78 MATERIAL-MAIN-rectype              value 20.
         78 MATERIAL-SUB-rectype-01            value 21.
         78 MATERIAL-SUB-rectype-02            value 22.
         78 MATERIAL-SUB-rectype-03            value 23.
         78 MATERIAL-SUB-rectype-04            value 24.
         78 MATERIAL-SUB-rectype-05            value 25.
         78 MATERIAL-SUB-rectype-06            value 26.
         78 MATERIAL-SUB-rectype-07            value 27.
         78 MATERIAL-SUB-rectype-08            value 28.
         78 MATERIAL-SUB-rectype-09            value 29.

         78 MaxRecType                         value 29.
         78 RTL                                value 36.
         01 RT1.
          05 pic x(RTL) value "15,08.Coatings".                    *> 1.
          05 pic x(RTL) value "40,13,Item comments".               *> 2.
          05 pic x(RTL) value "20,09,Country".                     *> 3.
          05 pic x(RTL) value "15,07,Designs".                     *> 4.
          05 pic x(RTL) value "20,14,Equipment type".              *> 5.
          05 pic x(RTL) value "30,20.Inspection companies".        *> 6.
          05 pic x(RTL) value "15,11,Insulations".                 *> 7.
          05 pic x(RTL) value "15,11,Instruments".                 *> 8.
          05 pic x(RTL) value "30,17,Item Descriptions".           *> 9.
          05 pic x(RTL) value "30,21,Item Sub-Descriptions".       *> 10.
          05 pic x(RTL) value "15,18,Lengths/Capacities".          *> 11.
          05 pic x(RTL) value "25,13,Manufacturers".               *> 12.
          05 pic x(RTL) value "30,15,Not used..........".          *> 13.
          05 pic x(RTL) value "24,20,Not used..........".          *> 14.
          05 pic x(RTL) value "20,25,Province, County, or State".  *> 15.
          05 pic x(RTL) value "30,16,System/Services".             *> 16.
          05 pic x(RTL) value "20,18,Technicians' names".          *> 17.
          05 pic x(RTL) value "30,12,Vessel names".                *> 18.
          05 pic x(RTL) value "20,12,Points".                      *> 19.
          05 pic x(RTL) value "30,15,Material Main Type".          *> 20.
          05 pic x(RTL) value "24,20,Material Sub-Type-01".        *> 21.
          05 pic x(RTL) value "24,20,Material Sub-Type-02".        *> 22.
          05 pic x(RTL) value "24,20,Material Sub-Type-03".        *> 23.
          05 pic x(RTL) value "24,20,Material Sub-Type-04".        *> 24.
          05 pic x(RTL) value "24,20,Material Sub-Type-05".        *> 25.
          05 pic x(RTL) value "24,20,Material Sub-Type-06".        *> 26.
          05 pic x(RTL) value "24,20,Material Sub-Type-07".        *> 27.
          05 pic x(RTL) value "24,20,Material Sub-Type-08".        *> 28.
          05 pic x(RTL) value "24,20,Material Sub-Type-09".        *> 29.

         01 RT2 redefines RT1.
           05 RT3 occurs MaxRecType.
              10  rt-nameLength              pic 99.
              10                             pic x.
              10  rt-TitleLength             pic 99.
              10                             pic x.
              10  rt-Title                   pic x(30).

        *>-------------------------------------------------------------

        copy "\copylib\ControlParams.cpy"
                                     replacing ==(tag)== by ==Dialog==.
        *>----------------- ControlParams.cpy-------------------------------

        *>   2005 Sep 10

         01 Dialog-Params.

            05 Control-Data.
              10 Dialog-ID                    pic 9(04).
              10                             pic x.
              10 Dialog-Sequence              pic 9(03).

              *> When passed to the template TabbedDialog (dlgtabbd)
              *> Sequence Number is moved to Dialog-Field below

              10                             pic x.
              10 Dialog-Length                pic 9(03).
              10                             pic x.
              10 Dialog-Alphanumeric          pic 9.
                 88 NumericK                 value 0.
                 88 LowerCase                value 1.
                 88 UpperCase                value 2.
                 88 Date4                    value 4.    *> yymm
              10                             pic x.
              10 Dialog-DisplayFormat         pic 9(02).
              10                             pic x.
              10 Dialog-ReturnType            pic 9.
                 88 DontReturn               value 0.
                 88 DoReturn                 value 1.
                 88 ReturnPrimeKey           value 2.
                 88 ReturnKeyboardChars      value 3.
                 88 ValidationReturn         value 9.
              10                             pic x.
              10 Dialog-ClearType             pic 9.
                 88 EmptyField               value 0.
                 88 InsertField              value 1.
              10                             pic x.
              10 Dialog-FontType              pic 9.
                 88 DefaultFont              value 0.
                 88 MSFont                   value 1.
                 88 SystemFont               value 2.
                 88 MonospacedFont           value 3.
              10                             pic x.
              10 Dialog-Position              pic 9.
                 88 LeftJustify              value 0.
                 88 Centered                 value 1.
                 88 RightJustify             value 2.
              10                             pic x.
              10 Dialog-ViewType              pic 9.
                 88 NotReadOnly              value 0.
                 88 ReadOnly                 value 1.
              10                             pic x.
              10 Dialog-EntryType             pic 9.
                  88 SingleLineEntry         value 0.
                  88 SingeLineWordWrap       value 1.
                  88 MultipleLineEntry       value 2.
                  88 MLENoScroll             value 3.
              10                             pic x.
              10 Dialog-Row                   pic 9(03).
              10                             pic x.
              10 Dialog-Column                pic 9(03).
              10                             pic x.
              10 Dialog-BorderType            pic 9.
                 88 Border                   value 0.
                 88 NoBorder                 value 1.
              10                             pic x.
              10 Dialog-MethodName            pic x(ML).

           05 Dialog-object                   object reference.
           05 Dialog-Text                     pic x(50).
           05 Dialog-Index                    pic x(4) comp-5.
           05 Dialog-Select                   pic 9.
              88 IsSelected                  value 1.
              88 IsNotSelected               value 0.

           05 Dialog-Field                    pic x(4) comp-5.


        *>  NOTE : (tag)-Field above is followed by a series of 78s
        *>  copy Dlgxxxxx.cpy).


        *> Resource Seq/Field Field
        *> ID       Number    Length   A DF R,C F P V EF Rw Col B, 
MethodName
        *> IIII,    SSS,      LLL, 
9,99,9,9,9,9,9,9,999,999,9,x-------30------------x
        *> -----    ----      ----     - -- - - - - - - --- --- - 
-------------------------
        *>
        *> ***** Table Parameters size = 40 + 30 = 70 *********
        *> see \copylib\dlg***.cpy for Level 78s - TL, ML and RL

        *> ws-Alphanumeric :
        *> A : 0   = Numerick      1 = Lowercase    2 = Uppercase
        *>     3   = Default (Numeric, Upper or Lower)
        *>     4   = Numeric Date (yymmdd)

        *> ws-DisplayFormat :
        *> I : 00 = Integer       13 = Decimal 1.3  etc.....

        *> ws-ReturnType :
        *> E : 0  = No return           1 = Return NOW
        *>     2  = Return PrimeKey     3 = Return KeyboardChars

        *> ws-ClearType :
        *> C : 0  = EmptyField     1 = InsertField

        *> ws-FontType :
        *> F : 0  = DefaultFont    1 = MsFont       2 = SystemFont
        *>                                          3 = MonospacedFont
        *>

        *> ws-Position :
        *> P : 0 = LeftJustify    1 = Centered      2 = RightJustify

        *> ws-ViewType
        *> V : 0 = NotReadOnly    1 = ReadOnly

        *> ReturnType :
        *>
        *>   0 = Don't Return IMMEDIATELY - a value is returned
        *>       when user presses PB-OK
        *>   1 = Do Return NOW - generally applies to Lists, Radio Button
        *>       or Checkboxes etc., when the selection should be returned
        *>       NOW so that the Business Class can make a decision what
        *>       to do next
        *>   2 = PrimeKey - used to specifically control return of a
        *>       Record PrimeKey for "new" records
        *>   3 = Return Keyboard characters, character by character -
        *>       used in association with a SEARCH on a List


        *> (tag)-Select is used to set the initial state for Checkboxes,
        *> whether initially 'blank', (= 0) or 'checked' (= 1)

        *> "Index" and "Select"  are used for highlighting/un-highlighting
        *> Listbox elements

        *>---------------------------------------------------------------

        copy "dlgdes.cpy".
        *>-----------------dlgdes.cpy --------------------------------

        *> Entryfields                    *> Resource ID

        88 field-PrimeKey       value 01. *>   0214
        88 field-Name           value 02. *>   0213
        88 field-OtherCode      value 03. *>   0212
        88 field-Other1         value 04. *>   0216
        88 field-Other2         value 05. *>   0217

        *> Listboxes

        88 field-ListBox        value 06. *>   0076

        *> Pushbuttons

        88 field-PB-Delete      value 07. *>   0054
        88 field-PB-OK          value 08. *>   0001
        88 field-PB-Cancel      value 09. *>   0002
        88 field-PB-Help        value 10. *>   0056
        88 field-PB-Next        value 11. *>   0058
        88 field-PB-NameSeq     value 12. *>   0074
        88 field-PB-KeySeq      value 13. *>   0073
        88 field-PB-OtherSeq    value 14. *>   0075


        78 DLG-DESRECRD               value 210.
        78 Last-DataField             value 05.
        78 MaxGadgets                 value 14.
        78 TextLength                 value 50.

*>-----------------------------------------------------------------

        01 DESCRIP-Info.
        copy "\copylib\sqlResult.cpy" replacing ==(tag)==  by ==05 ws==.
        copy "desrecrd.cpy" replacing
                                    ==(tag1)== by ==05 DESCRIP-record==,
                                    ==(tag)==  by ==DESCRIP==.
        *>-------------------- desrecrd.cpy -----------------------------

            05 DESCRIP-record.
            07 DESCRIP-DesPrimeKey.
               10 DESCRIP-DesID               PIC X(2).
               10 DESCRIP-DesCode             PIC X(4).
            07.
               10 DESCRIP-DesName             PIC X(40).
               10 DESCRIP-DesOther1           PIC X(10).
               10 DESCRIP-DesOther2           PIC X(10).

        *>--------------------------------------------------------------


        01 ws-CurrentIndex               pic x(4) comp-5.
        01 ws-ErrorCode                  pic 9(03).
           88 ContinueProgram            value 0.
           88 CancelProgram              value 99.
        01 ws-LastField                  pic x(4) comp-5.
        01 ws-PrimeKey.
           05 ws-CurrentRectype          pic 9(02).
           05 ws-key                     pic x(04).
        01 ws-suffix                     pic x(10).
        01 ValidationFlag                pic x(4) comp-5.
           88 ValidationOK               value 0.
           88 ErrorKey                   value 1.
           88 ErrorName                  value 2.
           88 ErrorComponent             value 3.
           88 ErrorLengthKey             value 4.
           88 ErrorOther1                value 5.
           88 ErrorOther2                value 6.
           88 OKtoDelete                 value 7.
           88 LogicError                 value 8.
           88 ValidationError            value 1 thru 99.

        *>--------------------------------------------------------------
        01 os-App                   object reference value null.
        01 os-Collection            object reference value null.
        01 os-ComponentDictionary   object reference value null.
        01 os-DescripsDBI           object reference value null.
        01 os-DesTable              object reference value null.
        01 os-Dialog                object reference value null.
        01 os-Menu                  object reference value null.
        01 os-ItemNames             object reference value null.
        01 os-Parent                object reference value null.
        01 os-Resource              object reference value null.

        *>-------------------------------------------------------------
        Method-id. "begin".
        *>-------------------------------------------------------------
        Local-storage section.
        01 ls-background                     pic x(12).
        01 ls-foreground                     pic x(12).
        01 DisplayFlag                       pic x(4) comp-5.
           88 EditMenu                       value 1.
           88 PrintMenu                      value 2.
        Linkage section.
        01 lnk-App                           object reference.

        Procedure Division using lnk-App.

         set os-App    to lnk-App
         invoke self "getHandles"
         initialize Dialog-Params
         invoke self "CreateDialog"
         invoke self "componentDictionary"
         set ContinueProgram to true
         invoke self "initDescripsCollection"
         invoke DescripsMenu "new" using os-Parent returning os-Menu
         set EditMenu to true
         invoke os-Menu "createMenu" using self, DisplayFlag
         invoke self "showMenu"

        End Method "begin".
        *>--------------------------------------------------------------
        Method-id. "checkItemComponent".
        *>--------------------------------------------------------------
        Local-storage section.
        01 ls-bool                           pic x comp-5.
           88 isTrue                         value 1.
        01 ls-length                         pic x(4) comp-5.
        01 ls-Key                            pic x(02).
        Linkage section.
        01 lnk-code                          pic x(02).
        Procedure Division using lnk-code.

         move function upper-case(lnk-code) to ls-Key
         invoke os-ComponentDictionary "includesKey"
                using     ls-Key
                returning ls-bool

        if     isTrue
               invoke os-ComponentDictionary "at"
                     using ls-Key returning DESCRIP-DesOther1
               invoke os-Dialog "setFocusDefault"

        else   set ErrorComponent to true
               invoke self "messageBox"
               set field-OtherCode to true
               invoke os-Dialog "setNextFocusEmpty" using Dialog-Params
        End-if

        End Method "checkItemComponent".
        *>------------------------------------------------------------
        Method-id. "checkRecordExists".
        *>--------------------------------------------------------------

          invoke os-DesTable "readRecord"
             using ws-PrimeKey returning DESCRIP-Info

          Evaluate true

            when ResultOK

                 if  ws-CurrentIndex = zeroes
                     invoke os-DESCRIPSDBI "getIndexfromKey"
                         using ws-key returning ws-CurrentIndex

                     if ws-CurrentIndex = zeroes
                        set LogicError to true
                        invoke self "messageBox"
                        invoke self "PB-Exit"
                        EXIT METHOD
                     End-if
                 End-if

                 invoke self "displayExistingRecord"

            when RecNotFound
                 set RecordNotFound      to true
                 set RecordNotChanged    to true
                 initialize                 DESCRIP-record
                 move ws-Currentrectype  to DESCRIP-DesID
                 move ws-key             to DESCRIP-DesCode

                 Evaluate true
                  when  ws-CurrentRectype = LENGTH-rectype
                        move ws-suffix to DESCRIP-DesOther1
                 End-evaluate

                 invoke self "displayNewRecord"

            when TableError
                 invoke self "PB-Exit"
          End-evaluate

        End Method "checkRecordExists".
        *>--------------------------------------------------------------
        Method-id. "ComponentDictionary".
        *>--------------------------------------------------------------
        Procedure Division.
           invoke ItemNames "new" returning os-ItemNames
           invoke os-ItemNames "createCompKeyOnlyDictionary"
                  returning os-ComponentDictionary

        End Method "ComponentDictionary".
        *>-------------------------------------------------------------
        Method-id. "createDescripsCollection".
        *>---------------------------------------------------------------
        Local-storage section.
        01 ls-values.
           05 ls-result              pic 9(03).
           05 ls-collection          object reference.

        Procedure Division.

         invoke os-DescripsDBI "CreateCollection"
            using      ws-CollectionParams, ws-CurrentRectype
            returning  ls-values

         Evaluate true
          when ls-result = zeroes

               *> Note the Tabstops Array is passed via Dialog-Object

               set Field-Listbox to true
               invoke os-DescripsDBI "createDialogTabstops"
                      using ws-CollectionParams returning Dialog-Object
               invoke os-Dialog "setTheTabstops"
                      using Dialog-Params
               set os-Collection to ls-collection
               invoke self "initTitleAndLength"
               invoke self "DisplayEmptyScreen"

          when other
               invoke self "PB-Exit"
         End-evaluate

        Exit Method.
        End Method "createDescripsCollection".
        *>---------------------------------------------------------------
        Method-id. "createDialog".
        *>--------------------------------------------------------------
        01 ls-id              pic x(4) comp-5.
        01 ls-template        object reference.

         move DLG-DESRECRD to ls-id
         invoke TabbedDialog "start"
         *>  invoke DropListDialog "start"
                using os-Parent, os-Resource, ls-id,
                returning os-Dialog
         invoke os-Dialog "begin" using self

         invoke DialogParameters "new" returning ls-template
         invoke ls-template "begin" using os-Dialog
         invoke ls-template "finalize" returning ls-template

        End Method "createDialog".
        *>-------------------------------------------------------------
        Method-id. "displayEmptyScreen".
        *>--------------------------------------------------------------
        *> Essentially the name of the method indicates what is
        *> happening. With the exception of the Primekey field, Listbox
        *> and Pushbuttons all other fields are disabled. The user is
        *> now left with three choices :-
        *>
        *> 1 - enter a PrimeKey for a "new" record
        *> 2 - select an entry from the Listbox
        *> 3 - use PB-Exit to get out of the program

        set RecordNotFound   to true
        set RecordNotChanged to true
        invoke os-Dialog     "clear"

        perform varying Dialog-Field from 2 by 1
                until Dialog-Field > Last-DataField
           invoke os-Dialog "setDisable" using Dialog-Params
        End-perform

        set Field-Listbox to true
        set Dialog-object to os-Collection
        invoke os-Dialog "setListboxContents" using Dialog-Params

        set Field-PrimeKey to true
        invoke os-Dialog "setEnable" using Dialog-Params

        if ws-CurrentIndex <> zeroes
           set Field-ListBox    to true
           move ws-CurrentIndex to Dialog-Index
           set isSelected       to true
           invoke os-Dialog "SetSelectedUnselected" using Dialog-Params
        End-if

        initialize  ws-CurrentIndex
        *>            ws-PrimeKey


        invoke os-Dialog "setFocusListbox"


        End Method "displayEmptyScreen".
        *>-----------------------------------------------------------
        Method-id. "displayExistingRecord".
        *>-----------------------------------------------------------
        Procedure Division.

         set Field-PrimeKey to true
         invoke self "passEntryfield"
         invoke os-Dialog "setDisable" using Dialog-Params

         invoke self "displayScreenEnabled"
         perform varying Dialog-Field from 2 by 1
                 until Dialog-Field > ws-LastField
             invoke self "passEntryfield"
         End-perform

        invoke os-Dialog "setFocusDefault"

        End Method "displayExistingRecord".
        *>------------------------------------------------------------
        Method-id. "displayNewRecord".
        *>-----------------------------------------------------------
        Procedure Division.

         set RecordNotFound    to true
         set RecordNotChanged  to true
         invoke os-Dialog "setRecordFlags"
             using RecordChangedFlag RecordFoundFlag

         move ws-PrimeKey      to Descrip-DesPrimeKey
         set Field-PrimeKey    to true
         invoke os-Dialog "setDisable" using Dialog-Params

         invoke self "displayScreenEnabled"
         perform varying Dialog-Field from 2 by 1
                 until Dialog-Field > ws-LastField
             invoke self "passEntryfield"
         End-perform

         set Field-Name to true
         invoke os-Dialog "setNextfocusEmpty" using Dialog-Params

        End Method "displayNewRecord".
        *>------------------------------------------------------------
        Method-id. "displayScreenEnabled".
        *>-----------------------------------------------------------
        *> Not all fields in the Dialog are applicable to each record
        *> type, so this routine signals to TabbedDialog the
        *> Last-DataField for each record type - see TabbedDialog
        *> "checkRecordChangerdFlag"

        Evaluate true
          when ws-CurrentRectype = ITEM-NAME-rectype
               move 3 to ws-LastField
          when ws-CurrentRectype = LENGTH-rectype
               move 3 to ws-LastField
          when ws-CurrentRectype = POINT-rectype
               move 3 to ws-LastField
          when ws-CurrentRectype = TECHNICIAN-NAME-rectype
               move 5 to ws-LastField
          when other
               move 2 to ws-LastField
        End-evaluate

        invoke os-Dialog "setLastField" using ws-LastField

        set field-PB-OtherSeq to true

        if    ws-LastField > 2
              invoke os-Dialog "setEnable" using Dialog-Params

        else  invoke os-Dialog "setDisable" using Dialog-Params
        End-if

        perform varying Dialog-Field from 2 by 1
                until Dialog-Field > ws-LastField
            invoke os-Dialog "setEnable" using Dialog-Params
        End-perform

        End Method "displayScreenEnabled".
        *>-----------------------------------------------------------
        Method-id. "finalizeObjects".
        *>------------------------------------------------------------
        Local-storage section.
        copy "\copylib\sqlResult.cpy" replacing ==(tag)== by ==01 ls==.
        01 ls-object                         object reference.

        Procedure Division.

         set ls-object to os-Collection
         perform FINALIZE-COLLECTIONS

         set ls-object to os-ComponentDictionary
         perform FINALIZE-COLLECTIONS

         set ls-object to os-ItemNames
         perform FINALIZE-SINGLE

         set ls-object to os-Dialog
         perform FINALIZE-DIALOG

         set ls-object to os-Menu
         perform FINALIZE-MENU

         invoke self "finalize" returning self
         .

        EXIT METHOD.

         copy "\copylib\finalize.cpy".

        End Method "finalizeObjects".
        *>------------------------------------------------------------
        Method-id. "getHandles".
        *>-------------------------------------------------------------
        Local-storage section.
        copy "cthandle.cpy".
*--------------------- cthandle.cpy --------------------------------------

        *>   Handles to file DBIs, Parent, Resource etc....

        01 cthandle                     pic x(4) comp-5.
           88 ClientsHandle             value 1.
           88 DefaultsHandle            value 2.
           88 DescripsHandle            value 3.
           88 DesktopHandle             value 4.
           88 MaterialsHandle           value 5.
           88 ParentHandle              value 6.
           88 ProbesHandle              value 7.
           88 ResourceHandle            value 8.
           88 TechniciansHandle         value 9.
           88 SqlConnectionHandle       value 10.
           88 DateTimeHandle            value 11.

        01 FacilityHandle               pic x(4) comp-5.
           88 GroupsHandle              value 1.
           88 InspectionsHandle         value 2.
           88 ItemsHandle               value 3.
           88 RangesHandle              value 4.
           88 ReadingsHandle            value 5.
           88 SummariesHandle           value 6.
           88 VesselsHandle             value 7.

*------------------------------------------------------------------------

        01 ls-handles.
           05 Object1                     object reference.
           05 Object2                     object reference.

        Procedure Division.

         *> This is an edited version of the copyfile
         *> gethandl.cpy, leaving in only relevant files

         *>-------- general handles


**** NOTE here how I am invoking "backwards" - my main Class is os-App 
which by menu selection invoked this class - now Program B is going back 
to Program-A to get necessary handles. The point being - for each menu 
invocation it's a simple "Invoke this Class". The code for each class 
knows which files/tables or other objects it needs and if they are part 
of the common table held in os-App they can be retrieved as necessasry 
by object reference.


           set DescripsHandle  to true
               invoke os-App "getGeneralHandles"
                 using CtHandle returning ls-handles
                       set os-DescripsDBI to Object1
                       set os-DesTable    to Object2

           set ParentHandle to true
               invoke os-App "getGeneralHandles"
                 using CtHandle returning ls-handles
                       set os-Parent to Object1

           set ResourceHandle  to true
               invoke os-App "getGeneralHandles"
                 using CtHandle returning ls-handles
                       set os-Resource to Object1

         *>-------- facility handles

        End Method "getHandles".
        *>-------------------------------------------------------------
        Method-id. "HelpFromMenu".
        *>-------------------------------------------------------------
         invoke CtHelp "helpFile" using os-Parent
        End Method "HelpFromMenu".
        *>-------------------------------------------------------------
        Method-id. "initDescripsCollection".
        *>-------------------------------------------------------------
        copy "deslist.cpy"    replacing ==(tag)== by ==ls==.

         initialize                ws-CollectionParams
         compute ws-DataLength   = function length (Des-Name)
         compute ws-Keylength    = function length (Des-Code)
         compute ws-RecordLength = function length (ls-DesListRecord)
         set ofReferences          to true
         move zeroes               to ws-sequence
         move 50                   to ws-Size
         set TypeSorted            to true

        End Method "initDescripsCollection".
        *>-------------------------------------------------------------
        Method-id. "initTitleAndLength".
        *>-----------------------------------------------------------
        Working-storage section.
        01 ws-WindowTitle  pic x(18) value "Description Codes ".
        Local-storage section.
        01 ls-DialogTitle            pic x(100).
        01 ls-length                 pic x(4) comp-5.
        01 ls-string                 object reference.
        01 ls-temp                   pic x(40).

        Procedure Division.

         move rt-Title (ws-CurrentRectype) to ls-temp
         string ws-WindowTitle     delimited by size
                " - "              delimited by size
                ls-temp            delimited by "  "
                x"00"              delimited by size
                into ls-DialogTitle
         End-string
         invoke os-Dialog "setTitleZ" using ls-DialogTitle

         set Field-Name                        to true
         move rt-namelength(ws-CurrentRectype) to Dialog-length
         invoke os-Dialog "setTheLength" using Dialog-Params


        End Method "initTitleAndLength".
        *>-----------------------------------------------------------
        Method-id. "messageBox".
        *>-----------------------------------------------------------
        Working-storage section.
        01 ws-Windowtitle    pic x(18) value z"Description Codes".
        78 ML                         value 45.
        01 ws-MsgA.
        05 pic x(ML) value z"Blank or non-numeric key".              *>1
        05 pic x(ML) value z"Can't have a blank name".               *>2
        05 pic x(ML) value z"Invalid Component Code".                *>3
        05 pic x(ML) value
            z"Key must start 'F' 'G' 'I' 'L' or 'U'".                *>4
        05 pic x(ML) value z"Missing Other1 Level".                  *>5
        05 pic x(ML) value z"Missing Other2 Level".                  *>6
        05 pic x(ML) value z"OK to delete this record ?".            *>7
        05 pic x(ML) value z"Program Indexing error".                *>8
        01 ws-MsgB redefines ws-MsgA occurs 8 pic x(ML).

        Local-storage section.
        01 ls-MsgBox              object reference.
        01 ls-MessageText         pic x(ML).

        linkage section.
        01 lnk-response             pic x(4) comp-5.

        Procedure Division returning lnk-response.

         invoke MessageBox "new" using os-Dialog returning ls-MsgBox
         invoke ls-MsgBox "setTitleZ" using ws-Windowtitle
         move ws-MsgB(ValidationFlag) to ls-MessageText

         evaluate true
           when OktoDelete
              invoke ls-MsgBox "setTypeInformation"
              invoke ls-MsgBox "okCancel"
           when LogicError
              invoke ls-MsgBox "setTypeCritical"
              invoke ls-MsgBox "ok"
           when other
              invoke ls-MsgBox "setTypeWarning"
              invoke ls-MsgBox "ok"
         End-evaluate

         invoke ls-MsgBox "setMessageZ" using ls-MessageText
         invoke ls-MsgBox "show" returning lnk-response
         invoke ls-MsgBox "finalize" returning ls-MsgBox

        End Method "messageBox".
        *>-------------------------------------------------------------
        Method-id. "passEntryfield".
        *>---------------------------------------------------------------
        Local-storage section.
        01 ls-CodeX                          pic x(02).
        01 ls-length                         pic x(4) comp-5.
        01 ls-text.
           05 occurs 1 to TextLength
              depending on ls-length     pic x(01).
        Procedure Division.

         evaluate true
           when Field-PrimeKey
                move length of DESCRIP-DesCode to Dialog-Length
                move DESCRIP-DesCode           to Dialog-text

           when field-name
                move length of DESCRIP-DesName to Dialog-Length
                move DESCRIP-DesName           to Dialog-text

           when field-OtherCode

                Evaluate true
                  when ws-CurrentRectype = ITEM-NAME-rectype
                       invoke os-ItemNames "getComponentAlphakey"
                              using DESCRIP-DesOther1 returning ls-CodeX
                       move length of ls-Codex to Dialog-Length
                       move ls-Codex           to Dialog-text

                  when ws-CurrentRectype = LENGTH-rectype
                       move length of DESCRIP-DesOther1 to Dialog-Length
                       move DESCRIP-DesOther1           to Dialog-text
                End-evaluate

           when field-Other1
                move length of DESCRIP-DesOther1 to Dialog-Length
                move DESCRIP-DesOther1           to Dialog-text

           when field-Other2
                move length of DESCRIP-DesOther2 to Dialog-Length
                move DESCRIP-DesOther2           to Dialog-text

         End-evaluate

         invoke os-Dialog "setEntryfield"
                using Dialog-Params, ls-length, ls-text

        End Method "passEntryfield".
        *>---------------------------------------------------------------
        Method-id. "returnMenuchoice".
        *>---------------------------------------------------------------
        Linkage section.
        01 lnk-choice                    pic x(4) comp-5.
        Procedure Division using lnk-choice.

        Evaluate lnk-choice
           when 998 invoke self "HelpFromMenu"
           when 999 invoke self "PB-Exit"

           when other
                invoke os-Menu "hide"
                move lnk-choice to ws-CurrentRecType
                set ByName       to true
                invoke self "createDescripsCollection"
        End-evaluate

        End Method "returnMenuchoice".
        *>---------------------------------------------------------------
        Method-id. "rewriteRecord".
        *>-------------------------------------------------------------
        Local-storage section.
        copy "deslist.cpy" replacing ==(tag)== by ==ls==.
        Procedure Division.

         invoke os-DesTable "rewriteRecord"
           using DESCRIp-record returning ws-SqlResult

         Evaluate true
            when ResultOK
              initialize ls-DesListRecord
              move DESCRIP-DesID     to Des-ID
              move DESCRIP-DesCode   to Des-Code
              move DESCRIP-DesName   to Des-Name
              move DESCRIP-DesOther1 to Des-Other1
              move DESCRIP-DesOther2 to Des-Other2

              invoke os-DescripsDBI "changeCollection"
              using     ws-CollectionParams,
                        ws-CurrentIndex,
                        ls-DesListRecord,
                        os-Collection
              returning os-Collection
              set Dialog-Object to os-Collection
              set field-Listbox to true
              invoke os-Dialog "setListboxContents"
                using  Dialog-Params
              invoke self "DisplayEmptyScreen"

            when TableError
               invoke self "PB-Exit"

         End-evaluate

        End Method "rewriteRecord".
        *>-------------------------------------------------------------
        Method-id. "showMenu".
        *>--------------------------------------------------------------
            invoke os-Dialog "hide"
            invoke os-Menu   "show"
        End Method "showMenu".
        *>--------------------------------------------------------------
        Method-id. "ValidationRoutine".
        *>-------------------------------------------------------------

         set ValidationOK     to true
         set ValidationReturn to true

         perform varying Dialog-Field from 2 by 1
                 until Dialog-Field > ws-LastField

            invoke os-Dialog "returnEntryField"
                   using Dialog-Field, Dialog-ReturnType

            if   ValidationError
                 invoke os-Dialog "setNextFocusEmpty" using Dialog-Params
                 EXIT METHOD
            End-if
         End-perform

        End Method "ValidationRoutine".
        *>---------------------------------------------------------------
        Method-id. "writeRecord".
        *>-------------------------------------------------------------
        Local-storage section.
        copy "deslist.cpy" replacing ==(tag)== by ==ls==.
        Procedure Division.

         invoke os-DesTable "writeRecord"
                using DESCRIp-record returning ws-SqlResult

         Evaluate true
            when ResultOK
              initialize ls-DesListRecord
              move DESCRIP-DesID     to Des-ID
              move DESCRIP-DesCode   to Des-Code
              move DESCRIP-DesName   to Des-Name
              move DESCRIP-DesOther1 to Des-Other1
              move DESCRIP-DesOther2 to Des-Other2

              invoke os-DescripsDBI "addToCollection"
                 using     ws-CollectionParams
                           ls-DesListRecord
                           os-Collection
                 returning os-Collection
              set Dialog-Object to os-Collection
              set field-Listbox to true
              invoke os-Dialog "setListboxContents"
                using  Dialog-Params
              invoke self "DisplayEmptyScreen"

             when TableError
                  invoke self "PB-Exit"
         End-evaluate

        End Method "writeRecord".
        *>---------------------------------------------------------------

        *> ++++ Return entries from Dialog :-

        *>       CB(Checkbox)
        *>       EF(Entryfield)
        *>       DL(DropList)
        *>       LB(Listbox)
        *>       PB(Pushbutton)
        *>       RB(RadioButton)
        *>--------------------------------------------------------------

        *> Key                  y
        *> Name                      y
        *> OtherCode                     y
        *> Other1                            y
        *> Other2                                y
        *>--------------------------------------------------------------
        *> All types             x   x   -   -   -
        *> Length(** 1)          x   x   x   -   -
        *> Name  (** 2)          x   x   x   -   -
        *> Points(** 3)          x   x   x   -   -
        *> Techs                 x   x   -   x   x
        *>--------------------------------------------------------------
        *> ** 1 - Check code(1:1) for Validity; display OtherCode
        *> ** 2 - Check Component Code and display Other Code
        *> ** 3 - Check code(1:1) = "X" & display OtherCode "Exception"
        *>--------------------------------------------------------------
        Method-id. "EF-name".
        *>--------------------------------------------------------------
        Linkage section.
        copy "\copylib\dlgReturn.cpy" replacing ==(tag)== by ==lnk==.
        *>----------------dilgreturn.cpy ------------------------------

        01  lnk-ReturnValues.

           *> Column and Line used to identify the 'cell' from a
           *> two-dimensional table

           05 lnk-Column                  pic x(4) comp-5.
           05 lnk-Line                    pic x(4) comp-5.
           05 lnk-length                  pic x(4) comp-5.

           *> Field-ID used where necessary to return a specific
           *> selection, such as the choice from a Radio Button Group

           05 lnk-FieldID                 pic x(4) comp-5.

           05 lnk-text.
              10 occurs 1 to ws-MaxLength
                 depending on lnk-length  pic x(01).

        *>--------------------------------------------------------------

        Procedure Division using lnk-ReturnValues.

        set RecordChanged to true

        if    lnk-text = spaces
              set ErrorName to true
              invoke self "messageBox"
              invoke os-Dialog "setnextfocusEmpty" using Dialog-Params
              EXIT METHOD

        else  move lnk-text to DESCRIP-DesName
        End-if

        if   ws-CurrentRectype = POINT-rectype
             perform CHECK-POINTS
        End-if

        EXIT METHOD.

        CHECK-POINTS.

        if   DESCRIP-DesCode(1:1) = "X"
             move "Exception" to DESCRIP-DesOther1

        else move spaces to DESCRIP-DesOther1
        End-if

        .
        End Method "EF-name".
        *>--------------------------------------------------------------
        Method-id. "EF-othercode".
        *>--------------------------------------------------------------
        Linkage section.
        copy "\copylib\dlgReturn.cpy" replacing ==(tag)== by ==lnk==.

        Procedure Division using lnk-ReturnValues.

         set RecordChanged to true

         if   ws-CurrentRectype = ITEM-NAME-rectype
             invoke self "checkItemComponent" using lnk-text
         End-if

        End Method "EF-othercode".
        *>--------------------------------------------------------------
        Method-id. "EF-Other1".
        *>--------------------------------------------------------------
        Linkage section.
        copy "\copylib\dlgReturn.cpy" replacing ==(tag)== by ==lnk==.

        Procedure Division using lnk-ReturnValues.

           set RecordChanged to true
           move lnk-text to DESCRIP-DesOther1
           invoke os-Dialog "setFocusDefault"

        End Method "EF-Other1".
        *>--------------------------------------------------------------
        Method-id. "EF-Other2".
        *>--------------------------------------------------------------
        Linkage section.
        copy "\copylib\dlgReturn.cpy" replacing ==(tag)== by ==lnk==.

        Procedure Division using lnk-ReturnValues.

           set RecordChanged to true
           move lnk-text to DESCRIP-DesOther2

           if   RecordNotFound
                set field-Other2 to true
                invoke os-Dialog "setNextFocusEmpty" using Dialog-Params

           else invoke os-Dialog "setFocusDefault"
           End-if

        End Method "EF-Other2".
        *>--------------------------------------------------------------
        Method-id. "EF-primekey".
        *>--------------------------------------------------------------
        Local-storage section.
        01 ls-result                    pic 9(03).
        Linkage section.
        copy "\copylib\dlgReturn.cpy" replacing ==(tag)== by ==lnk==.

        Procedure Division using lnk-ReturnValues.

          Evaluate true
            when  lnk-text = spaces
                  invoke self "DisplayEmptyScreen"
                  EXIT METHOD

            when  ws-CurrentRectype = LENGTH-rectype
                  invoke self "EF-Primekey2"
                         using lnk-length lnk-text
                         returning ls-result

                  if ls-result <> zeroes
                     set Field-PrimeKey to true
                     invoke os-Dialog "setNextFocusEmpty"
                            using Dialog-Params
                     EXIT METHOD
                  End-if

          End-evaluate

          move lnk-text to ws-key
          invoke self "checkRecordExists" using ws-PrimeKey

        End Method "EF-primekey".
        *>--------------------------------------------------------------
        Method-id. "EF-Primekey2".
        *>----------------------------------------------------------
        Working-storage section.
        78 TableSize                    value 5.
        78 TL                           value 12.
        01 wt1.
           05 pic x(TL) value "F,Feet".
           05 pic x(TL) value "G,Gallon-Imp".
           05 pic x(TL) value "I,inches".
           05 pic x(TL) value "L,Litres".
           05 pic x(TL) value "U,US-Gallon".
        01 wt2 redefines wt1.
           05 wt3 occurs TableSize.
              10 ws-char                pic x.
              10                        pic x.
              10 ws-code                pic x(10).

        Local-storage section.
        01 n                            pic x(4) comp-5.
        01 ls-text                      pic x(04).

        Linkage section.
        01 lnk-length                   pic x(4) comp-5.
        01 lnk-text.
           05 occurs 1 to TextLength
              depending on lnk-length   pic x(01).
        01 lnk-result                   pic 9(03).

        Procedure Division using     lnk-length lnk-text
                           returning lnk-result.


          move function upper-case (lnk-text) to ls-text
          move 99 to lnk-result
          perform varying n from 1 by 1 until n > TableSize

             if ls-text(1:1)     = ws-char(n)
                move zeroes       to lnk-result
                move ls-text      to ws-key
                move ws-code(n)   to ws-suffix
                Exit perform
             End-if
          End-perform

          if    lnk-result <> zeroes
                set ErrorLengthKey to true
                invoke self "messageBox"
          End-if

        End Method "EF-Primekey2".
        *>--------------------------------------------------------------
        Method-id. "LB-index".
        *>---------------------------------------------------------------
        Linkage section.
        01 lnk-Index                     pic x(4) comp-5.

        Procedure Division using lnk-Index.

          move lnk-Index to ws-CurrentIndex
          invoke os-DescripsDBI "getKeyFromIndex"
             using ws-CurrentIndex returning ws-Key

          if   ws-Key <> spaces
               invoke self "checkRecordExists" using ws-PrimeKey
          End-if

        End Method "LB-index".
        *>---------------------------------------------------------------
        Method-id. "PB-cancel".
        *>-------------------------------------------------------------
        Procedure Division.
          invoke self "DisplayEmptyScreen"
        End Method "PB-cancel".
        *>-------------------------------------------------------------
        Method-id. "PB-delete".
        *>-------------------------------------------------------------
        Local-storage section.
        01 ls-response                            pic x(4) comp-5.

        Procedure Division.

         if  RecordFound
             set OKtoDelete to true
             invoke self "messageBox" returning ls-response

             if ls-response = mb-return-ok
                invoke os-DesTable "deleteRecord"
                       using     DESCRIP-DesID
                                 DESCRIP-DesCode
                       returning ws-SqlResult

                if  ResultOK
                    invoke os-DescripsDBI "deleteFromCollection"
                           using     ws-CurrentIndex
                                     os-Collection
                           returning os-Collection
                    set Dialog-Object to os-Collection
                    set field-Listbox to true
                    invoke os-Dialog "setListboxContents"
                      using  Dialog-Params
                End-if

              End-if
         End-if

         if ContinueProgram
            invoke self "DisplayEmptyScreen"
         End-if

        End Method "PB-delete".
        *>-------------------------------------------------------------
        Method-id. "PB-Exit".
        *>-------------------------------------------------------------
        Procedure Division.
         set CancelProgram to true
         invoke os-Dialog "hide"
         invoke os-Menu "hide"
         invoke self "finalizeObjects"
        End Method "PB-Exit".
        *>-------------------------------------------------------------
        Method-id. "PB-help".
        *>-------------------------------------------------------------
         invoke CtHelp "helpFile" using os-Parent
         invoke self "showMenu"
        End Method "PB-help".
        *>-------------------------------------------------------------
        Method-id. "PB-nextMenuChoice".
        *>-------------------------------------------------------------
        Procedure Division.
         invoke self "showMenu"
        End Method "PB-nextMenuChoice".
        *>-------------------------------------------------------------
        Method-id. "PB-ok".
        *>-------------------------------------------------------------
        Local-storage section.
        01 n                                pic x(4) comp-5.
        Linkage section.
        01 lnk-Flag                         pic x(4) comp-5.
        Procedure Division using lnk-Flag.

        *> If individual entries have been returned before user clicks
        *> PB-OK, then "RecordChanged" will have alredy been set

        if     RecordNotChanged
               move lnk-Flag to RecordChangedFlag
        End-if

        if     RecordChanged
               perform VALIDATE-RECORD

        else   invoke self "displayEmptyScreen"
        End-if

        EXIT METHOD.

        VALIDATE-RECORD.

         set ValidationOK to true
         invoke self "validationRoutine"

         If ValidationOK

             if     RecordFound
                    invoke self "rewriteRecord"

             else   invoke self "writeRecord"
             End-if

             invoke self "displayEmptyScreen"

         End-if
         .
        End Method "PB-ok".
        *>-------------------------------------------------------------
        Method-id. "PB-sequenceByKey".
        *>---------------------------------------------------------------
        Procedure Division.

          if not ByKey
             set ByKey to true
             invoke self "createDescripsCollection"
          End-if

        End Method "PB-sequenceByKey".
        *>---------------------------------------------------------------
        Method-id. "PB-sequenceByName".
        *>---------------------------------------------------------------
        Procedure Division.

          if not ByName
             set ByName to true
             invoke self "createDescripsCollection"
          End-if
        End Method "PB-sequenceByName".
        *>---------------------------------------------------------------
        Method-id. "PB-sequenceByOther".
        *>---------------------------------------------------------------
        Procedure Division.

          if not ByOther
             Evaluate true
               when ws-CurrentRectype = ITEM-NAME-rectype
               when ws-CurrentRectype = LENGTH-rectype
               when ws-CurrentRectype = POINT-rectype
               when ws-CurrentRectype = TECHNICIAN-NAME-rectype
                    set ByOther to true
                    invoke self "createDescripsCollection"
              End-evaluate
          End-if

        End Method "PB-sequenceByOther".
        *>---------------------------------------------------------------

        End OBJECT.
        End CLASS DescripsEdit.

       *>---------------------------------------------------------------

*** DBI MAIN contains all common routines required by sub-classes 
prefixed "DBI"

Rather than copy/paste or try to remember collection creation syntax, it 
is all included in this class - in fact this is the only class in which 
I include direct references to the Collection support classes. The 
remainder of the program is used for creating/changing/searching 
collections.
        *>-----------------------dbimain.cbl ---------------------------

        copy "\copylib\dbimain.cbl".
        *>-----------------------dbimain.cbl ---------------------------

        Class-id.          DbiMain
                           inherits from Base.

        Class-control.     DbiMain                 is class "dbimain"

                           Array                   is class "array"
                           Association             is class "associtn"
                           Callback                is class "callback"
                           CharacterArray          is class "chararry"
                           Cobolcomp5              is class "comp5"
                           Cobolpicx               is class "picx"
                           Dictionary              is class "dictinry"
                           OrderedCollection       is class "ordrdcll"
                           SortedCollection        is class "srtdclln"
                           .

        *> Errors displayed are not in Windows GUI format because these
        *> errors occur as a result of coding and development testing -
        *> so they are DOS Text display with a STOP RUN.

        *>---------------------------------------------------------------
        FACTORY.
        *>---------------------------------------------------------------
        END FACTORY.
        *>---------------------------------------------------------------
        OBJECT.
        *>---------------------------------------------------------------
        OBJECT-STORAGE SECTION.
        copy "\copylib\collection.cpy"  replacing ==(tag)== by ==ws==.
        copy "\copylib\lnkObjVal.cpy" replacing ==(tag)== by ==ws==.

        01 n                                       pic x(4) comp-5.
        01 charx                                   pic x.
        01 ws-Kounter                              pic x(4) comp-5.
        01 ws-KounterDisplay                       pic zzzzz9.
        01 ErrorFlag                               pic x(4) comp-5.
           88 Error-Type                           value 1.
           88 Error-Sequence                       value 2.
           88 Error-RefOrValues                    value 3.
           88 Error-RecordLength                   value 4.
           88 Error-DictionaryLength               value 5.
           88 Error-DictionaryKey                  value 6.
           88 Error-ObjectFromKey                  value 7.
           88 Error-ChangeNoIndex                  value 8.
           88 Error-UnequalObjects                 value 9.

        01 ws-Key.
           05 occurs 1 to 100
              depending on ws-KeyLength            pic x.

        01 ws-Pos.
           10 PosName       pic 9(03).
           10 PosKey        pic 9(03).
           10 PosOther      pic 9(03).

        01 os-List                  object reference value null.
        01 os-OldString             object reference value null.

        *>---------------------------------------------------------------
        Method-id. "addToCollection".
        *>-------------------------------------------------------------
        Local-storage section.
        01 ls-string                             object reference.
        Linkage section.
        01 lnk-record.
           05 occurs 1 to 100
              depending on ws-RecordLength            pic x.

        Procedure Division using lnk-Record.

         Evaluate true
           when ofReferences
                invoke CharacterArray "withLengthValue" using
                   ws-RecordLength, lnk-record returning ls-string
                invoke os-List "add"
                         using ls-string returning ls-string
           when other
                invoke os-List "add" using lnk-record
         End-Evaluate

        End Method "addToCollection".
        *>--------------------------------------------------------------
        Method-id. "addToDictionary".
        *>--------------------------------------------------------------
        Linkage section.
        01 lnk-Key.
           05 occurs 1 to 100 depending
              on ws-KeyLength                   pic x.
        01 lnk-Data.
           05 occurs 1 to 100 depending
              on ws-DataLength                  pic x.

        Procedure Division using lnk-Key, lnk-Data.

         invoke os-List "atput"
                using lnk-key, lnk-data

        End Method "addToDictionary".
        *>--------------------------------------------------------------
        Method-id. "changeCollection".
        *>-------------------------------------------------------------
        Local-storage section.
        01 ls-Index1                         pic x(4) comp-5.
        01 ls-Index2                         pic x(4) comp-5.
        01 ls-string1                        object reference.
        01 ls-string2                        object reference.

        Linkage section.
        01 lnk-index                         pic x(4) comp-5.
        01 lnk-record.
           05 occurs 1 to 100
              depending on ws-RecordLength           pic x.

        Procedure division using lnk-Index, lnk-Record.

        invoke CharacterArray "withLengthValue" using
               ws-RecordLength, lnk-record returning ls-string2
        move lnk-index to ls-Index1 ls-Index2
        invoke os-List "at"
               using ls-index1 returning ls-string1
        invoke os-List "replaceAll"
               using ls-string1 ls-string2 ls-index1 ls-index2

        End Method "changeCollection".
        *>-------------------------------------------------------------
        Method-id. "changeWithNoIndex".
        *>-------------------------------------------------------------
        *> As a result of changing a Droplist inter-acting with a
        *> Dialog used in conjunction with say, a Treeview, the
        *> 'parent' class will not have an :-

        *> 'invoke ls-Object "selectedItem" returning ls-index'

        *> Where no Index is supplied objects are compared
        *> in "CheckObjects" to establish the index of the
        *> Element to be changed

        *> Local-storage section.
        01 ls-NewString                         object reference.

        Linkage section.
        01 lnk-Strings.
           05 lnk-OldString                      object reference.
           05 lnk-NewString                      object reference.

        Procedure division using lnk-Strings.

        set os-OldString     to lnk-OldString
        initialize              ws-ObjectValues
        move ws-RecordLength to ws-RecLength
        set ws-ObjectString  to null
        invoke self "compareObjects"

        if   ws-Index <> zeroes
             invoke os-List "replaceAll"
                  using os-OldString, lnk-NewString, ws-index ws-index

        else set Error-ChangeNoIndex to true
             invoke self "showError"
        End-if

        End Method "changeWithNoIndex".
        *>-------------------------------------------------------------
        Method-id. "CheckObjects".          *> Callback   PRIVATE
        *>-------------------------------------------------------------

        *> Using method "KeysAndDataDo", the index and element are
        *> automaticaly passed to this method

        01 ls-Bool                      pic 99 comp-5.
           88 IsTrue                    value 1.
           88 isFalse                   value 0.

        Linkage section.
        01 lnk-Index                    pic x(4) comp-5.
        01 lnk-Element                  object reference.

        Procedure Division using lnk-Index, lnk-Element.

        *> Having read the N/E V 4.0 OO Book, could have coded this as
        *>
        *> if os-Oldstring = lnk-Element
        *>    set isTrue to true.........

           invoke os-OldString "equal"
                  using lnk-Element returning ls-Bool

           if IsTrue
              move lnk-Index to ws-Index
              invoke os-List "quitIteration"
           End-if

        Exit Method.
        End Method "CheckObjects".
        *>-------------------------------------------------------------
        Method-id. "checkParameters".
        *>-------------------------------------------------------------

        *>----------------------------------------------------------
        *>  1 - Collection Types :
        *>----------------------------------------------------------
        *>  TypeOrdered  :
        *>  TypeSorted   :
        *>  TypeDroplist :

        *>  -  ofReferences   y  y  y  y  n  n  n  n
        *>  -  ofValues       y  y  n  n  y  y  n  n
        *> RecordLength ?     y  n  y  n  y  n  y  n
        *>---------------------------------------------------------
        *> Actions
        *>---------------------------------------------------------
        *> N/A                x  x  x  x  x  -  x  -
        *> Error              -  -  -  -  -  x  -  x
        *>----------------------------------------------------------

        *>----------------------------------------------------------
        *> 2 - Dictionary Types :
        *>----------------------------------------------------------
        *>  TypeDictionary :
        *>  - Data/NameLength   y  y  n  n
        *>  - KeyLength         y  n  y  n
        *>----------------------------------------------------------
        *> Actions :
        *>----------------------------------------------------------
        *> N/A                  x  -  -  -
        *> Error                -  x  x  x
        *>----------------------------------------------------------

        if not ValidType
               set Error-Type to true
               invoke self "showError"
               EXIT METHOD
        End-if

        if not ValidSequence
               set Error-Sequence to true
               invoke self "showError"
               EXIT METHOD
        End-if

        if     ws-size = zeroes
               move 10 to ws-size
        End-if

        if not TypeDictionary
               perform COLLECTION-CHECKS

        else   perfoRM DICTIONARY-CHECKS
        end-if

        EXIT METHOD.

        COLLECTION-CHECKS.

        if not ValidRefValues
             set Error-RefOrValues to true
             invoke self "showError"
             EXIT METHOD
        End-if

        if   ws-RecordLength = zeroes
             set Error-RecordLength to true
             invoke self "showError"
             EXIT METHOD
        End-if

        .
        DICTIONARY-CHECKS.

        if   ws-NameLength = zeroes
             set Error-DictionaryLength to true
             invoke self "showError"
             EXIT METHOD
        End-if

        if   ws-KeyLength = zeroes
             set Error-DictionaryKey to true
             invoke self "showError"
             EXIT METHOD
        End-if

        .

        End Method "checkParameters".
        *>-------------------------------------------------------------
        Method-id. "compareObjects".    *> create Callback PRIVATE
        *>-------------------------------------------------------------
        01 ls-Callback                      object reference.

        move zeroes  to ws-Index
        invoke Callback "new" using     self "CheckObjects "
                              returning ls-Callback
        invoke os-List "KeysAndDataDo" using ls-Callback
        invoke ls-Callback "finalize" returning ls-Callback

        *> Note :  when using Method "KeysandDataDo", for each
        *>         iteration the invoke passes (1) Index and (2) Element

        End Method "compareObjects".
        *>-------------------------------------------------------------
        Method-id. "deleteFromCollection".
        *>------------------------------------------------------------
        Local-storage section.
        01 ls-string                         object reference.
        Linkage section.
        01 lnk-index                         pic x(4) comp-5.

        Procedure Division using lnk-index.

          invoke os-List "removeAtIndex"
                 using lnk-index returning ls-string

        End Method "deleteFromCollection".
        *>------------------------------------------------------------
        Method-id. "deleteWithNoIndex".    *> Creates a Callback
        *>------------------------------------------------------------

        *> As a result of changing a Droplist inter-acting with a
        *> Dialog used in conjunction with say, a Treeview, the
        *> 'parent' class will not have an :-

        *> 'invoke ls-Object "selectedItem" returning ls-index'

        *> Where no Index is supplied objects are compared in
        *> "CheckObjects" to establish the index of the
        *> Element to be deleted

        Linkage section.
        01 lnk-record.
           05 occurs 1 to 100
              depending on ws-RecordLength           pic x.

        Procedure division using lnk-Record.

        invoke CharacterArray "withLengthValue" using
               ws-RecordLength, lnk-record returning os-OldString

        invoke self "compareObjects"

        if  ws-Index <> zeroes
            invoke os-List "removeAtIndex"
               using ws-index returning os-OldString
        End-if

        End Method "deleteWithNoIndex".
        *>------------------------------------------------------------
        Method-id. "DisplayList". *> Test - invoked from requesting class
        *>---------------------------------------------------------------
        01 ls-Callback                      object reference.

        move zeroes to n, ws-Kounter
        Evaluate true
          when TypeDictionary
             invoke Callback "new"
             using self "DisplayDictionaryVal "
             returning ls-Callback
             invoke os-List "KeysAndDataDo" using ls-Callback

          when other

             Evaluate true
               when ofReferences
                  invoke Callback "new"
                    using self "DisplayCollectionRef "
                    returning ls-Callback
               when other
                  invoke Callback "new"
                  using self "DisplayCollectionVal "
                  returning ls-Callback
              End-Evaluate

              invoke os-List "do" using ls-Callback

        End-Evaluate

        move ws-Kounter to ws-KounterDisplay
        display "Elements displayed =    ", ws-KounterDisplay
        accept charx
        invoke ls-Callback "finalize" returning ls-Callback

        End Method "DisplayList".
        *>------------------------------------------------------------
        Method-id. "displayCollectionRef".        *> PRIVATE
        *>------------------------------------------------------------
        01 ls-Length                            pic x(4) comp-5.
        01 ls-Text                              pic x(100).
        Linkage section.
        01 lnk-Element                          object reference.

        Procedure Division using lnk-Element.

         invoke lnk-Element "sizeInBytes" returning ls-Length
         invoke lnk-Element "getValueWithSize"
                   using ls-Length returning ls-Text
         invoke self "displayCollection2" using ls-text

        End Method "displayCollectionRef".
        *>------------------------------------------------------------
        Method-id. "displayCollectionVal".           *> PRIVATE
        *>------------------------------------------------------------
        Linkage section.
        01 lnk-Element              object reference.
        01 lnk-Text.
           05 occurs 1 to 100
              depending on ws-RecordLength           pic x.

        Procedure division using lnk-Element.
         invoke self "displayCollection2" using lnk-Element

        End Method "displayCollectionVal".
        *>------------------------------------------------------------
        Method-id. "DisplayCollection2".           *> PRIVATE
        *>------------------------------------------------------------
        Working-storage section.
        01 Line1 pic x(80) value
        ".........1.........2.........3.........4.........5.........6....
       -".....7.........8".

        01 Line2 pic x(80) value
        ".........0.........0.........0.........0.........0.........0....
       -".....0.........0".

        Linkage section.
        01 lnk-Text.
           05 occurs 1 to 100
              depending on ws-RecordLength           pic x.

        Procedure division using lnk-Text.

         inspect lnk-Text replacing all x"09" by "^"
         inspect lnk-Text replacing all x"00" by "~"

         if n = 0
            perform HEADER-LINES

         else add 1 to n
         End-if

         if   n > 23
              accept charx
              perform HEADER-LINES

         else add 1 to ws-Kounter
              move ws-Kounter to ws-KounterDisplay
              display ws-KounterDisplay " " lnk-text(1:ws-RecordLength)
         End-if

        EXIT METHOD
        .

        HEADER-LINES.

        display Line1
        display Line2
        move 3 to n
        .
        End Method "DisplayCollection2".
        *>---------------------------------------------------------------
        Method-id. "displayDictionaryVal".           *> PRIVATE
        *>------------------------------------------------------------
        Working-storage section.
        01 Line1 pic x(80) value
        ".........1.........2.........3.........4.........5.........6....
       -".....7.........8".

        01 Line2 pic x(80) value
        ".........0.........0.........0.........0.........0.........0....
       -".....0.........0".

        Linkage section.
        01 lnk-Record.
              10 occurs 1 to 100
                 depending on ws-RecordLength              pic x.

        Procedure division using lnk-Record.

         inspect lnk-Record replacing all x"09" by "^"

         if n = 0
            perform HEADER-LINES

         else add 1 to n
         End-if

         if   n > 23
              accept charx
              perform HEADER-LINES

         else add 1 to ws-Kounter
              move ws-Kounter to ws-KounterDisplay
              display
               ws-KounterDisplay " " lnk-record(1:ws-RecordLength)
         End-if

        EXIT METHOD
        .

        HEADER-LINES.

        display Line1
        display Line2
        move 3 to n
        .
        End Method "displayDictionaryVal".
        *>---------------------------------------------------------------
        Method-id. "finalizeObjects".
        *>------------------------------------------------------------
        Local-storage section.
        copy "\copylib\sqlresult.cpy" replacing ==(tag)== by ==01 ls==.
        01 ls-object                         object reference.

           *> NOT currently used

        End Method "finalizeObjects".
        *>------------------------------------------------------------
        Method-id. "getObjectFromIndex".
        *>------------------------------------------------------------
        Linkage section.
        01 lnk-index-In                      pic x(4) comp-5.
        copy "\copylib\lnkObjVal.cpy" replacing ==(tag)== by ==lnk==.

        Procedure Division using     lnk-Index-In
                           returning lnk-ObjectValues.

        move lnk-Index-In     to ws-Index
        set  ws-ObjectString  to null
        move ws-RecordLength  to ws-RecLength
        initialize               ws-Record


        invoke os-List "at"
               using ws-index returning ws-ObjectString
        invoke ws-ObjectString "getValuewithSize"
               using ws-RecLength returning ws-record
        move ws-ObjectValues to lnk-ObjectValues

        Exit Method.
        End Method "getObjectFromIndex".
        *>------------------------------------------------------------
        Method-id. "getObjectFromKey".
        *>------------------------------------------------------------
        01 ls-Callback                  object reference.
        Linkage section.
        01 lnk-Key.
           05 occurs 1 to 100
              depending on ws-KeyLength           pic x.
        copy "\copylib\lnkObjVal.cpy" replacing ==(tag)== by ==lnk==.

        Procedure Division using     lnk-Key
                           returning lnk-ObjectValues.

        *> TEST----------
        *>invoke self "displayList"

        move lnk-key          to ws-Key
        move zeroes           to ws-Index
        set ws-ObjectString   to null
        move ws-RecordLength  to ws-RecLength
        initialize               ws-Record

        invoke Callback "new"  using     self "getObjectFromKey2 "
                               returning ls-Callback
        invoke os-List "KeysAndDataDo" using ls-Callback
        move ws-ObjectValues to lnk-ObjectValues
        set lnk-objectString to ws-ObjectString

        if  lnk-Index = zeroes OR
            lnk-ObjectString = null
            set Error-ObjectFromKey to true
            invoke self "showError"
        End-if

        invoke ls-Callback "finalize" returning ls-Callback

        End Method "getObjectFromKey".
        *>------------------------------------------------------------
        Method-id. "getObjectFromKey2".        *> Callback  PRIVATE
        *>-------------------------------------------------------------
        *> Using method "KeysAndDataDo", the index and element are
        *> automaticaly passed to this method

         01 ls-Length                   pic x(4) comp-5.

        Linkage section.
        01 lnk-Index                    pic x(4) comp-5.
        01 lnk-Element                  object reference.

        Procedure Division using lnk-Index, lnk-Element.

        invoke lnk-Element "sizeInBytes" returning ls-Length
        invoke lnk-Element "getValueWithSize"
                  using ls-Length returning ws-Record

        *> ws-Key is in Working-Storage

        if ws-Key(1:ws-KeyLength) =
           ws-Record(PosKey:ws-KeyLength)
           move lnk-Index       to ws-Index
           set  ws-ObjectString to lnk-Element
           invoke os-List "quitIteration"
        End-if

        End Method "getObjectFromKey2".
        *>-------------------------------------------------------------
        Method-id. "initDictionary".
        *>---------------------------------------------------------------
        01 ls-AssocTemplate         object reference value null.
        01 ls-DataTemplate          object reference value null.
        01 ls-KeyTemplate           object reference value null.

        Linkage section.
        copy "\copylib\collection.cpy" replacing ==(tag)== by ==lnk==.
        01 lnk-Dictionary                    object reference.

        Procedure Division using     lnk-CollectionParams
                           returning lnk-Dictionary.

        move lnk-CollectionParams to ws-CollectionParams
        invoke self "checkParameters"

        invoke CobolPicx   "newclass"   using     ws-KeyLength
                                        returning ls-KeyTemplate
        invoke CobolPicx   "newclass"   using     ws-DataLength
                                        returning ls-DataTemplate
        invoke Association "newclass"   using     ls-KeyTemplate
                                                  ls-DataTemplate
                                        returning ls-AssocTemplate

        Evaluate true
          when ofValues
               invoke Dictionary "ofValues"
                      using     ls-AssocTemplate
                                ws-DataLength
                      returning os-List
               set lnk-Dictionary to os-List

        End-evaluate

        invoke ls-AssocTemplate "finalize" returning ls-AssocTemplate
        invoke ls-DataTemplate  "finalize" returning ls-DataTemplate
        invoke ls-KeyTemplate   "finalize" returning ls-KeyTemplate

        End Method "initDictionary".
        *>---------------------------------------------------------------
        Method-id. "initOrderedCollection".
        *>---------------------------------------------------------------
        *> Local-storage section.
        01 ls-Template                      object reference.
        Linkage section.
        copy "\copylib\collection.cpy" replacing ==(tag)== by ==lnk==.
        01 lnk-Pos.
           05 occurs 3 pic 9(03).

        01 lnk-Collection                     object reference.

        Procedure Division using     lnk-CollectionParams
                                     lnk-Pos
                           returning lnk-Collection.

        move lnk-CollectionParams to ws-CollectionParams
        move lnk-Pos              to ws-Pos
        invoke self "checkParameters"

        Evaluate true
          when ofReferences
               invoke OrderedCollection "ofReferences"
                      using     ws-size
                      returning os-List
               set lnk-Collection to os-List

          when ofValues
               invoke CobolPicx "newClass" using ws-RecordLength
                      returning ls-template
               invoke OrderedCollection "ofValues"
                      using     ls-template, ws-size
                      returning os-List
               set lnk-Collection to os-List
        End-evaluate

        End Method "initOrderedCollection".
        *>---------------------------------------------------------------
        Method-id. "initSortedCollection".
        *>---------------------------------------------------------------
        *> Local-storage section.
        01 ls-Template                      object reference.
        Linkage section.
        copy "\copylib\collection.cpy" replacing ==(tag)== by ==lnk==.
        01 lnk-Pos.
           05 occurs 3 pic 9(03).

        01 lnk-Collection                     object reference.

        Procedure Division using     lnk-CollectionParams
                                     lnk-Pos
                           returning lnk-Collection.

        move lnk-CollectionParams to ws-CollectionParams
        move lnk-Pos              to ws-Pos
        invoke self "checkParameters"

        Evaluate true
          when ofReferences
               invoke SortedCollection "ofReferences"
                      using     ws-size
                      returning os-List
               set lnk-Collection to os-List

          when ofValues
               invoke CobolPicx "newClass" using ws-RecordLength
                      returning ls-template
               invoke SortedCollection "ofValues"
                      using     ls-template, ws-size
                      returning os-List
               set lnk-Collection to os-List
        End-evaluate

        End Method "initSortedCollection".
        *>---------------------------------------------------------------
        Method-id. "setOslist".
        *>---------------------------------------------------------------
        *> When using "CopyEmptyWithSize" in the invoker, the new
        *> reference to the revised list is passed to this 'super'.

        Linkage section.
        01 lnk-List                     object reference.
        Procedure Division using lnk-List.
        set os-List to lnk-List
        End Method "setOslist".
        *>---------------------------------------------------------------
        Method-id. "showError".               *> PRIVATE
        *>---------------------------------------------------------------
        Working-storage section.
        78 ML                            value 50.
        78 MN                            value 9.
        01 T1.
         05 pic x(ML) value "Error - Type".                     *> 1
         05 pic x(ML) value "Error - Sequence".                 *> 2
         05 pic x(ML) value "Error - RefOrValues".              *> 3
         05 pic x(ML) value "Error - RecordLength".             *> 4
         05 pic x(ML) value "Error - DictionaryLength".         *> 5
         05 pic x(ML) value "Error - DictionaryKey".            *> 6
         05 pic x(ML) value "Error - ObjectFromKey".            *> 7
         05 pic x(ML) value "Error - ChangeNoIndex".            *> 8
         05 pic x(ML) value "Error - Non-matching objects".     *> 9

        01 T2 redefines T1.
         05 T3 occurs MN pic x(ML).

           display T3(ErrorFlag)
           accept charx
           STOP RUN

        End Method "showError".
        *>---------------------------------------------------------------

        END OBJECT.
        End CLASS DbiMain.



*>----------------------------------------------------------------------
        *>--------------------------------------------------------------


*** DBI DESCRIPTONS - INHERITS FROM DBI MAIN ABOVE :

*>---------------------------- dbides.cbl -----------------------------

        Class-id.          DescripsDBI
                           inherits from DbiMain.

        Class-control.     DbiMain                 is class "dbimain"
                           DescripsDBI             is class "dbides"

                           Array                   is class "array"
                           CharacterArray          is class "chararry"
                           Cobolcomp5              is class "comp5"
                           .

        *> NOTE : For changes to DESCRIP-records, (held in the DB),
        *> copyfile "tabdes.cpy" is used. For changes to Collections/
        *> Dictionaries, copyfile "deslist.cpy" is used
        *>
        *> While Schedule Nos. are not part of the DesTable - routines to
        *> access them are included here for convenience because they are
        *> part of the 'descriptive set'

        *>---------------------------------------------------------------
        FACTORY.
        *>---------------------------------------------------------------
        End FACTORY.
        *>---------------------------------------------------------------
        OBJECT.
        *>---------------------------------------------------------------
        OBJECT-STORAGE SECTION.
        copy "\copylib\collection.cpy"   replacing ==(tag)== by ==ws==.
*>--------------------- collection.cpy-------------------------------

        *> Note : the pic x(4) comp-5's are required by M/F classes

        01  ws-CollectionParams.

            10 ws-DataLength               pic x(4) comp-5.
            10 ws-NameLength
               redefines ws-DataLength     pic x(4) comp-5.
            10 ws-KeyLength                pic x(4) comp-5.
            10 ws-RecordLength             pic x(4) comp-5.

            10 ws-RefOrValues              pic 9.
               88 ofReferences                value 1.
               88 ofValues                    value 2.
               88 ValidRefValues              value 1 thru 2.

            10 ws-sequence                 pic 9(02).
               88 ByName                      value 1.
               88 ByKey                       value 2.
               88 ByOther                     value 3.
               88 ByItem                      value 4.
               88 ByKeyOnly                   value 5.
               88 ByKeyAndName                value 6.
               88 ByKeyAndData                value 7.
               88 ByKeyAndOther               value 8.
               88 ByNameAndKey                value 9.

               88 RmPointsDictionary          value 20.

               88 ValidSequence               value 1 thru 9, 20.

            10 ws-size                     pic x(4) comp-5.

            10 ws-type                     pic 9(02).
               88 TypeDictionary              value 1.
               88 TypeDropList                value 2.
               88 TypeOrdered                 value 3.
               88 TypeSorted                  value 4.
               88 TypeDummyDropList           value 5.
               88 ValidType                   value 1 thru 5.
               88 TypeTabbed                  value 3 thru 4. *> x"09"

*>-------------------------------------------------------------------

        copy "deslist.cpy" replacing ==(tag)== by ==ws==.
        *>---------------- deslist.cpy ---------------------------------

        01  ws-DesListRecord.
           05 Des-ID                    pic 9(02).
           05 Des-Code                  pic x(04).
           05 Tab1                      pic x.
           05 Des-Name                  pic x(40).
           05 Tab2                      pic x.
           05 Des-Other1                pic x(10).
           05 Des-Other2                pic x(10).

        *>---------------------------------------------------------------

        01 charx                           pic x.

        01 DisplayFlag                     pic 9 value 0.
           88 DisplayList                  value 1.
           88 DontDisplayList              value 0.

        *> Listbox records :-

        *> ByName (Name, Key, Other)
        *>0                                        4    4
        *>1                                        2    7
 
*>nnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnn^kkkk^OOOOOOOOOOOOOOOOOOOOO

        01 ByNameA.
           05 PosName    pic 9(03) value 01.
           05 PosKey     pic 9(03) value 42.
           05 PosOther   pic 9(03) value 47.

        01 Name-Record.
           05 Des-Name                     pic x(40).
           05 Tab1                         pic x.
           05 Des-Code                     pic x(04).
           05 Tab2                         pic x.
           05 Des-Other1                   pic x(10).
           05 Des-Other2                   pic x(10).

        *> ByKey (Key, Name, Other)
        *>0    0                                        4
        *>1    6                                        7
        *>kkkk^nnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnn^OOOOOOOOOOOOOOOOOOOO

        01 ByKeyA.
           05 PosKey     pic 9(03) value 01.
           05 PosName    pic 9(03) value 06.
           05 PosOther   pic 9(03) value 47.

        01 Key-Record.
           05 Des-Code                     pic x(04).
           05 Tab1                         pic x.
           05 Des-Name                     pic x(40).
           05 Tab2                         pic x.
           05 Des-Other1                   pic x(10).
           05 Des-Other2                   pic x(10).


        *> ByOther (Other, Name, Key)
        *>0                    2                                        4
        *>1                    2                                        4
        *>ooooooooooOOOOOOOOOO^nnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnn^k
        *>

        01 ByOtherA.
           05 PosOther   pic 9(03) value 01.
           05 PosName    pic 9(03) value 22.
           05 PosKey     pic 9(03) value 44.

        01 Other-Record.
           05 Des-Other1                   pic x(10).
           05 Des-Other2                   pic x(10).
           05 Tab1                         pic x.
           05 Des-Name                     pic x(40).
           05 Tab2                         pic x.
           05 Des-Code                     pic x(04).

        01 ws-Pos.
           05 PosName           pic 9(03).
           05 PosKey            pic 9(03).
           05 PosOther          pic 9(03).

        01 ws-TempRecord                   pic x(66).
        01 ws-LastNumber                   pic 9(03).

        *> OBJECTS
        01 os-DesTable            object reference value null.
        01 os-List                object reference value null.

        *>------------------------------------------------------------
        Method-id. "begin".
        *>------------------------------------------------------------
        Linkage section.
        01 lnk-Table                         object reference.
        Procedure Division using lnk-Table.
          set os-DesTable to lnk-Table
        End Method "begin".
        *>-------------------------------------------------------------
        Method-id. "addToCollection".
        *>-------------------------------------------------------------
        *> also invoked from DesTable by iteration

        Linkage section.
        copy "deslist.cpy" replacing ==(tag)== by ==lnk==.

        Procedure Division using lnk-DesListRecord.
         move lnk-DesListRecord to ws-DesListRecord
         invoke self "makeListRecord"
         invoke super "addToCollection" using ws-TempRecord
        End Method "addToCollection".
        *>-------------------------------------------------------------
        Method-id. "changeCollection".
        *>-------------------------------------------------------------
        Linkage section.
        01 lnk-index                         pic x(4) comp-5.
        copy "deslist.cpy" replacing ==(tag)== by ==lnk==.

        Procedure Division using     lnk-index
                                     lnk-DesListRecord.

          move lnk-DesListRecord to ws-DesListRecord
          invoke self "makeListRecord"
          invoke super "changeCollection"
                 using lnk-Index, ws-TempRecord

        End Method "changeCollection".
        *>-------------------------------------------------------------
        Method-id. "createCollection".
        *>------------------------------------------------------------
        Working-storage section.
        01 ws-MethodName pic x(30) value "AddToCollection".

        Linkage section.
        copy "\copylib\collection.cpy" replacing ==(tag)==  by ==lnk==.
        01 lnk-Rectype                       pic 9(02).
        01 lnk-values.
         copy "\copylib\sqlResult.cpy" replacing ==(tag)== by ==05 lnk==.
        *>----------sqlresult.cpy-------------------------------------

            05 lnk-SqlResult                        pic 9(03).

            88  ResultOK                    value 0.
            88  FileResultOK                value 0.

            88  RecNotFound                 value 1.

            88  NoMoreRows                  value 2.
            88  FileFinis                   value 2.

            88  DuplicateKey                value 3.

            88  DataError                   value 97.
            88  CursorError                 value 98.

            88  FileError                   value 99.
            88  TableError                  value 97 thru 99.

        *> This copyfile is multi-purpose with Level 88s used for either
        *> COBOL files or (R)DBMS Tables using SQLERROR or SQLSTATE

        *>-------------------------------------------------------------

         05 lnk-collection                 object reference.

        Procedure division using     lnk-CollectionParams
                                     lnk-Rectype
                           returning lnk-values.

         set ResultOK              to true
         set lnk-Collection        to null
         move zeroes               to ws-LastNumber
         move lnk-CollectionParams to ws-CollectionParams

         Evaluate true
           when ByName  move CORR ByNameA  to ws-Pos
           when ByKey   move CORR ByKeyA   to ws-Pos
           when ByOther move CORR ByOtherA to ws-Pos
         End-Evaluate

         invoke super "initSortedCollection"
                using       ws-CollectionParams
                            ws-Pos
                returning   os-List
         set lnk-Collection to os-List
         invoke os-DesTable "makeCollection"
                using       self
                            ws-MethodName
                            lnk-Rectype
                returning   lnk-SqlResult

         *>   NOTE : DesTable using iteration, invokes
         *>  "AddToCollection" in this Class

         if DisplayList
            invoke super "displayList"
         End-if


        End Method "createCollection".
        *>---------------------------------------------------------------
        Method-id. "createDialogTabstops".
        *>-------------------------------------------------------------
        *> NOTE : To use Tabs in a Listbox you need to set the property
        *> "Use Tabstops" for the Listbox control in the Dialog Editor.
        *> You have to do a bit of 'fiddling' with "78 Kounter" below
        *> using a value which gives you an aesthetically pleasing
        *> Listbox

        *> Rather than 'manually' figuring out the tab positioning, let
        *> the method calculate the positioning !

        01 n                        pic x(4) comp-5.
        78 tabcount                 value 2.
        78 Kounter                  value 10.
        01 TabsPos occurs tabcount  pic x(4) comp-5.
        01 TabsVal occurs tabcount  pic x(4) comp-5.

        Linkage section.
        copy "\copylib\collection.cpy" replacing ==(tag)== by ==lnk==.
        01 lnk-tabstops             object reference.

        Procedure Division using     lnk-CollectionParams
                           returning lnk-tabstops.

          evaluate true
           when ByName
              compute TabsPos(1) =
                ( function length (Des-Name of Name-record ) + 1 )
              compute TabsPos(2) = TabsPos(1) +
                ( function length (Des-Code of Name-record ) + 1 )

           when ByKey
              compute TabsPos(1) =
                ( function length (Des-Code of Key-record ) + 1 )
              compute TabsPos(2) = TabsPos(1) +
                ( function length (Des-Name of Key-record ) + 1 )

           when ByOther
              compute TabsPos(1) =
                ( function length (Des-Other1 of Other-Record )) +
                ( function length (Des-Other2  of Other-Record )) +  1
              compute TabsPos(2) = TabsPos(1) +
                ( function length (Des-Name of Other-record )  + 1 )

          End-evaluate

          move tabcount to n
          invoke Array "ofValues" using     cobolcomp5, n
                                  returning lnk-tabstops

          perform varying n from 1 by 1 until n > tabcount
             multiply TabsPos(n) by Kounter giving TabsVal(n)
             invoke lnk-tabstops "atPut" using n TabsVal(n)
          End-perform

        End method "createDialogTabstops".
        *>--------------------------------------------------------------
        Method-id. "createDictionary".
        *>--------------------------------------------------------------
        Working-storage section.
        01 ws-MethodName        pic x(30) value "newDictionaryRecord".
        Linkage section.
        copy "\copylib\collection.cpy" replacing ==(tag)== by ==lnk==.
        01 lnk-Rectype                       pic 9(02).
        01 lnk-values.
         copy "\copylib\sqlResult.cpy" replacing ==(tag)== by ==05 lnk==.
         05 lnk-Dictionary                 object reference.

        Procedure division using     lnk-CollectionParams
                                     lnk-Rectype
                           returning lnk-values.

         set ResultOK              to true
         move zeroes               to ws-LastNumber
         move lnk-CollectionParams to ws-CollectionParams
         invoke super "initDictionary"
                using ws-CollectionParams returning os-List
         set lnk-Dictionary to os-List
         invoke super "makeCollection"
                using     self
                          ws-MethodName
                          lnk-RecType
                returning lnk-SqlResult

         *>   NOTE : super (DesTable) using iteration, invokes
         *>  "newDictionaryRecord" in this Class

        End Method "createDictionary".
        *>---------------------------------------------------------
        Method-id. "createScheduleCollection".
        *>------------------------------------------------------------

        *> As the following is a static list, the data is stored here for
        *> convenience, rather than create a file or DB table.

        Working-Storage section.
        01 s1 pic x(52) value
        "Std,XS ,XXS,10 ,20 ,30 ,40 ,60 ,80 ,100,120,140,160,".
        01 s2 redefines s1.
          05  occurs 13.
              10 ScheduleCode pic x(03).
              10              pic x.

        01 n                        pic 9(04).
        01 ls-length                pic x(4) comp-5.
        01 ls-size                  pic x(4) comp-5.
        01 ls-string                object reference.

        Linkage section.
        01 lnk-Collection            object reference.

        Procedure division returning lnk-Collection.

         initialize                    ws-CollectionParams
         move length of Name-record to ws-NameLength
         move 13                    to ws-size
         set ofReferences           to true
         invoke super "initSortedCollection"
            using ws-CollectionParams returning os-List
         set lnk-Collection to os-List
         initialize Name-record

          perform varying n from 1 by 1 until n > 13

             move ScheduleCode(n) to Des-Name of Name-record
             move n               to Des-Code of Name-record

             if   OfReferences
                  move x"09" to Tab1 of Name-record
                  invoke CharacterArray "withLengthValue"
                         using     ls-Length
                                   Name-record
                         returning ls-string
                  invoke os-List "add"
                         using ls-string returning ls-string

             else  invoke os-List "add" using Name-record
          End-perform

        End Method "createScheduleCollection".
        *>------------------------------------------------------------
        Method-id. "getLastNumber".
        *>------------------------------------------------------------
        Linkage section.
        01 lnk-number                    pic 9(03).
        Procedure division returning lnk-number.
          move ws-LastNumber to lnk-number
        End Method "getLastNumber".
        *>------------------------------------------------------------
        Method-id. "getIndexFromKey".
        *>------------------------------------------------------------
        copy "\copylib\lnkObjVal.cpy" replacing ==(tag)== by ==ls==.
        *>------------------ lnkobjval.cpy------------------------------

        01  ls-ObjectValues.
           05 ls-Index                          pic x(4) comp-5.
           05 ls-RecLength                      pic x(4) comp-5.
           05 ls-Record.
              10 occurs 1 to 100
                 depending on ls-RecLength      pic x.
           05 ls-ObjectString                   object reference.

        *>--------------------------------------------------------------

        Linkage section.
        01 lnk-Key                       pic x(04).
        01 lnk-Index-Out                 pic x(4) comp-5.

        Procedure Division using     lnk-Key
                           returning lnk-Index-OUT.

         invoke super "getObjectFromKey"
                using lnk-Key returning ls-ObjectValues
         move ls-Index to lnk-Index-Out

        End Method "getIndexFromKey".
        *>------------------------------------------------------------
        Method-id. "getKeyFromIndex".
        *>------------------------------------------------------------
        copy "\copylib\lnkObjVal.cpy" replacing ==(tag)== by ==ls==.
        Linkage section.
        01 lnk-Index-In                  pic x(4) comp-5.
        01 lnk-Key                       pic x(04).

        Procedure Division using     lnk-Index-IN
                           returning lnk-Key.

         move spaces to lnk-key
         invoke super "getObjectFromIndex"
                using lnk-Index-In returning ls-ObjectValues
         move ls-record(PosKey of ws-Pos : 4 ) to lnk-key

        End Method "getKeyFromIndex".
        *>------------------------------------------------------------
        Method-id. "getScheduleNameObject".
        *>---------------------------------------------------------
        01 n                                 pic x(4) comp-5.
        01 ls-length                         pic x(4) comp-5.
        01 ls-length2                        pic x(4) comp-5.
        01 ls-size                           pic x(4) comp-5.
        01 ls-string                         object reference.

        Linkage section.
        01 lnk-key                           pic x(04).
        01 lnk-Collection                    object reference.
        01 lnk-values.
           05 lnk-result                     pic 9(03).
           05 lnk-NameObject                 object reference.

        Procedure Division using     lnk-Key,
                                     lnk-Collection
                           returning lnk-values.

        move length of Name-record             to ls-length
        move length of Des-Name of Name-Record to ls-length2
        move 99                                to lnk-result

        invoke lnk-Collection "size" returning ls-size
        perform varying n from 1 by 1 until n > ls-size

           invoke lnk-Collection "at"
                  using n returning ls-string
           invoke ls-string "GetValueWithSize"
                  using ls-length returning Name-record

           if  lnk-key = Des-Code of Name-record
               move zeroes to lnk-result
               set lnk-NameObject to ls-string
               EXIT PERFORM
           End-if

        End-perform

        if    lnk-result <> zeroes
              move "* Invalid Name *" to Des-Name of Name-Record
              invoke CharacterArray "withLengthValue"
                 using ls-length2, Des-Name of Name-record
                 returning lnk-NameObject
        End-if

        End Method "getScheduleNameObject".
        *>---------------------------------------------------------
        Method-id. "makeListRecord".
        *>------------------------------------------------------------

           Evaluate true
             when ByName
               move CORR ws-DesListRecord to Name-record

               if TypeTabbed
                   move x'09' to Tab1 of Name-record
                                 Tab2 of Name-record
               End-if

               move Name-record to ws-TempRecord

             when ByKey
               move CORR ws-DesListRecord to Key-record

               if TypeTabbed
                   move x'09' to Tab1 of Key-record
                                 Tab2 of Key-record
               End-if

               move Key-Record to ws-TempRecord

             when other
               move CORR ws-DesListRecord to Other-record

               if TypeTabbed
                   move x'09' to Tab1 of Other-record
                                 Tab2 of Other-record
               End-if

               move Other-Record to ws-TempRecord
           End-evaluate

        if   ( Des-Code of ws-DeslistRecord is numeric      ) and
             ( Des-Code of ws-DeslistRecord > ws-LastNumber )
             move Des-code of ws-DeslistRecord to ws-LastNumber
        End-if

        End Method "makeListRecord".
        *>----------------------------------------------------------
        Method-id. "newDictionaryRecord".
        *>-------------------------------------------------------------

        *>  ***** invoked from super (DesTable) by iteration

        Linkage section.
        copy "deslist.cpy" replacing ==(tag)== by ==lnk==.

        Procedure division using lnk-DesListRecord.

          Evaluate true
            when ByKeyOnly
              invoke super "addToDictionary"
                     using Des-Code
                           Des-Name(1:1)

            when ByKeyAndName
            when ByKeyAndData

              invoke super "addToDictionary"
                     using Des-Code
                           Des-Name

            when ByKeyAndOther
              invoke super "addToDictionary"
                     using Des-Code
                           Des-Other1

            when ByNameAndKey
              invoke super "addToDictionary"
                     using Des-Name
                           Des-Code

            when RmPointsDictionary
              invoke super "addToDictionary"
                     using Des-Other2
                           Des-Code

          End-evaluate

        End Method "newDictionaryRecord".
        *>-------------------------------------------------------------

        End OBJECT.
        End CLASS DescripsDBI.

*>-----------------------------------------------------------------------

**** SQL DES TABLE ( I use MS Access) :

This is the final piece and the bit directly applicable to creating 
Collections is method "makeCollection". Note I've used COBOL syntax 
"wrtie" and "rewrite" as method-names - the intention of that - in 
classes invoking the data either COBOL ISAMs or SQL Tables can invoke 
"write" "rewrite" and it is only the object ISAMfile or SQLtable that is 
changed.

       $set sql (dbman=ODBC)

        *>-----------------tabdes.cbl----------------------------------

        Class-id.          DescriptionsTable
                           inherits from Base.

        Class-control.     DescriptionsTable       is class "tabdes"
                           SqlErrors               is class "sqlerror"
                           .

        *> NOTE : For creation of Collections, selective Descriptions
        *> fields are moved to the shorter format xxxListRecord, which is
        *> returned to the invoking DBIMain

        *>---------------------------------------------------------------
        FACTORY.
        *>---------------------------------------------------------------
        End FACTORY.
        *>------------------------------------------------------------
        OBJECT.
        *>----------------------------------------------------------
        OBJECT-STORAGE SECTION.
        EXEC SQL INCLUDE SQLCA    END-EXEC
        copy "descriptions.cpy".
        *> **** The following was generqted by the M/E ESQL

            EXEC SQL DECLARE
            `Descriptions` TABLE
            ( "DesID"               VARCHAR(2)
             ,"DesCode"             VARCHAR(4)
             ,"DesName"             VARCHAR(40)
             ,"DesOther1"           VARCHAR(10)
             ,"DesOther2"           VARCHAR(10)
             ,"LastUpdated"         VARCHAR(16)
            ) END-EXEC.
       ******************************************************************
       * COBOL DECLARATION FOR TABLE Descriptions                       *
       ******************************************************************
        01  DCLTable.
            03 A-DesID                          PIC X(2).
            03 A-DesCode                        PIC X(4).
            03 A-DesName                        PIC X(40).
            03 A-DesOther1                      PIC X(10).
            03 A-DesOther2                      PIC X(10).
            03 A-LastUpdated                    PIC X(16).
       ******************************************************************
       * COBOL INDICATOR VARIABLES FOR TABLE                            *
       ******************************************************************
        01  DCLTable-NULL.
            03 A-DesID-NULL                     PIC S9(04)  COMP-5.
            03 A-DesCode-NULL                   PIC S9(04)  COMP-5.
            03 A-DesName-NULL                   PIC S9(04)  COMP-5.
            03 A-DesOther1-NULL                 PIC S9(04)  COMP-5.
            03 A-DesOther2-NULL                 PIC S9(04)  COMP-5.
            03 A-LastUpdated-NULL               PIC S9(04)  COMP-5.

        *>copy "desrecrd.cpy"
        *>             replacing ==(tag1)== by ==01 Descriptions-Record==,
        *>                       ==(tag)== by ==Descriptions==.
        copy "\copylib\sqlerror.cpy" replacing ==(tag)== by ==ws==.

        01 ws-dbID              pic 9 value 1. *> General

        01 os-DateTime          object reference.
        01 os-Parent            object reference.
        01 os-SqlConnection     object reference.

        *>--------------------------------------------------------------
        Method-id. "begin".
        *>--------------------------------------------------------------
        Linkage section.
        01 lnk-DateTime                    object reference.
        01 lnk-Parent                      object reference.
        01 lnk-Sql                         object reference.

        Procedure Division using lnk-DateTime, lnk-Parent, lnk-Sql.

           set os-DateTime          to    lnk-DateTime
           set os-Parent            to    lnk-Parent
           set os-SqlConnection     to    lnk-Sql
           initialize                     ws-ErrorParams
           move "Descriptions"      to    ws-TableName

        End Method "begin".
        *>--------------------------------------------------------------
        Method-id. "checkForRectype".
        *>--------------------------------------------------------------
        Linkage section.
        01 lnk-rectype                      pic x(02).
        copy "\copylib\sqlResult.cpy" replacing ==(tag)== by ==01 lnk==.

        Procedure Division using lnk-rectype
                          returning lnk-SqlResult.

         set ResultOK to true
         invoke os-SqlConnection "setCurrentConnection"
                using ws-dbID returning lnk-SqlResult

         if TableError
            EXIT METHOD
         End-if

         move lnk-Rectype to A-DesID

             EXEC SQL
              SELECT DISTINCT
                     `A`.`DesID`
              INTO
                   :A-DesID:A-DesID-NULL
                FROM `Descriptions` A
               WHERE ( `A`.`DesID` = A-DesID )
             END-EXEC

         Evaluate true
             when SQLSTATE = "00000"
                  continue
             when SQLSTATE = "02000"              *> No more rows
                  set RecNotFound to true
             when other
                  move lnk-rectype to ws-ErrorID
                  invoke self "setErrorMessage"
                  set TableError to true
         End-Evaluate

        End Method "checkForRectype".
        *>--------------------------------------------------------------
        Method-id. "deleteRecord".
        *>--------------------------------------------------------------
        Linkage section.
        01 lnk-Key.
           05 lnk-DesID                pic x(02).      *> = Record Type
           05 lnk-DesCode              pic x(04).
        copy "\copylib\sqlResult.cpy" replacing ==(tag)== by ==01 lnk==.

        Procedure Division using     lnk-Key
                           returning lnk-SqlResult.

           set ResultOK to true
           invoke os-SqlConnection "setCurrentConnection"
                  using ws-dbID returning lnk-SqlResult

           if TableError
              EXIT METHOD
           End-if

           move lnk-DesId   to A-DesID
           move lnk-DesCode to A-DesCode

             EXEC SQL
              DELETE FROM `Descriptions`
               WHERE ( `DesID` = :A-DesID )
                AND  ( `DesCode` = :A-DesCode )
             END-EXEC

           Evaluate true
              when SQLSTATE = "00000"
                   EXEC SQL COMMIT END-EXEC
              when SQLSTATE = "02000"           *> No more rows
                   set RecNotFound to true
              when other
                   move lnk-Key to ws-ErrorID
                   invoke self "setErrorMessage"
                   set TableError to true
          End-evaluate

        End Method "deleteRecord".
        *>--------------------------------------------------------------
        Method-id. "makeCollection".
        *>--------------------------------------------------------------
        Local-storage section.
        copy "deslist.cpy" replacing ==(tag)== by ==ls==.
        Linkage section.
        01 lnk-DBI                      object reference.
        01 lnk-MethodName               pic x(30).
        01 lnk-rectype                  pic x(02).
        copy "\copylib\sqlResult.cpy" replacing ==(tag)== by ==01 lnk==.

        Procedure Division using     lnk-DBI
                                     lnk-MethodName
                                     lnk-Rectype
                           returning lnk-SqlResult.

         set ResultOK to true
         invoke os-SqlConnection "setCurrentConnection"
                using ws-dbID returning lnk-SqlResult

         if TableError
            EXIT METHOD
         End-if

         move lnk-rectype to A-DesID

             EXEC SQL
              DECLARE CSR80 CURSOR FOR SELECT DISTINCT
                     `A`.`DesID`
                    ,`A`.`DesCode`
                    ,`A`.`DesName`
                    ,`A`.`DesOther1`
                    ,`A`.`DesOther2`
                FROM `Descriptions` A
               WHERE ( `A`.`DesID` = :A-DesID )
             END-EXEC
             EXEC SQL OPEN CSR80 END-EXEC
             PERFORM UNTIL SQLSTATE <> "00000"
               EXEC SQL
               FETCH CSR80 INTO
                   :A-DesID:A-DesID-NULL
                  ,:A-DesCode:A-DesCode-NULL
                  ,:A-DesName:A-DesName-NULL
                  ,:A-DesOther1:A-DesOther1-NULL
                  ,:A-DesOther2:A-DesOther2-NULL
               END-EXEC

                Evaluate true

                  when SQLSTATE = "00000"
                    initialize ls-DesListRecord
                    move A-DesID     to Des-ID
                    move A-DesCode   to Des-Code
                    move A-DesName   to Des-Name
                    move A-DesOther1 to Des-Other1
                    move A-DesOther2 to Des-Other2
                    invoke lnk-DBI lnk-MethodName using ls-DesListRecord

                  when SQLSTATE = "02000"               *> no more rows
                    set ResultOK to true
                    EXIT PERFORM
                  when other
                    move A-DesID   to ws-ErrorID(1:2)
                    move A-DesCode to ws-ErrorID(3:4)
                    invoke self "setErrorMessage"
                    set TableError to true
                    EXIT PERFORM
                End-evaluate

             END-PERFORM

             EXEC SQL CLOSE CSR80 END-EXEC

        End Method "makeCollection".
        *>--------------------------------------------------------------
        Method-id. "readRecord".
        *>--------------------------------------------------------------
        Linkage section.
        01 lnk-key.
           05 IN-DesID                    pic 9(02).
           05 IN-DesCode                  pic x(04).
        01 lnk-record.
         copy "\copylib\sqlResult.cpy" replacing ==(tag)== by ==05 lnk==.
         copy "desrecrd.cpy" replacing ==(tag1)== by ==05 lnk-data==,
                                       ==(tag)==  by ==lnk==.

        Procedure Division using     lnk-key
                           returning lnk-record.

         set ResultOK to true
         invoke os-SqlConnection "setCurrentConnection"
                using ws-dbID returning lnk-SqlResult

         if TableError
            EXIT METHOD
         End-if

         initialize          lnk-record
         move IN-DesID    to A-DesID
         move IN-DesCode  to A-DesCode
         move lnk-key     to ws-ErrorID

             EXEC SQL
              SELECT DISTINCT
                     `A`.`DesID`
                    ,`A`.`DesCode`
                    ,`A`.`DesName`
                    ,`A`.`DesOther1`
                    ,`A`.`DesOther2`
                    ,`A`.`LastUpdated`
              INTO
                   :A-DesID:A-DesID-NULL
                  ,:A-DesCode:A-DesCode-NULL
                  ,:A-DesName:A-DesName-NULL
                  ,:A-DesOther1:A-DesOther1-NULL
                  ,:A-DesOther2:A-DesOther2-NULL
                  ,:A-LastUpdated:A-LastUpdated-NULL
                FROM `Descriptions` A
               WHERE ( `A`.`DesID` = :A-DesID )
                AND  ( `A`.`DesCode` = :A-DesCode )
             END-EXEC

         Evaluate true
             when SQLSTATE = "00000"
                  move DCLTable to lnk-data
             when SQLSTATE = "02000"              *> No more rows
                  set RecNotFound to true
             when other
                  move lnk-key to ws-ErrorID
                  invoke self "setErrorMessage"
                  set TableError to true
         End-Evaluate

        End Method "readRecord".
        *>--------------------------------------------------------------
        Method-id. "rewriteRecord".
        *>--------------------------------------------------------------
        Linkage section.
        copy "desrecrd.cpy" replacing ==(tag1)== by ==01 lnk-record==,
                                      ==(tag)==  by ==lnk==.
        copy "\copylib\sqlResult.cpy" replacing ==(tag)== by ==01 lnk==.

        Procedure Division using lnk-record
                           returning lnk-SqlResult.

         set ResultOK to true
         invoke os-SqlConnection "setCurrentConnection"
                using ws-dbID returning lnk-SqlResult

         if TableError
            EXIT METHOD
         End-if

         move lnk-DesPrimeKey to ws-ErrorID
         move lnk-Record to DCLTable
         invoke os-DateTime "getDateModified" returning A-LastUpdated

             EXEC SQL
              UPDATE `Descriptions`
               SET `DesID` = :A-DesID:A-DesID-NULL
                  ,`DesCode` = :A-DesCode:A-DesCode-NULL
                  ,`DesName` = :A-DesName:A-DesName-NULL
                  ,`DesOther1` = :A-DesOther1:A-DesOther1-NULL
                  ,`DesOther2` = :A-DesOther2:A-DesOther2-NULL
                  ,`LastUpdated` = :A-LastUpdated:A-LastUpdated-NULL
               WHERE ( `DesID` = :A-DesID )
                AND  ( `DesCode` = :A-DesCode )
             END-EXEC

         Evaluate true
            when SQLSTATE = "00000"
              EXEC SQL COMMIT END-EXEC
            when other
              invoke self "setErrorMessage"
              set TableError to true
         End-evaluate

        End Method "rewriteRecord".
        *>--------------------------------------------------------------
        Method-id. "setErrorMessage".
        *>--------------------------------------------------------------
        01 ls-object                     object reference.

        Procedure Division.

          move SQLSTATE to ws-SqlState
          invoke SqlErrors "new" returning ls-object
          invoke ls-object "sqlstateMessage"
                 using os-Parent, ws-ErrorParams
          invoke ls-object "finalize" returning ls-object

        End Method "setErrorMessage".
        *>--------------------------------------------------------------
        Method-id. "writeRecord".
        *>--------------------------------------------------------------
        Linkage section.
        copy "desrecrd.cpy" replacing ==(tag1)== by ==01 lnk-record==,
                                      ==(tag)==  by ==lnk==.
        copy "\copylib\sqlResult.cpy" replacing ==(tag)== by ==01 lnk==.
        *>----------sqlresult.cpy-------------------------------------

            01 lnk-SqlResult                        pic 9(03).

            88  ResultOK                    value 0.
            88  FileResultOK                value 0.

            88  RecNotFound                 value 1.

            88  NoMoreRows                  value 2.
            88  FileFinis                   value 2.

            88  DuplicateKey                value 3.

            88  DataError                   value 97.
            88  CursorError                 value 98.

            88  FileError                   value 99.
            88  TableError                  value 97 thru 99.

        *> This copyfile is multi-purpose with Level 88s used for either
        *> COBOL files or (R)DBMS Tables using SQLERROR or SQLSTATE

        *>-------------------------------------------------------------

        Procedure Division  using     lnk-Record
                            returning lnk-SqlResult.

         set ResultOK to true
         invoke os-SqlConnection "setCurrentConnection"
                using ws-dbID returning lnk-SqlResult

         if TableError
            EXIT METHOD
         End-if

        move lnk-DesId  to ws-ErrorID
        move lnk-record to DCLTable
        invoke os-DateTime "getDateModified" returning A-LastUpdated

             EXEC SQL
              INSERT INTO `Descriptions`
              (`DesID`
              ,`DesCode`
              ,`DesName`
              ,`DesOther1`
              ,`DesOther2`
              ) VALUES
              (:A-DesID:A-DesID-NULL
              ,:A-DesCode:A-DesCode-NULL
              ,:A-DesName:A-DesName-NULL
              ,:A-DesOther1:A-DesOther1-NULL
              ,:A-DesOther2:A-DesOther2-NULL
              )
             END-EXEC

           Evaluate true
             when SQLSTATE = "00000"
                  EXEC SQL COMMIT END-EXEC
             when other
                  invoke self "setErrorMessage"
                  set TableError to true
           End-evaluate

        End Method "writeRecord".
        *>--------------------------------------------------------------

        END OBJECT.
        End CLASS DescriptionsTable.
        *>---------------------------------------------------------------

*****************************************************************

OK above is not picture perfect and can probably be fine tuned. Even if 
you don't buy the approach, hopefuly some of it might illustrate some 
useful coding techniques associated with OO.

GET/SET - really don't want to get into that one. As far as I recall GET 
and SET methods were not available for me in N/E V 1.0, 2.0. - so I 
parameter pass. I can see the documentation for them in the N/E 4.0 
manuals and truly they seem somwehat kludgy to me. Just like F/J does, I 
believe, M/F generates a separate copyfile for GET/SET methods.

OK - I've only partially checked the above with a spell-checker - I 
realy don't want to sit for hours where it queries every bit of COBOL 
shorthand :-)

If I've failed to include any copyfiles you would need to understand the 
coding then e-mail me privately for the missing copyfiles.

Jimmy
```

#### ↳ Re: invoking a method

- **From:** "William M. Klein" <wmklein@nospam.netcom.com>
- **Date:** 2006-12-06T22:01:47+00:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<e5Hdh.303866$c42.300406@fe02.news.easynews.com>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net>`

```
I raised this issue during the '02 Standard development (along with WHY the 
method had to be a literal, when CALL can have an identifier).  Oh, well !!!

P.S.  You may want to look at Micro Focus "vocabularies" that let you "write 
your own syntax" (that uses OO under the covers).
```

##### ↳ ↳ Re: invoking a method

- **From:** "James J. Gavan" <jgavandeletethis@shaw.ca>
- **Date:** 2006-12-06T22:37:26+00:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<GCHdh.435324$5R2.119525@pd7urf3no>`
- **In-Reply-To:** `<e5Hdh.303866$c42.300406@fe02.news.easynews.com>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net> <e5Hdh.303866$c42.300406@fe02.news.easynews.com>`

```
William M. Klein wrote:
> I raised this issue during the '02 Standard development (along with WHY the 
> method had to be a literal, when CALL can have an identifier).  Oh, well !!!
…[3 more quoted lines elided]…
> 
True Bill it is a literal, but nothing to stop you doing the following :-

W/S
----------------------
01 MyLastMethod pic x(30) value "addLast".

invoke myCollection MyLastMethod using xxxx

(Actually I would use a Level 78 to signify it is a Constant).

And a variation on above, which I have used but can't recall the 
location of the code I did, so example is purely illustrative :-

Method-id. "xxxxx".
------------------
Working-Storage Section. *> (non-standard - M/F only and existed BEFORE 
COBOL 2002).

01 M1.
  05 pic x(30) value "HidetheObject.
  05 pic x(30) value "HighlightTheObject".

01 MethodName redefines M1 occurs 2 pic x(30).

Procedure Division.

If	FinishedWith
	invoke MyDialog Methodname(1) using xxxx

else    invoke MyDialog Methodname(2) using xxxx
End-if


Your suggestion about VOCABULARY - honestly, never found where I wanted 
to use it.

Jimmy
```

#### ↳ Re: invoking a method

- **From:** "andrewmcdonagh" <andrewmcdonagh@gmail.com>
- **Date:** 2006-12-08T09:44:44-08:00
- **Newsgroups:** comp.lang.cobol
- **Message-ID:** `<1165599884.335702.151130@n67g2000cwd.googlegroups.com>`
- **In-Reply-To:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net>`
- **References:** `<50lch.5827$ql2.1611@newsread3.news.pas.earthlink.net>`

```


On Dec 2, 8:05 pm, Frank Swarbrick <info...@earthlink.net> wrote:
> Looking at examples supplied with MF Net Express reminded me of
> something that seems odd to me about all OO languages I've looked in to.
…[19 more quoted lines elided]…
>

Its good observation, especially given that in OO we are not 'invoking
a method on an Object' we are in fact 'sending a Message to an Object'

Methods are the language's name for Message.

so in your examples above, the OO way of thinking about them would be

Send the Message 'method' with these Parameter Values to Object

The problem with this though, is its verbosity, hence why most
languages opt for the means you described.


> where 'insert' is the method name, 'employee' is the object to be
> inserted (the parameter) and 'list' is the object that is to receive the
…[78 more quoted lines elided]…
>


> }Objective-C:
> {
…[41 more quoted lines elided]…
>

Just a minor point, but in Java and Ruby, you can achieve the same
results as your code, in much less.....(I don't know the other
languages so can't comment on them)

Regards

Andew
```

---

[← Index](../README.md) · [Topics](../topics.md) · [Years](../years.md) · [Subjects](../subjects.md) · [Authors](../authors.md)
